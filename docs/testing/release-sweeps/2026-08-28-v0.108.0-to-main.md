# Pre-release sweep — v0.108.0 → main (2026-08-28)

| | |
|---|---|
| Range | `v0.108.0` (`fe6b01cb9`, 2026-08-26) → `origin/main` `54be3ee4a` |
| Gate | PRs waited on: #4163 (What's New v0.109.0) ; merged: #4163 @ `54be3ee4a` (CI Success green on head b06570bae) ; known-noise dismissed: Trivy `CVE-2026-14456` libcrypto3 base-image (confirmed in run 33183694170 log) |
| Stack | `pnpm wt-stack up` on `qa/sweep-post-v0.108.0` @ `54be3ee4a`, baseUrl `http://localhost:32873` |
| Flags enabled | `BREEZE_AI_AGENTS_ENABLED`, `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED`, `LLM_PROVIDER_CATALOG_ENABLED`, `BREEZE_WORKSPACE_ENABLED`, (all `true`; `MCP_OAUTH_ENABLED` already on). #4097 flags left at shipping default `false` — `AUTH_BROWSER_TERMINAL_PREPARATION_ENABLED=true` alone is refused at boot with a clear validate.ts message (`requires AUTH_BROWSER_TRANSITIONS_ENFORCED=true`) ✅ |
| Driver | skill `pre-release-sweep`; sweep agents: … |

## Change inventory

Status ∈ `TODO | PASS | PARTIAL | FAIL | BLOCKED | N/A`. Every row must leave `TODO`.

| PR | Merged | Surface | Title | Route / click-path | Expected visible outcome | Prereq | Status |
|---|---|---|---|---|---|---|---|
| 4066 | 08-26 14:47 | docs/ci/chore | chore(deps): bump github-actions group | — (`.github/workflows/*` only) | n/a | n/a | N/A — docs/ci only, no UI surface. |
| 4091 | 08-26 19:05 | api-only (+agent) | fix(agent,api): self-host→hosted update transition + edition-aware offer gating | No UI surface. `agent/internal/updater/edition.go` + heartbeat `agentEdition` reporting only. | Not user-visible; a self-host agent in the 0.105–0.106 band should now accept a hosted-edition update instead of wedging at `status='updating'`. | BLOCKED on a plain stack — requires a real self-host-edition agent binary in the affected version band; can't be triggered from the web UI. | N/A — backend/agent only, smoke OK (no UI surface; needs a real self-host-edition agent in the 0.105–0.106 band). |
| 4071 | 08-26 19:21 | web | feat(api,web): lockout preflight + confirm-through before Enforce SSO | Settings → **SSO** (`/settings/sso`) → edit/create a provider → toggle **Enforce SSO** on → Save | If any active user lacks a link to the effective login provider, a **"Users will be locked out"** modal appears listing unlinked users, requiring the checkbox "I understand these users will be unable to sign in until they connect SSO" before **Enable enforcement** is clickable. | Needs an SSO provider already configured (no live IdP needed — preflight only reads membership/linkage) and at least one active unlinked user (the seeded admin, if never SSO-linked, qualifies and would show `enforceLockoutSelfWarning`). | PASS |
| 4083 | 08-26 19:21 | api-only | fix(api): delivery idempotency and atomic winners (AI agents wave 3.5a) | No standalone UI; makes `resolveAlert()` compare-and-swap. Verify via the #4099 click-path below (resolve races). | Two simultaneous resolutions of the same alert should produce exactly one `alert.resolved` event / one escalation-cancel, not two. | Hard to demonstrate via a single browser session; needs two concurrent resolve calls (e.g. two tabs) on the same active alert. | N/A — backend only, smoke OK |
| 4070 | 08-26 19:22 | docs/ci/chore | docs(release-skill): pre-tag What's New entry step | `.claude/skills/release/SKILL.md` only | n/a | n/a | N/A — docs/ci only, no UI surface. |
| 4080 | 08-26 19:56 | web | [Auth][SSO] Link-on-first-SSO-login ceremony | Login page → click org's SSO button → IdP redirects back with an email match to a password-holding, unlinked account → app routes to `/auth/connect-sso` ("Connect your sign-in") instead of `?error=sso_link_required` | User is prompted to re-enter their password (+ MFA if enrolled); on success `user_sso_identities` link is created and login completes normally. | BLOCKED without a working test IdP (SAML/OIDC) wired to a seeded SSO provider — the ceremony only triggers off a real SSO callback with an email match. | BLOCKED |
| 4099 | 08-26 23:23 | web | fix(api): route every alert-resolve path through the compare-and-swap | `/alerts` (list) or `/alerts/:id` (detail) → click **Resolve** on an active alert | Alert transitions to Resolved once; no duplicate `alertsPage.alertResolved` toast / no double escalation-cancel. Regression check: resolving via `AlertsPage` bulk action, `AlertDetailPage`, and alert-correlations group-resolve should all be identical CAS-guarded behavior. | Needs at least one active alert in the seed. Race behavior (the actual bug) needs two near-simultaneous resolves to observe; a single resolve just confirms no regression. | PASS — first resolve toasts success and moves the counters; the stale-drawer second resolve returns `409 {"error":"Alert is already resolved"}` and surfaces it as an error toast with the row self-correcting to Resolved. Exactly one `alert.resolve` audit row for two attempts. Ack/suppress/rules/channel create+test+delete all PASS; paper cut 34 (delete-channel confirm copy). |
| 4098 | 08-26 23:23 | api-only | fix(api): recover webhook deliveries recorded but never enqueued | Indirectly: Settings → **Webhooks** (`/settings/webhooks`) → delivery history for a configured webhook | A delivery stuck in "recorded but not enqueued" should recover and show delivered instead of stuck. | BLOCKED for a simple click-path — requires contriving a recorded-but-unenqueued row (e.g. Redis restart mid-enqueue); not reproducible by clicking. | N/A — backend only, smoke OK (`/settings/webhooks` loads clean with a proper empty state; the recorded-but-unenqueued state cannot be contrived by clicking). |
| 4117 | 08-27 06:13 | api-only | feat(api): wave 3.5c — durable event dispatch (BullMQ ingress + per-subscriber delivery receipts) | No direct UI; backend event-bus rework. Regression-smoke via any alert/automation/webhook flow. | Alerts, automations, and webhooks should keep firing exactly as before (event delivery now durable/BullMQ-backed with per-subscriber receipts). | Needs Redis (already required); no new env flags. Not independently clickable — verify as a smoke pass over Alerts/Automations/Webhooks. | N/A — backend only, smoke OK |
| 4087 | 08-27 06:33 | docs/ci/chore | docs: v0.108.0 sweep — AI agents docs page | `apps/docs/**` content only | n/a | n/a | N/A — docs only (`apps/docs/**`), no UI surface. |
| 4096 | 08-27 06:33 | docs/ci/chore | docs: land 2026-08-24 UI QA sweep + docs-citation rule | `.claude/skills/**`, `docs/**` only | n/a | n/a | N/A — docs/skills only, no UI surface. |
| 4110 | 08-27 06:33 | docs/ci/chore | docs(plans): BYO-LLM phase 2 + workspace enrichment BYOK plans | `docs/superpowers/plans/**` only | n/a | n/a | N/A — docs/plans only, no UI surface. |
| 4112 | 08-27 06:33 | web (nominal) | feat: workspace enrichment honors partner BYOK | Settings → **Partner** → **AI Provider** tab (`/settings/partner#ai-provider`, pre-existing tab) — the web diff here is a code comment only, no new copy. Real change is `ee/workspace` enrichment routing + Settings → **AI Usage & Budget** (`/settings/ai-usage`) billing-source attribution. | A workspace content-enrichment run should bill to the org's configured BYOK key (`billing_source='partner_key'`) instead of always the platform key; a broken BYOK config should surface a visible enrichment failure. | BLOCKED without `BREEZE_WORKSPACE_ENABLED=true` (default off, commented out in `.env.example`) and a running workspace/catalog ingest pipeline — no simple UI trigger. | BLOCKED |
| 4113 | 08-27 06:33 | web | feat: llm provider catalog foundations (#3922 W01) | Log in as **platform admin** → Sidebar → Settings section → **LLM Provider Catalog** (`/admin/llm-provider-catalog`) → **Add provider** → fill form → **Create** revision → **Verify** → **Activate** | New provider entry appears in the catalog list; verify performs a live round-trip tool call against the declared base URL before allowing activation; activated revision becomes selectable downstream (#4118). | `platformAdminOnly` nav item — requires platform-admin role, not just org/partner admin. **Verify** step calls a real external base URL — BLOCKED for full pass without a reachable OpenAI-compatible/gateway endpoint (a local echo/stub endpoint would substitute). | PARTIAL |
| 4115 | 08-27 06:53 | api-only | feat: llm egress enforcement layer (#3922 W02) | No UI; egress-proxy + cost-tracking backend for catalog traffic. | AI sessions routed through a catalog endpoint should be metered/guarded (SSRF-safe egress, correct pricing). | BLOCKED for standalone QA — only observable through #4118's endpoint-selection UI plus a live AI chat/agent session. | N/A — backend only, smoke OK (egress proxy; only observable through #4118 plus a live AI session — see Group 1). |
| 4126 | 08-27 07:13 | web | feat(enrollment): default installer keys to 30 days / 50 devices | Devices → **Add Device** (`/devices`, click "Add Device") — default expiry picker and device-count field | Expiry defaults to **30 days** (was 24h) and device count defaults to **50** (was 1). Same defaults apply on Settings → **Enrollment Keys** (`/settings/enrollment-keys`) → new key. | None — visible immediately on a fresh seed. | PASS — Add Device defaults measured at `device-count=50` / `link-ttl=43200` ("30 days"); install command + share link both read "50"; blank-expiry key on Settings → Enrollment Keys lands at exactly 30 days; revoke toasts success. Paper cuts 31/32 (Max Usage still defaults to 1 against an "Unlimited" placeholder; Generate Link writes a stray "0 / 1" parent). |
| 4133 | 08-27 10:20 | api-only | feat(api): wave 3.5b — socket-affinity command relay | No UI; agent-WS command relay/fenced presence for multi-instance API. | Agent command dispatch should keep working identically in single-instance dev; only observable under a scaled/worker-split deployment. | BLOCKED for single-instance local stack — needs multiple API replicas to exercise the affinity/fencing logic. | N/A — backend only, smoke OK |
| 4138 | 08-27 11:56 | api-only | fix(api): ordered graceful shutdown (3.5d part A) | No UI; server shutdown-sequencing fix. | Not observable via browser; verify via `docker compose stop`/SIGTERM behavior in logs. | Not a UI QA item. | N/A — backend only, smoke OK (shutdown sequencing; not browser-observable. API restarted once mid-sweep and returned healthy in ~30 s with no stuck state). |
| 4082 | 08-27 17:11 | api-only | feat(api): org lifecycle wave 1 — status values, columns, deferrable FKs | No UI itself; schema/status foundation consumed by #4111/#4114/#4128/#4132. | n/a standalone | Foundational only — test via the wave 3/5 UI PRs below. | N/A — covered by #4114 |
| 4129 | 08-27 17:10 | api-only | fix(api,workspace): review round 2 — permanent-vs-transient AI errors, billing/stamp telemetry | No UI; `ee/workspace` error/telemetry hardening. | n/a | Same BLOCKED reasoning as #4112 (workspace pipeline). | N/A — backend only, smoke OK (`ee/workspace` error/telemetry hardening; workspace pipeline BLOCKED, see #4112/#4164). |
| 4130 | 08-27 17:11 | web | fix(api,web): review round 2 — self-managed revisions route, egress-blocked verdicts, listing-gate tests | Same page as #4113: `/admin/llm-provider-catalog` | Self-managed provider revisions list correctly scoped; egress-blocked verdicts surfaced. Fold into the #4113 catalog click-path as a bug-fix check. | Same platform-admin requirement as #4113. | PARTIAL |
| 4131 | 08-27 17:44 | api-only | fix(api): review round 2 — proxy audit completeness, recorder observability, shutdown drain | No UI; LLM egress proxy hardening. | n/a | Same as #4115 (egress backend, no standalone UI). | N/A — backend only, smoke OK (LLM egress proxy hardening; same surface as #4115). |
| 4116 | 08-27 17:45 | api-only | feat: partner catalog endpoint selection (#3922 W03) | No UI of its own — surfaces entirely through #4118's Endpoint card. | n/a standalone | Fold into #4118 click-path. | BLOCKED |
| 4118 | 08-27 18:07 | web | feat: byo-llm partner endpoint UI + docs (#3922 W04) | Settings → **Partner** → **AI Provider** tab (`/settings/partner#ai-provider`) | **Endpoint** card (only rendered when the catalog is non-empty) shows a radio list: "Anthropic (direct)" + one row per active catalog entry with verified models. Picking a non-direct entry with a data-handling note opens a confirm dialog quoting the note behind a consent checkbox before saving. Settings → **AI Usage & Budget** (`/settings/ai-usage`) then names the catalog endpoint in its "billed to your key" note. | BLOCKED unless `LLM_PROVIDER_CATALOG_ENABLED=true` **and** a platform admin has already created+verified+activated at least one catalog entry via #4113's page — on a fresh seed the catalog is empty and the Endpoint card does not render. | BLOCKED |
| 4111 | 08-27 18:25 | web (+api) | feat(api,web): org lifecycle wave 2 — merge engine | Backend + `OrganizationList.tsx`/`orgStore.ts` wiring only, no new buttons yet. Real UI ships in #4114. | n/a standalone | Fold into #4114 click-path. | FAIL — covered by #4114 |
| 4114 | 08-27 19:26 | web | feat(web): org lifecycle wave 3 — merge UI | Settings → **Organizations** (`/settings/organizations`) → select an org → **"Merge into another organization"** button (only shown to partner-scope admins) → pick survivor org → preview (per-table row counts, warnings) → type org name to confirm → **Merge organization** | Live progress ("Merge in progress") then a persistent **"Merge complete"** summary (`N row(s) moved, M row(s) dropped`); merged org disappears from the active list. | Requires a **partner-scope** login (org-scope admins never see the button — `canMergeOrgs` checks `jwt.claims.scope === 'partner'`) **and MFA enabled** on the account (`organizationsPage.merge.errors.mfaRequired`), plus a second eligible org to merge into. | FAIL |
| 4140 | 08-27 19:31 | api-only | feat(api): wave 3.5d — BREEZE_ROLE split (worker entrypoint, compose worker-split profile) | No UI; introduces `BREEZE_ROLE` / `BREEZE_API_ROLE` and the worker-split docker-compose profile. | n/a | BLOCKED on the default local compose profile (`BREEZE_API_ROLE=all`); only observable if the stack is booted with the worker-split profile. | N/A — backend only, smoke OK |
| 4128 | 08-27 19:44 | api-only | feat(api): org lifecycle wave 4 — archive backend | Backend only; UI ships in #4132. | n/a standalone | Fold into #4132 click-path. | PARTIAL — covered by #4132 |
| 4145 | 08-27 19:57 | api-only | feat(api): relay lazy agentWs import — monitor/snmp/backup/discovery to worker container | No UI; part of the worker-split cluster with #4133/#4138/#4140. | n/a | Same BLOCKED reasoning as #4140. | N/A — backend only, smoke OK |
| 4132 | 08-27 20:01 | web | feat(web): org lifecycle wave 5 — archive UI | Settings → **Organizations** (`/settings/organizations`) → select an org → **"Archive organization"** (red button) → pick retention (**30/90/365 days / Never / Custom 1–3650**) → read consequences copy → **Archive organization** confirm | Done screen ("Archiving started" or "Organization archived" with purge date). Collapsed **Archived** section below the list now shows the org with a purge countdown and searchable/paginated read-only detail; **Restore** button returns it to its pre-archive status. | **MFA required** on the acting account (`organizationsPage.archive.errors.mfaRequired`) — same prereq as merge. | PARTIAL |
| 4146 | 08-27 22:31 | api-only | feat(api): wave 4 part A — act-mode foundations (execution ledger, agent attribution, notify, caps v2, device pinning) | Backend only; config UI ships in #4148. | n/a standalone | Fold into #4148 click-path. | PASS — verified through #4148's UI (act warning + ack gate + 422 prerequisites) |
| 4148 | 08-28 02:59 | web | feat(api,web): wave 4 part B — act mode UI | Settings → **AI Agents** (`/settings/ai-agents`) → create/edit an agent → **Mode** dropdown → select **"Act — execute matched operations unattended, with verification"** | An **"Act mode executes without a human clicking approve"** warning box appears requiring the acknowledgement checkbox ("I understand act mode executes unattended, verification can fail, and there is no rollback.") before Save succeeds; form blocks save with "Add at least one notification recipient before enabling act mode." / "Allow at least one act-eligible tool..." if prerequisites aren't met. | Config/save flow works regardless of flags, but actual unattended execution requires `BREEZE_AI_AGENTS_ENABLED=true` (default `false`) plus a live/online device — BLOCKED for real act execution on the offline-fixture stack. | PASS — warning box, ack gate, and both prerequisite 422s all behave as described (2 UI/UX cuts: 18, 19) |
| 4154 | 08-28 05:26 | api-only | feat(api): wave 5 part A — policy-decide foundations (decision-state lifecycle, exposure ledger, kill state) | Backend only; UI ships in #4155. | n/a standalone | Fold into #4155 click-path. | PASS — exposure ledger drives the run-detail readout; kill state has NO operator surface (cut 22) |
| 4155 | 08-28 08:45 | web | feat(api,web): wave 5 part B — policy-decide lane (dark) | Settings → **AI Agents** → edit an agent with **Mode = Act** → new **"Unattended policy authorization"** section appears with a checklist of policy-decidable actions ("Operations this agent may execute WITHOUT a human clicking approve...") | Section renders and lets you select/deselect action keys; saving persists `actAssets.supervisedActionKeys`. | Feature is **dark by default**: `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED=false` — the form section itself is visible, but actual policy-approved unattended execution is BLOCKED without the flag on and a live device/queued run. | PARTIAL — section renders and persists supervisedActionKeys; an org-only agent is inert with no warning (cut 27) and the budget reads "0 of 0 devices" (cut 21) |
| 4157 | 08-28 13:11 | web | feat(api,web): wave 6.1 — supervision: execution-trace runs UI | Sidebar → Settings section → **"Agent Runs"** (`/ai-agents/runs`) → click a run row → run detail (`/ai-agents/runs/:id`) | Runs list (filterable by agent/status, keyset-paginated) with columns Agent/Status/Trigger/Verdict/Queued/Finished/Cost; detail page shows an execution-trace outcome timeline (executed actions, proposals, denials) and an exposure-budget readout. Legacy per-agent runs route no longer leaks raw tool args. | BLOCKED to see populated data on a fresh seed — list shows **"No runs yet."** unless AI agent runs have actually executed (needs `BREEZE_AI_AGENTS_ENABLED=true` + a live device + a triggered run, e.g. from an alert). | PASS — list, filters, both empty states, trace, flags, exposure card, linked approvals (cuts 20, 23, 24, 28) |
| 4102 | 08-28 13:20 | api-only (UI-reachable) | feat(api): edition-migration script in the shared system script library | Scripts (`/scripts`) → **"Import from Library"** button → find **"Migrate Agent Edition (Windows)"** → import | Script clones into the org's script library (content + parameter definitions) via `POST /scripts/import/:id`; visible in the org's script list afterward. | Running the script itself requires a live Windows agent mid-edition-transition — import/visibility is fully testable locally, execution is not. | PASS — present in library, imports 201, content + 3 params render, appears in the run picker; execution BLOCKED (offline fixtures). Cuts 25, 26 |
| 4158 | 08-28 14:14 | api-only | fix(api): realign act-mode integration assertion (main red) | Test-only fix (`__tests__/integration/aiAgents.routes.integration.test.ts`, shared types) — no runtime behavior change. | n/a | Not a QA item — CI-only fix. | N/A — test-only fix, no runtime behavior; not a QA item. |
| 4161 | 08-28 14:33 | web | fix(web): RemoteTerminal double-init/dead-terminal race + connectedBang locale | Device detail → **Remote Tools** → **Terminal** tab (`/remote/terminal/[deviceId]`) — connect, switch to **File Browser** tab, switch back to **Terminal** | Terminal reconnects cleanly instead of leaving a dead/disposed instance (previously reproduced issue #4152's "Terminal won't connect until visiting File Browser and back"); on connect the terminal prints a correctly localized **"Connected!"** (`remoteTerminal.connectedBang`) instead of a raw/English-only string in translated locales. | BLOCKED on fully offline fixture devices — Remote Terminal requires a live WebSocket agent connection to open a session at all. | PASS (offline half) / BLOCKED (reconnect half) — exactly one `.xterm` across mount → File Browser → back → Retry (no double-init, no dead terminal), offline state explicit ("Error: Device is not online" + Failed + Retry), no duplicate-init console logs, `remoteTerminal.connectedBang` present in all 8 locales. The live reconnect + "Connected!" render needs a WS agent — not reachable on offline fixtures. |
| 4097 | 08-28 14:55 | api-only | feat(auth): add Phase 1 browser transition foundation (#3852) | No UI surface added. Login/MFA/passkey/invite/email-verification/Cloudflare Access/SSO issuance paths + terminal logout all re-plumbed underneath, legacy issuer kept live. | Not independently visible; a full login/MFA/logout regression smoke should behave identically to before. | Both new flags default **off**: `AUTH_BROWSER_TRANSITIONS_ENFORCED=false`, `AUTH_BROWSER_TERMINAL_PREPARATION_ENABLED=false` (PR body also references `AUTH_BROWSER_TRANSITIONS_ENABLED` conceptually, but only the two flags above exist in `config/env.ts`/`.env.example`). Phase 1 explicitly does not activate anything — BLOCKED for any new-behavior QA on a stock stack; only a "nothing broke" smoke test applies. | PASS (regression smoke) — flags confirmed off; full login → logout → re-login cycle, four org switches, and a deliberate concurrent-refresh race all behave correctly. **The one regression #4097 did introduce (org switch logging the user out via a bare `409` on `/auth/refresh`) is FIXED and re-verified this run** — see the Group 4+5 log: 8/8 refreshes `200` across 4 switches with no logout, and a direct two-concurrent-refresh repro now returns `200` + `401 {"reason":"refresh_raced"}` instead of `409`. Fixes `1e303bf58` (web) + `aa23045a6` (api). |
| 4163 | 2026-08-28 | web | chore(web): What's New entry for v0.109.0 | Log in → the What's New splash/menu entry (`apps/web/src/lib/whatsNew.ts`) — open the What's New panel from the header/help menu | v0.109.0 entry renders with its bullets; upgrade splash fires once for a user who last saw 0.108.0 | none | PASS |

### Feature-flag env vars referenced by these PRs

| Env var | Default | Introduced by | Gates |
|---|---|---|---|
| `AUTH_BROWSER_TRANSITIONS_ENFORCED` | `false` | #4097 | Enforcement of the new guarded auth-issuance path over the legacy issuer (Phase 1 foundation; not activated by this merge). |
| `AUTH_BROWSER_TERMINAL_PREPARATION_ENABLED` | `false` | #4097 | Terminal-logout preparation behavior tied to the browser-transition work. |
| `LLM_PROVIDER_CATALOG_ENABLED` | `false` | #4116 (validated in `apps/api/src/config/validate.ts`; consumed by `llmConfigResolver`) | Whether a partner's catalog-endpoint selection (#4118 UI) actually resolves/routes traffic through the catalog instead of raw BYOK credentials. Not in `.env.example` — set directly. |
| `BREEZE_ROLE` / `BREEZE_API_ROLE` | `all` | #4140 | Worker-split deployment profile (API vs. worker container role separation); only meaningful under the worker-split docker-compose profile. |
| `BREEZE_AI_AGENTS_ENABLED` | `false` | pre-existing (referenced by #4146/#4148/#4154/#4155/#4157 as the umbrella flag) | Whether AI-agent runs (shadow/act) actually execute; without it the Settings → AI Agents config UI is editable but nothing runs. |
| `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED` | `false` | #4155 | Sub-flag of `BREEZE_AI_AGENTS_ENABLED`; gates the policy-decide unattended Tier-3 approval lane. Flag off = byte-identical to Part A (#4154), per PR body. |
| `BREEZE_WORKSPACE_ENABLED` | `false` (commented out) | pre-existing, consumed by #4112/#4129 | Whether the `ee/workspace` content-enrichment pipeline (now BYOK-aware) runs at all. |
| `ENROLLMENT_KEY_DEFAULT_TTL_MINUTES` / `CHILD_ENROLLMENT_KEY_TTL_MINUTES` / `INSTALLER_BOOTSTRAP_TOKEN_TTL_MINUTES` | `43200` (30 days, was 60/1440/1440) | #4126 | Env-fallback defaults for enrollment key / installer bootstrap TTLs (self-host deployments that already set these keep their own values). |


### Groups for sweep dispatch

1. **Settings/Partner + SSO** — #4071 (SSO enforce lockout preflight), #4080 (link-on-first-SSO-login), #4112 (workspace BYOK, nominal UI), #4113 + #4130 (LLM Provider Catalog admin page), #4116 + #4118 (BYO-LLM partner endpoint selection UI + AI Usage note). Needs: platform-admin login for the catalog page, partner-scope login for the AI Provider tab, an SSO provider fixture, `LLM_PROVIDER_CATALOG_ENABLED=true`.
2. **Organizations lifecycle** — #4082, #4111 (foundations, no standalone UI), #4114 (merge UI), #4128, #4132 (archive UI). Needs: partner-scope login + MFA enrolled on the account, a second org to merge into.
3. **AI agents / workspace** — #4146+#4148 (act mode config UI), #4154+#4155 (policy-decide unattended lane UI), #4157 (execution-trace Agent Runs UI), #4102 (edition-migration script in library, adjacent). Needs: `BREEZE_AI_AGENTS_ENABLED` (and `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED`) to see anything execute; runs list will be empty without fixture/live runs.
4. **Devices/remote/scripts** — #4126 (enrollment key defaults), #4161 (RemoteTerminal race + locale), #4102 (Scripts → Import from Library). Needs: a live/online agent for #4161's actual reconnection behavior (BLOCKED on pure offline fixtures — everything else in this group is testable offline).
5. **Alerts/webhooks/eventing (mostly backend, thin UI)** — #4083, #4098, #4099 (alert resolve CAS — the one with a real click-path), #4117. Needs: at least one active alert fixture; race conditions need two concurrent tabs/requests to actually exercise.
6. **Infra/no-UI (skip or ops-verify only)** — #4066, #4070, #4087, #4091, #4096, #4097 (flags off), #4110, #4115, #4129, #4131, #4133, #4138, #4140, #4145, #4158. No browser click-path; smoke-test that login, alerts, webhooks, and AI sessions still work end-to-end after these land, and confirm the two new flags in #4097 default off.

## Sweep log (append as you go)

### [Env — stack bring-up] — main session, 2026-08-28 09:20–09:45

#### [#4113/#4116 — LLM_PROVIDER_CATALOG_ENABLED] — FAIL (fixed)
- ❌ BUG: flag is accepted by `apps/api/src/config/validate.ts:804` but never mapped in the `api` `environment:` block of `docker-compose.yml` nor documented in `.env.example`. Compose only interpolates listed vars → a self-hoster's `.env` value is silently ignored; the BYO-LLM catalog UI (#4116/#4118) can never be reached on a compose deploy. `envComposeParity.test.ts` did not catch it. → fix on this branch (see Fixes applied).

#### [dev stack — ee/workspace vs extension-sdk mounts] — FAIL (fixed)
- ❌ BUG: `pnpm wt-stack up` (no `--rebuild`) on current main crashed the API at boot: `SyntaxError: The requested module '@breeze/extension-sdk' does not provide an export named 'ExtensionAiError'` (`ee/workspace/src/hostTypes.ts:31`). `docker-compose.override.yml.dev` host-mounts `apps/api/src`, `packages/shared/src` and `ee/workspace/src` but **not** `packages/extension-sdk/src`, so the mounted workspace code ran against the SDK baked into an older image. Any SDK change (here #4129's `ExtensionAiError`) silently requires `--rebuild` while sibling packages hot-reload. → mount added (see Fixes applied).
- ⚠️ UI/UX (dev tooling): wt-stack reports only `dependency failed to start: container … api-1 is unhealthy`; the real error is three `docker logs` calls away.

### [Group 1 — Settings/Partner + SSO + BYO-LLM] — opus sweep agent, 2026-08-28 11:23

#### [nav crawl] — PARTIAL (60/62 render clean)
- ✅ Crawled all 62 sidebar links (55 in the doc + 7 `platformAdminOnly`/extension items the promoted admin now sees). 60 render with real content and no error boundary.
- ❌ BUG: **both Workspace extension pages are permanently dead** with `BREEZE_WORKSPACE_ENABLED=true` — `/extensions/workspace/extensions/workspace/{dashboard,sources}` render the error-boundary fallback "This extension is unavailable right now. workspace / workspace-dashboard" (`[data-testid="extension-element-unavailable"]`). Root cause is structural, not transient: `apps/web/src/lib/extensions/registry.ts:261` loads the extension web entry with `moduleImporter = (url) => import(url)`, and a browser dynamic `import()` cannot carry an `Authorization: Bearer` header — but `apps/api/src/routes/extensionsWeb.ts:98` gates the whole group (`/registry` **and** `/assets/...`) behind `authMiddleware`, which is bearer-only (`apps/api/src/middleware/auth.ts:504-507`, no cookie fallback). API actual: `GET /api/v1/extensions/assets/workspace/sha256:f0df14b5…/web/index.js` → **401** `{"error":"Missing or invalid authorization header","message":"Missing or invalid authorization header"}` (reproduced both from the app's own import and from a `credentials:'same-origin'` fetch). UI actual: fallback card, no toast, no mention of auth. The code comment at `registry.ts:293` calls a 401 here "transient … racing a token refresh" — it is permanent. This also makes the `device.detail.tabs` Workspace slot unreachable, i.e. **the whole #4112 workspace surface is unreachable from the UI**, independent of the enrichment pipeline.
- ⚠️ UI/UX: extension nav hrefs double their own prefix. The registry serves `navigation[].path = "/extensions/workspace/dashboard"` (already fully qualified) and `apps/web/src/components/extensions/useExtensionNavigation.ts:61` prepends `/extensions/${extension.name}` again → the sidebar links to `/extensions/workspace/extensions/workspace/dashboard`. The page router doubles it identically, so the ugly URL is the *working* one and the clean `/extensions/workspace/dashboard` renders "Page not found. This extension page doesn't exist, or the extension isn't enabled."
- ⚠️ UI/UX: a `429` from the global rate limiter (300 req/60 s per IP, `middleware/globalRateLimit.ts:125`) renders as a permanent hard-error state with no retry and no hint that it is a rate limit — e.g. `/settings/sso` showed "Failed to fetch SSO providers / Try again", `/billing/*`, `/contracts`, `/timesheet` showed "Failed to load", `/reports` and `/admin/quarantined` stalled on their loading strings. **Not product failures** — my own 62-page crawl tripped the limiter (284 of ~290 console errors in the crawl were 429s). Every one of those pages re-rendered clean when revisited individually, and they are recorded as PASS above.
- Measurement note: the sidebar fires ~8 independent badge/config fetches per page load (`/config`, `/system/version`, `/orgs/partners/me`, `/approvals/pending/count`, `/admin/account-deletion-requests/pending-count`, `/permissions/catalog`, `/extensions/registry`, events WS), which is what makes a fast click-through hit 300/min so easily.

#### [#4163 What's New] — PASS
- ✅ Sidebar footer → **What's new** opens the panel with the v0.109.0 entry: title "Org archive, guided SSO linking, and 30-day installer keys" and all five highlight bullets rendered verbatim from `apps/web/src/lib/whatsNew.ts`.
- ✅ **Learn more** → `https://breezermm.com/release-notes` with `target="_blank" rel="noreferrer"`.
- ✅ **Got it** closes the panel and writes `localStorage['breeze.whatsNew.lastSeenVersion'] = "0.109.0"` (was `null`). **Show me later** closes without writing. Re-opening from the sidebar after dismissal still shows the entry (`latestApplicableEntry` correctly ignores the floor).
- BLOCKED (by design, not a defect): the *automatic* upgrade splash cannot fire on this stack. `WEB_VERSION` is the `'dev'` sentinel (sidebar footer reads "Web dev · API 0.82.0" — `PUBLIC_APP_VERSION` is unset in the wt-stack build), and `decideWhatsNew` short-circuits `if (webVersion === 'dev') return { entry: null … }` (`apps/web/src/lib/whatsNewState.ts:32`). Testing the "fires once for a user who last saw 0.108.0" half needs a version-stamped web build. The splash mechanism itself is unchanged by #4163, which only adds the entry.

#### [#4071 SSO enforce lockout preflight] — PASS
- Fixture note: the stack was NOT the documented "zero SSO providers" seed — `/settings/sso` already held an org-axis OIDC provider "QA Test IdP" (Active, **Enforced**, created 8/28). Left in its original Active/Enforced state at the end of the run.
- ✅ **Toggle enforcement OFF → ON on the existing org provider fired the preflight and correctly did NOT warn.** `POST /sso/providers/enforcement-preflight` → **200** `{"data":{"totalActiveUsers":0,"unlinkedCount":0,"selfLockedOut":false,"truncated":false,"loginProvider":{…"QA Test IdP"…},"unlinked":[]}}`, then `PATCH /sso/providers/:id` → 200 and the ENFORCE SSO cell went Optional → Enforced. `totalActiveUsers: 0` is **correct, not a miscount**: the org-axis population query excludes any org member who is also a partner user (`notExists(partnerUsers …)`, `apps/api/src/routes/sso.ts:1617-1626`), and the only active user on this seed is the partner-scope admin. Enforcing an org provider genuinely cannot lock a partner tech out.
- ✅ **The real lockout path reproduces on the partner axis.** Created a partner-axis provider ("Partner (technician login)" `ownerScope` radio, present on create only, per the partner-wide-first playbook) with Enforce SSO on → it lands **Inactive**, so no lockout moment yet. Clicking **Activate** on that enforcing provider opened `[data-testid="enforce-lockout-modal"]`: title **"Users will be locked out"**, summary "1 of 1 active users have not connected SSO and will not be able to sign in once SSO enforcement is active:", one `[data-testid="enforce-lockout-user"]` row = `admin@breeze.local` with the **"your account"** badge, plus the self-warning **"Your own account is not connected — you would lock yourself out."**
- ✅ Consent gate works in both directions: **Enable enforcement** is `disabled` on open, becomes enabled when `[data-testid="enforce-lockout-ack"]` is checked, and goes back to `disabled` when unchecked. **Cancel** closes the modal and leaves the provider **Inactive** — no PATCH issued.
- ✅ Negative case: submitting the create form empty is blocked client-side with inline "Provider name is required"; the drawer stays open and no request is sent.
- ✅ Cleanup: the probe provider was deleted (list back to "1 of 1 providers") so no partner-wide enforcing provider is left behind.
- ⚠️ UI/UX: **no mutation on this page produces a success confirmation.** Save (Enforced→Optional→Enforced), create, and delete all returned 200 and all showed **no `[data-testid="toast"]`** within a 6 s poll — the only feedback is the drawer closing and the list re-rendering. Source confirms it is not a measurement artifact: `SsoProvidersPage.tsx` contains exactly **one** `runAction` call (line 383, the save path) and it passes `errorFallback`/`friendly` but **no `successMessage`**; `handleConfirmDelete` (line 425) and `handleToggleStatus` (line 283) bypass `runAction` entirely and surface failures only through inline `setError`. The file is absent from both the `no-silent-mutations` adopted set and `runActionAllowlist.ts`, so nothing guards it either way.

#### [#4113 + #4130 LLM Provider Catalog] — PARTIAL
- ✅ `/admin/llm-provider-catalog` is reachable as platform admin (the freshly-minted session carries `isPlatformAdmin: true`) and renders a proper empty state: "Total entries: 0 … No catalog entries yet." (`[data-testid="llm-catalog-empty"]`).
- ✅ **Create entry**: slug + name + notes → `POST /admin/llm-provider-catalog` **201**, success toast "Provider "QA Echo Gateway" created.", row appears as **Draft / 0 revisions**. Submit is `disabled` while slug/name are empty (empty submit can't be issued).
- ✅ **Revision validation, three distinct negative cases, all correctly refused with the modal left open**: (a) `not-a-url` → 400, (b) a valid HTTPS URL with an unfilled model row → 400, (c) **SSRF/egress guard** — `https://169.254.169.254/v1` → **400** with the user-facing toast **"Base URL host is not a permitted egress target: URL points to blocked address: 169.254.169.254"**. That is #4130/#4115's egress guard firing at revision-create time (`services/llmProviderCatalog.ts:165` → `assertSafeUrl`), surfaced legibly in the UI.
- ✅ **Create revision** with `https://example.com/v1` → **201**, toast "New revision created for "QA Echo Gateway".", revision row shows `Base URL: https://example.com/v1`, auth mode, and the mapped model as **Not verified**.
- ✅ **Lifecycle gating holds**: with an unverified revision, both **Activate** (`…-activate`) and **List** (`…-list`) are rendered `disabled` — an unverified endpoint cannot be activated or exposed to partners.
- ✅ **Verify** opens `[data-testid="llm-catalog-verify-modal"]` asking for a transient **Test API key** (`type="password"`, copy: "The key is used only for this request and is never stored.") and performs a real outbound round-trip.
- ❌ BUG: **the verification verdict is thrown away — the operator sees only the word "Failed".** API actual: `POST /admin/llm-provider-catalog/revisions/:id/verify` → **200** with a full three-step diagnosis — `{"passed":false,"steps":[{"name":"direct_tool_use","ok":false,"detail":"request failed: Error: 405 <!doctype html>…"},{"name":"direct_tool_result","ok":false,"detail":"skipped: the tool_use stage did not complete"},{"name":"sdk_subprocess","ok":false,"detail":"skipped: the direct SDK stage did not pass"}],"harnessVersion":"1"}`. UI actual: a red pill reading **"Failed"** and nothing else — no step names, no `detail`, no HTTP status, and no toast. Source confirms it is deliberate narrowing, not a render race: `apps/web/src/components/admin/LlmProviderCatalog.tsx:153` types the state as `useState<{ passed: boolean } | null>`, line 357 does `setVerifyResult({ passed: result.passed })`, and the renderer at 845-851 is a binary pass/fail pill. A platform admin cannot distinguish "wrong URL" from "bad key" from "not an OpenAI-compatible endpoint" from "egress blocked" without opening devtools — which defeats the point of a verification step that exists to be diagnosed.
- BLOCKED: **activation could not be completed.** Activation requires a verified model, verification requires a reachable OpenAI-compatible endpoint with a working key, and a local stub cannot substitute — the same egress guard that produced the ✅ above rejects loopback/private targets unconditionally (`assertSafeUrl` is called with no `allowPrivateNetwork` option, and loopback/link-local sit in `isAlwaysBlockedIp`, which that option cannot relax). Exact prerequisite: **a publicly reachable HTTPS OpenAI-compatible endpoint plus a valid key for it.**
- Residue: the Draft entry `qa-echo-gateway` / "QA Echo Gateway" is left in the catalog — the page offers no delete affordance for an entry (only Add revision / List / Delist). It is Draft + unlisted + unverified, so it is invisible to partners and inert.
- ⚠️ UI/UX: a server validation error is surfaced with its raw field path attached — the model-map case toasted **"modelMap.claude-opus-4-8.providerModel: Too small: expected string to have >=1 characters"**, a raw Zod message shown to a human.
- ⚠️ UI/UX: **Activate** and **List** are disabled with no tooltip or inline reason; the only hint is the "Not verified" badge elsewhere in the row.

#### [#4116 + #4118 BYO-LLM partner endpoint selection] — BLOCKED
- ✅ Reachable part: `/settings/partner#ai-provider` renders the **AI Provider** tab — "Run Breeze AI on your own Anthropic API key instead of the platform key", the **Platform key** state ("AI features run on Breeze's Anthropic key and are billed as Breeze AI credits"), and the **Connect your Anthropic key** form.
- ✅ Negative case: **Save key** is `disabled` on an empty field; an invalid key (`not-a-real-key`) is refused with an error toast and no state change.
- ✅ Confirmed **not** a silent-feature failure: `GET /ai/provider` → **200** `{"configured":false,"provider":"anthropic",…,"status":"platform","catalogEntryId":null,"catalog":[]}`. The `catalog` key is present in the payload, i.e. `LLM_PROVIDER_CATALOG_ENABLED` is wired through and the API is catalog-aware — the array is simply empty.
- BLOCKED: the **Endpoint** card never renders, correctly. It is gated on `status.catalog.length > 0 || status.catalogEntryId !== null` (`apps/web/src/components/settings/PartnerAiProviderTab.tsx:372`), and `catalog` is documented at line 38-40 as "Empty when the catalog feature is disabled **or nothing is listed**". The doc's stated prereq ("catalog is non-empty") is too weak — a Draft entry is not enough. Exact prerequisite: **a catalog entry in `listed` status with an active revision whose models are verified**, which chains onto the #4113 blocker above. The radio list, the data-handling-note consent dialog, and the consent checkbox are therefore all untested.
- BLOCKED: the AI Usage "billed to your key" note likewise never renders. `[data-testid="ai-usage-billed-to-note"]` is gated on `usage.billedTo === 'partner_key'` and names the endpoint only when `catalogEndpointName` is set (`AiUsagePage.tsx:186-190`); `/settings/ai-usage` shows "No AI sessions yet" and $0.00, so there is no billed session to attribute.
- ⚠️ UI/UX: `/settings/ai-usage` left `<main>` **completely empty for 9.3 s** on load (measured) — no skeleton, no spinner, no heading — before the whole page appeared at once.
- ⚠️ UI/UX: the invalid-key toast reads **"apiKey: Enter a valid Anthropic API key."** — the message is well-written but is prefixed with its raw field path, the same leak as the catalog form.

#### [#4112 workspace enrichment honors partner BYOK] — BLOCKED
- ✅ The one nominal web surface is present: the AI Provider tab ends with "**Workspace content enrichment uses your configured AI provider and appears in your AI usage.**"
- BLOCKED (enrichment): no way to trigger a workspace content-enrichment run from the UI, so `billing_source='partner_key'` attribution cannot be observed. Requires the ingest pipeline plus a configured BYOK key.
- BLOCKED (worse than documented): even the workspace UI is unreachable on this stack — with `BREEZE_WORKSPACE_ENABLED=true` both Workspace pages and the device-detail Workspace tab die on the extension-asset **401** documented under [nav crawl] above. Any QA of #4112/#4129 through the workspace UI is blocked on that bug first, not just on the pipeline.

#### [#4080 link-on-first-SSO-login ceremony] — BLOCKED
- ✅ Reachable part 1 — **the ceremony page degrades correctly when reached without a ceremony.** A cold `GET /auth/connect-sso` (no temp token) resolves its bootstrap and renders `[data-testid="connect-sso-expired"]`: "This sign-in link has expired — For your security, the connection window is short. Start single sign-on again to get a new one." with a **Back to sign in** button. No crash, no error boundary, and no hang on the `connect-sso-checking` skeleton. `[data-testid="connect-sso-back-to-login"]` returns to `/login`.
- ✅ Reachable part 2 — the softened fallback copy is wired: `/login?error=sso_link_required` renders "Your sign-in succeeded, but it couldn't be connected to your account automatically." (`getSsoLoginNotice`, `LoginPage.tsx:62-68`), i.e. the pre-#4080 dead end now reads as a recoverable state rather than a refusal.
- BLOCKED: the ceremony itself (password re-entry → MFA → `user_sso_identities` link → login completes) needs a real IdP. Exact prerequisite: **a working SAML/OIDC IdP bound to a seeded SSO provider, that can complete a callback whose email matches an existing password-holding, unlinked account.** `describeCeremony()` is driven entirely by a server-issued temp token from that callback; it cannot be forged from the browser.
- BLOCKED (second, undocumented prerequisite worth recording): **the login page's SSO entry point never appears for an org-axis provider.** Even with the seeded "QA Test IdP" Active+Enforced, `/login` renders no `[data-testid="partner-sso-button"]`, before or after entering the admin's email. The button is driven by `ctx.partnerSso` from the login-context payload (`LoginPage.tsx:174,184-196`), i.e. a **partner-axis** provider only. So reproducing #4080 needs the IdP wired to an **active partner-scope** provider — an org-scope one gives the user no way to start SSO from the login page at all.

### [Group 2 — Organizations lifecycle] — opus sweep agent, 2026-08-28 18:05

#### [org/site setup] — PASS
- Fixture note: seed had ONE org. I created **QA Merge Source B** (renamed → **QA Source B**, site "B HQ Site" → "B HQ Site (renamed)") and **QA Archive C**. Both left behind — see the closing state note under the archive block.
- ✅ **Create org**: `POST /orgs/organizations` → **201**. No success toast, but the outcome is unmistakable: the page selects the new org, writes its id to the URL hash, and opens the guided **"Add the first site for &lt;org&gt;"** step. Source confirms the missing toast is deliberate-by-omission, not a silent handler — `OrganizationsPage.tsx:762` DOES route the create through `runAction` (with `errorFallback` + `onUnauthorized`), it just passes no `successMessage`.
- ✅ **Guided first site**: `POST /orgs/sites` → **201**, modal closes, site list re-renders "1 of 1 sites" with the new row. **Skip for now** (tested on org C) dismisses the step with **no** request.
- ✅ **Empty-name negatives, three surfaces, all blocked client-side with zero requests issued**: (a) create-org submit with both fields blank → inline "Organization name is required" + "Slug is required" (zodResolver, `OrganizationForm.tsx:88`); (b) guided first-site submit blank → inline "Site name is required"; (c) org-settings rename field cleared → **Save is `disabled`**.
- ✅ **Slug auto-derives from the name** while untouched (`QA Merge Source B` → `qa-merge-source-b`) and is correctly NOT re-derived on a later rename (org B kept `qa-merge-source-b` after being renamed to "QA Source B").
- ✅ **Org rename** (Edit → `/settings/organizations/:id` → ORGANIZATION NAME → Save): `PATCH /orgs/organizations/:id` → **200**, success toast **"Organization name saved"**, breadcrumb + page heading update in place. **Site rename** (`/settings/sites/:id` → Save changes): `PATCH /orgs/sites/:id` → **200**, header/breadcrumb update and the "Saved at" stamp advances (no toast, but the inline stamp is the feedback).
- ✅ **Org list stays in sync** — the renamed org shows correctly in the left ORGANIZATIONS list immediately.
- ⚠️ UI/UX: **the org switcher goes stale after a rename.** With the rename done on `/settings/organizations/:id`, the top-bar switcher panel still listed the OLD name ("QA Merge Source B") until a full page load; the list and switcher only agreed again after reload. `OrgSwitcher.tsx:83-85` calls `fetchOrganizations()` in a mount-only effect and nothing invalidates it on an org mutation. (A *create* appears to work only because reaching the settings page is an Astro full page load, which remounts the switcher.)
- ⚠️ UI/UX: `SiteDetailPage.tsx:97` hardcodes `const REFERENCE_TIME = '12:00'` as the initial `lastSavedAt`, so a freshly opened site page always claims **"Saved at 12:00"** before anything has been saved in that session. Confirmed on `origin/main`.
- ✅ Audit trail records every one of these: `organization.create`, `organization.update` (`changedFields: ["name"]`), `site.create`, `site.update`, each scoped to the right `org_id`, and all render on `/audit`.

#### [#4114 merge UI (+#4082/#4111)] — FAIL

- ❌ **BUG (blocker): the merge engine cannot complete a single merge on a stack with `BREEZE_WORKSPACE_ENABLED=true`.** The whole ceremony works and the job is accepted, then dies in the executor.
  - API actual: `POST /orgs/organizations/:loser/merge` → **202** `{"jobId":"org-merge-f79617a7-…"}`; then `GET /orgs/organizations/merge-runs/:jobId` polls `{"state":"active"}` for ~25 s and lands on **`{"state":"failed","result":null,"failedReason":"[orgMerge] no merge policy registered for 'workspace_org_settings' — add one to orgMergeRegistry.ts (the registry has no default)"}`**.
  - UI actual: `[data-testid="org-merge-failed"]` — "Merge failed" + that raw developer string, with **Cancel / Retry merge**. **Retry reproduces it identically** (second 202, same reason ~25 s later) — deterministic, not transient.
  - Root cause (verified on `origin/main`, not inferred): the two lists that must agree do not. `orgMerge.ts:858` walks `topologicalCascadeOrder()`, which is seeded from `getOrgCascadeDeleteOrder()` → `withExtensionOrgCascade(CORE_ORG_CASCADE_DELETE_ORDER)` (`tenantCascade.ts:405-407`), i.e. it **does** include extension-contributed tables. `orgMerge.ts:869-873` then throws for any table without a policy, and `getOrgMergePolicies()` (`orgMergeRegistry.ts:524-539`) is built **only** from the core `SPECIAL` + `REPOINT_TABLES` — there is no extension hook analogous to `withExtensionOrgCascade`. `workspace_org_settings` is contributed by `ee/workspace` (`ee/workspace/src/schema/orgSettings.ts`, `ee/workspace/migrations/2026-07-24-org-settings-dlp.sql`) and has no core entry. **So merge is broken for every org, on every deployment where any extension has registered an org-cascade table.**
  - Why CI is green: the contract test `orgMergeRegistry.integration.test.ts:233` *does* build its required set from `getOrgCascadeDeleteOrder()` — but `runtimeTenancy` is only populated by `registerRuntimeExtensionTenancy()` at the extension loader's boot, which never runs in a plain vitest process. The main **Run integration tests** step also doesn't set `BREEZE_WORKSPACE_ENABLED` (only the separate "Boot API once …" / "Integration-test workspace (ee)" steps do, `ci.yml:1855,1956`). This is the merge-registry twin of the cascade blind spot already in CLAUDE.md.
  - ✅ Silver lining, verified in Postgres: the failure **rolls back completely.** After two failed attempts `QA Source B` was still `status='active'` and its site still carried `org_id = <B>` — no half-merged rows, no org stranded in `merging`.
- ✅ **Everything up to execution is good.** Partner-scope gating (`canMergeOrgs`, `OrganizationsPage.tsx:133`) shows `[data-testid="org-merge-open"]`; the modal states the irreversibility in the header; survivor `<select>` **excludes the source org** so merge-into-self is structurally unreachable; **Merge organization** is `disabled` until a survivor is chosen.
- ✅ **Preview is genuinely informative.** `POST /orgs/organizations/:id/merge-preview` → **200** `{"totalMovableRows":1,"verdict":"ok","tables":[{"table":"sites","policy":"repoint","loserRows":1,"wouldDrop":0},{"table":"audit_logs","policy":"leave-for-erasure","loserRows":4,"wouldDrop":4},{"table":"audit_log_chain",…}],"warnings":[…]}`. The UI renders the per-table breakdown ("sites: 1 row(s), 0 would be dropped") and the full warning that the loser's **audit and provenance trail is PERMANENTLY DESTROYED** (8 rows across `audit_logs`, `audit_log_chain`), with the "export them first" instruction.
- ✅ **Typed-name ceremony is exact and case-sensitive**: submit stayed `disabled` for empty, `"QA Source"` (prefix) and `"qa source b"` (lowercase), and only enabled on the exact `"QA Source B"`.
- ✅ **Negatives at the API, forced past the UI** (injected `<option>` values into the survivor select so the client-side filter couldn't hide the case):
  - merge into an **archiving/archived** org → **403** `{"error":"Access denied to the surviving organization"}`, surfaced verbatim as an error toast; preview never returns a plan.
  - merge into **itself** → **400** `{"error":"An organization cannot be merged into itself"}`, also toasted verbatim.
- ✅ **Cancel** at any stage (survivor chosen, preview loaded, failed panel) closes the modal and issues **no** request.
- ✅ Audit: `org.merge.requested` (success, `resource_name` = loser, details carry `jobId`/`survivorId`/`survivorName`) is written per attempt and renders on `/audit` as "Org merge requested", correctly scoped to the **survivor's** `org_id`.
- ⚠️ UI/UX: **the merge failure is not in the audit trail as far as any user can tell.** `org.merge.failed` is written with `org_id = NULL` (deliberate — `orgMerge.ts:927`, and the comment at `jobs/orgMerge.ts:28` calls it "org-less"), so it appears in neither the org-scoped `/audit` nor the **All organizations** fleet view (both confirmed empty of it while the rows exist in `audit_logs`). The only place the failure is ever visible is the modal, which the user can close forever.
- ⚠️ UI/UX: the failure surfaces a raw internal instruction to the operator — "…add one to `orgMergeRegistry.ts` (the registry has no default)".
- ⚠️ UI/UX: `organizationsPage.merge.totalMovableRows` is `"{{count}} rows will move"` with no `_one` variant, so a one-row merge reads **"1 rows will move"**. The same file already uses the `_one`/`_other` convention for 16 other keys.
- Prereq note that the inventory row overstates: **MFA was not required in practice.** `requireMfa()` (`middleware/auth.ts:855-888`) passes whenever `hasSatisfiedMfa` is true, and login mints `mfa: mfaSatisfied` where `mfaSatisfied = !ENABLE_2FA || (!user.mfaEnabled && !policy.required)` (`routes/auth/login.ts:618`). With `ENABLE_2FA` at its default `true` but the seed admin neither enrolled nor policy-required, the token carries `mfa: true` and the step-up gate is vacuous. Pre-existing by design, not introduced by these PRs — but it means the "MFA enabled" prereq in the inventory did not block anything here, and a partner that hasn't mandated MFA gets no step-up on either irreversible action.

#### [#4132 archive UI (+#4128)] — PARTIAL

- ✅ **Archive → done screen.** `[data-testid="org-archive-open"]` → modal with the full consequences list (`org-archive-consequences`): hidden from list/search, data intact and read-only, **agents uninstalled from its devices**, billing stops, permanently deleted after retention — plus "Restoring … is the undo for this action, available any time before it's purged." `POST /orgs/organizations/:id/archive` → **202** `{"status":"offboarding","purgeAt":"2026-09-27T17:57:13.067Z"}` and the done panel reads "Archiving started … its agents are being uninstalled now. This finishes automatically within a few minutes. / **Scheduled to be permanently deleted on 9/27/2026.**"
- ✅ **Retention picker is correct end-to-end.** 30 / 90 (default, "recommended") / 365 / Never / Custom. 30 days → `purgeAt` exactly +30 d; a later run left on the default → +90 d (11/26/2026). **Custom validation is airtight**: `0`, `-5`, `3651` and `abc` each disable submit and show `[data-testid="org-archive-custom-error"]` "Enter a number of days between 1 and 3650."; `1` and `3650` enable it; empty disables with no error.
- ✅ **Cancel mid-ceremony** issues no request, closes the modal, and re-opening resets to the 90-day default with the custom input gone.
- ✅ **Archived section, once finalized**: `[data-testid="org-archived-row"]` shows "QA Archive C / **Archived** badge / **Purges in 30 days**". Selecting it gives a read-only detail panel — `org-archived-detail-badge`, `org-archived-detail-purge`, the notice "This organization is archived and read-only. Restore it to resume normal access.", a **Restore** button, and **Edit / Archive / Merge are all correctly absent**.
- ✅ **Archived search works**: `GET /orgs/organizations?…&includeArchived=true`; a non-matching term gives "No archived organizations match your search.", a matching one returns just the archived row.
- ✅ **Archived orgs are not switchable.** After finalization the org switcher dropped to "Fleet view · 2 organizations" and no longer lists it — you cannot switch into an archived org.
- ✅ **Restore is the best mutation on this page.** `POST /orgs/organizations/:id/restore` → **200** `{"status":"active","recreateRequired":["Agents that completed the archive uninstall must be re-enrolled."],"aborted":false,"uninstallsCancelled":0}`, and the success toast **quotes the caveat**: "QA Archive C restored — status: Active. Before it's fully usable again: Agents that completed the archive uninstall must be re-enrolled." Org reappears in the active list instantly and the detail panel regains Edit/Archive/Merge.
- ❌ **BUG: an archiving org is invisible for the whole drain window — it is in neither list.** The `202` sets `status='offboarding'`, which drops it out of the active list *immediately*, but the Archived section is fed by `status='archived'` and reports "**No archived organizations.**" until the drain reaper finalizes. Measured on this stack: request 17:57:13 → `archived_at` 18:00:16, i.e. **~3 minutes with the org nowhere in the UI** — not listed, not searchable, not restorable (Restore only exists on an archived row). `offboardingDrainReaper.ts:21` runs on a 5-minute cadence and `OFFBOARDING_DRAIN_WINDOW_HOURS` bounds the worst case, so on a real tenant with live agents this window is **hours, not minutes** — an operator who archives by mistake has no in-product way to see or undo it during exactly the period the modal tells them it is "being archived". Nothing in the UI names the in-between state.
- ❌ **BUG: `org.archive.requested` records `"purgeAt": {}`.** The route returns the date correctly in the response body, but the audit detail serializes the `Date` to an empty object, so the retention decision — the one field that determines when the tenant's data is destroyed — is absent from the immutable record. Confirmed in Postgres for both archive runs: `{"purgeAt": {}, "partnerId": "…", "retentionDays": 30}`. (Also of note: two `org.archive.requested` rows are written per request, one with `orgId`/`orgName`/`durable:true` and one without.)
- ⚠️ UI/UX: **archiving the org you are currently switched into is allowed with no warning and leaves the app pointing at a dead tenant.** The modal says nothing about it being your current org; after the 202 the switcher still read "QA Archive C | **Active**" while the org had already vanished from the list, `localStorage['breeze-org'].currentOrgId` still held its id, and `/devices` rendered a **completely empty `<main>`** with no message (screenshot: `.playwright-mcp/g2-archived-current-org-blank-devices.png`). Nothing auto-switches the session to a surviving org.
- ⚠️ UI/UX: **`/settings/organizations/:id` for an archived org renders the full editable settings page** — name field editable, **Save** enabled, every tab present, no archived badge and no read-only banner — directly contradicting the "archived and read-only" notice on the list page. The write is correctly refused server-side, but with misleading copy: `PATCH /orgs/organizations/:id` → **404** `{"error":"Organization not found"}`, toasted verbatim, for an org whose name is on screen.
- ⚠️ UI/UX: **archive has no typed-name confirmation**, unlike merge — one click on **Archive organization** in the modal uninstalls the agents from every device in the org. Restore explicitly does *not* undo that ("must be re-enrolled"), so the irreversible half of archive sits behind less ceremony than the reversible parts of other flows.
- ❌ **BUG (adjacent, but it blocked this sweep four times): switching organizations logs the user out.** Every org switch is followed, on the next navigation, by `?reason=session-expired`. API actual, captured on the fourth occurrence: two concurrent `POST /api/v1/auth/refresh` — one `status=200 duration_ms=4791`, one **`status=409 duration_ms=13999`** — after which the client's retry gets `401` and tears the session down. The switch re-mints the token for the new `orgId` concurrently with the page's own refresh, and the loser of the refresh-family conflict kills the session rather than adopting the winner's token. Amplified by this stack's refresh latency (measured 1.3 s–14.0 s), so severity in production depends on that; it is not caused by #4128/#4132, but it is on the org path and reproduced 4/4. Recovery is a plain re-login, and login resets the org context to the seed default.
- **Orgs left behind:** `QA Source B` (**active**, one site "B HQ Site (renamed)") and `QA Archive C` (**archived**, `archived_at` 2026-08-28 18:05 UTC, `purge_at` 2026-11-26 — 90-day retention, restorable from the Archived section). Session left logged in on **Default Organization**.

### [Root cause — org switch logs out] — main session + opus investigator, 2026-08-28

#### [#4097 auth browser transition — regression] — FAIL (fix in flight)
- ❌ BUG: **regression since v0.108.0, introduced by `f88dbaa4f` (#4097), unreleased.** Org switch = full reload (`apps/web/src/lib/orgSwitch.ts:59-71`); the reloaded page's bootstrap `/auth/refresh` races the pre-reload refresh the unload aborted client-side but the server still runs under the new per-binding issuance lease (`authBrowserTransition.ts:18`). Loser: `AuthIssuanceConflictError` (`retryable=true`) flattened by `authIssuanceAdmissionError` (`routes/auth/login.ts:144-147`) to `409 {"error":"Authentication issuance unavailable"}`. Web store only retries `401 {reason:'refresh_raced'}` (`stores/auth.ts:472-476`); 409 → `session-expired`. curl repro: 2 concurrent refreshes → `200` + `409` (0.66 s) — not dev-latency dependent. At v0.108.0 the same race yielded `401 refresh_raced` and was retried.
- Severity: **release blocker for v0.109.0** — deterministic logout on an everyday action.
- Fix: client treats 409 from `/auth/refresh` as raced (this branch); optional server per-route mapping to the `refresh_raced` contract.

### [Group 3 — AI agents / act mode / policy-decide / runs / script library] — opus sweep agent, 2026-08-28 20:20

#### [#4146+#4148 act mode config] — PASS
- ✅ Settings → **AI Agents** → **New agent**: Mode dropdown carries all three options (`off` / `shadow` / `act`). Selecting **"Act — execute matched operations unattended, with verification"** reveals `[data-testid="ai-agent-act-warning"]` with the full five-bullet copy ("Matched, rule-equivalent operations run unattended once revalidated…" / "Verification can still fail…" / "There is no automatic rollback…" / "Each run acts on one device only." / "A run stops acting once it hits its configured action cap.") plus the acknowledgement checkbox.
- ✅ **Save is hard-gated on the acknowledgement** — `disabled={saving || (isCreate && availableKinds.length === 0) || (enteringActMode && !actAck)}` (`apps/web/src/components/settings/AiAgentForm.tsx:812`); measured `button.disabled === true` until the box is ticked.
- ✅ Invalid input (client): Save with an empty name renders "Give the agent a name." in `ai-agent-issues` and fires **no** network request.
- ✅ Invalid input (server, the #4148 prerequisites): act-mode create with no recipient and no act-eligible tool → `POST /api/v1/ai/agents` **422** `{"error":"act_prerequisites_not_met: recipient, act_eligible_tool","code":"act_prerequisites_not_met","missing":["recipient","act_eligible_tool"]}`. UI actual: toast `data-toast-type="error"` — "This agent isn't ready for act mode yet." — and both messages rendered in the issues list ("Add at least one notification recipient before enabling act mode." / "Allow at least one act-eligible tool, or authorize a script, before enabling act mode."). Adding `manage_services` to the tool allowlist narrowed the next 422 to `missing:["recipient"]` — the two prerequisites are checked independently, as intended.
- ✅ Valid save: `POST /ai/agents` **201** + toast `success` "Agent saved", editor closes, row appears. Edit → `PATCH /ai/agents/:id` **200** + "Agent saved". **Cancel** discards an edited name with no write.
- ✅ Re-opening an agent already in act mode keeps the warning box but correctly **omits** the ack checkbox (`enteringActMode` only), and omits the create-only `ai-agent-ownerscope` selector.
- ✅ Per-agent kill switch: **Disable** → label flips to **"Confirm disable"** → `DELETE /ai/agents/:id` **200** + toast "Agent disabled"; the agent leaves the list.
- ⚠️ UI/UX: the `ai-agent-issues` panel is **stale** — after filling in the name the box still reads "Give the agent a name." (it is only recomputed inside `save()`, which cannot run because the ack gate disables the button). The operator sees a solved error while the real blocker (the unticked acknowledgement, far up a ~900px form) has no message anywhere near the disabled Save button.
- ⚠️ UI/UX: the notification-recipient checklist offers **all ten roles regardless of membership**. Selecting "Org Admin" (zero members on this seed) still fails the prerequisite — `hasResolvableAgentRecipient` resolves roles to live users (`services/aiAgents/recipients.ts:218-247`) — and repeats the identical "Add at least one notification recipient…" message with no hint that the chosen role resolves to nobody. Only "Partner Admin" (the one role with an active member) satisfied it.

#### [#4154+#4155 policy-decide lane] — PASS (dark-lane caveats below)
- ✅ With Mode = Act the **"Unattended policy authorization"** fieldset (`ai-agent-policy-decide`) renders with the exact hint copy ("Operations this agent may execute WITHOUT a human clicking approve, when a matching policy exists and the org's exposure cap has room…") and 11 selectable keys grouped by tool: `manage_services` (start/stop/restart), `manage_startup_items` (disable/enable), `manage_scheduled_tasks` (run/disable/enable), `security_scan` (quarantine/remove/restore). Sourced from `GET /ai/agents/policy-decidable-keys`.
- ✅ Selection persists: after save, `ai_agents.act_assets` = `{"scriptIds": [], "supervisedActionKeys": ["manage_services:restart"]}` (verified in Postgres) and the checkbox is re-checked on reopen.
- ✅ Exposure readout (`run-detail-budget-card`) renders on a run detail once an *effective* policy exists: "0 of 0 devices" / "0 of 10 policy decisions today" / "Recorded exposure — trailing 24h". With `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED=true` the `run-detail-budget-partial-note` is correctly absent (full accounting).
- ❌ BUG: **an org-only agent is inert but the UI presents it as live.** The agents list showed "QA Sweep Triage Agent · Alert triage · Act… · **Enabled**" while `resolveEffectiveAgentInner` returns `null` for any org+kind with **no partner-wide baseline** (`services/aiAgents/effectivePolicy.ts:309` — "No partner baseline means the org override cannot self-enable the agent"). API actual: `GET /api/v1/ai/agents/exposure-budget?orgId=4f62e5d3…&kind=triage` → **404** `{"error":"No active agent policy for this organization/kind"}`; UI actual: the exposure card reads "No active agent policy for this run's organization." for a run produced by the agent the same page names in its header. Creating a partner-wide `triage` baseline made the same request 200 immediately. Nothing in the editor or the list warns that an org-only agent without a partner baseline can never run — and the create form defaults to "This organization only" whenever the session is pinned to a single org (`useDefaultOwnerScope.ts:28`), which is the default state after login.
- ⚠️ UI/UX: `allowance = Math.floor(contractDeviceCount * maxFleetPercentPerDay / 100)` with **no `max(1, ·)`** (deliberate, `services/actionIntents/exposureBudget.ts:94` + its docstring), so a small fleet gets **"0 of 0 devices"** — visually identical to "nothing used yet" while actually meaning "this agent can never act unattended at this fleet size". No copy explains it.
- ⚠️ UI/UX (safety): the act-mode and policy-decide copy both promise a **kill switch** ("Each one still passes live guardrails, the kill switch, and per-org caps at execution time"), but `ai_kill_state` has **no operator surface at all** — no route (`grep` over `apps/api/src/routes` finds none) and `bumpAiKillState` has zero callers, documented in its own header: "Nobody calls this in this PR (grep-verified, Task 6) — Part B or an ops runbook is the eventual caller." Tripping the emergency stop today requires `UPDATE ai_kill_state …` by hand.
- BLOCKED: actual unattended execution — needs a live agent; both seed devices are offline fixtures and no LLM key is configured.

#### [#4157 Agent Runs / execution trace] — PASS
- ✅ Sidebar → **Agent Runs** → `/ai-agents/runs`. Empty state "No runs yet."; with a status filter applied and no match it correctly switches to "No runs match these filters."
- ✅ With fixture runs the table renders all seven columns (AGENT / STATUS / TRIGGER / VERDICT / QUEUED / FINISHED / COST), humanised statuses and verdicts ("Failed"/"Needs attention", "Completed"/"Remediated") and cost formatted from cents (`512` → `$5.12`). Both filters work (status=failed → 1 row; agent dropdown lists live agents).
- ✅ Run detail (`/ai-agents/runs/:id`): header + verdict badge, Device / Trigger / Cost / Duration / Queued / Started / Finished / Turns, summary, exposure card, **Execution trace** with all three entry kinds — `Executed manage_services restart OK · 2s · Succeeded · Verified` + the verify detail line, `Proposed manage_scheduled_tasks disable` + downgrade reason, `Denied manage_startup_items` + reason — plus "Tool executions"/"Linked approvals" empty states. Flags render on the failed run: "Budget exceeded", "Ran past its time limit", "Hit its turn limit". No raw tool args are exposed anywhere.
- ✅ A run with `intent_ids` renders the **Linked approvals** row (`manage_scheduled_tasks.disable`, status, "Review in Approvals" → `/approvals`).
- ⚠️ UI/UX: the trace **drops the target it acted on**. `runTrace.ts:110-111` maps `actOpKey` and `actTargetName` into the DTO, but `RunDetailPage.tsx` references neither (grep on `origin/main` returns nothing), so an executed entry reads "manage_services / restart" with no indication of *which* service — on the one screen whose job is to audit unattended execution. My fixture's `actTargetName: "Spooler"` appears on screen only because it happened to be repeated inside the free-text `verifyDetail`.
- ⚠️ UI/UX: the runs-list **agent filter is built from `GET /ai/agents`** (`RunsListPage.tsx:150`), i.e. live agents only. After disabling an agent its two runs still list correctly (the route left-joins for exactly this reason) but the agent vanishes from the filter — historical runs of a disabled agent can never be filtered to.
- ⚠️ UI/UX: in Linked approvals the intent status is printed **raw** (`pending_approval`) while the same page humanises run statuses two sections above; the link is a bare `/approvals` with no deep-link or filter to the intent.
- ⚠️ UI/UX: neither runs filter is reflected in the URL (no hash), so a filtered view is not shareable and resets on reload — the repo convention for this kind of UI state is `window.location.hash`.
- Measurement, not a finding: `/approvals` stayed on "No approvals waiting" for my fixture intent because the inbox reads `approval_requests` fanout rows keyed by user (`routes/approvals.ts:330`), which a hand-inserted `action_intents` row does not create.

#### [#4102 edition-migration script in library] — PASS
- ✅ `/scripts` → **Import from Library** lists **"Migrate Agent Edition (Windows)"** under Maintenance/windows. Import → `POST /api/v1/scripts/import/f0d512d1-6095-4587-8e41-eb169bc93174` → **201**; the script appears in the org list ("2 of 2") owned by Default Organization.
- ✅ Opening it renders the full PowerShell content in Monaco and **all three parameter definitions**: `msi_url` (string, required), `msi_sha256` (string, required), `target_edition` (select, required, options `hosted,self-hosted`, default `hosted`).
- ✅ Run-script picker: the row's **Run script** action opens **Execute Script** with the script's metadata, `Run As`, and the three parameters (`msi_url*`, `msi_sha256*`, `target_edition*` with the hosted/self-hosted select) — `[data-testid="script-parameters"]`.
- ✅ Cmd+K global search finds it under **SCRIPTS** and Enter opens the script editor.
- BLOCKED: executing it — the device-detail **Run Script** button is `disabled` with `title="Device is offline"` on both seed devices, and in the Execute modal offline devices render `disabled` (`ScriptExecutionModal.tsx:402`). Known deliberate over-gating vs. the API (`DeviceActions.test.tsx:103` — "stricter than the API requires (#2426)").
- ⚠️ UI/UX: the Execute modal's empty device list says **"No compatible devices found. This script requires Windows."** while the actual reason is that `statusFilter` defaults to `'online'` (`ScriptExecutionModal.tsx:55`) and the org's only Windows box is offline — switching Status → Offline immediately reveals `e2e-windows.local / Windows / Offline`. The message sends the operator after an OS mismatch that does not exist.
- ⚠️ UI/UX: this script's ~900-character description (a full runbook, including a `C:\ProgramData\…` path) is rendered **unclamped in four separate surfaces** — the library modal card, the scripts table row, the Execute Script modal, and the Cmd+K result subtitle, where it fills the entire palette (screenshot `.playwright-mcp/g3-cmdk-migrate-script-description-overflow.png`). Every other library entry is one line.

#### [scripts everyday checks] — PASS
- ✅ Create: `/scripts/new` → Save with an empty name gives inline "Script name is required"; with a name but no body, "Script content is required". Filled → `POST /api/v1/scripts` **201** + toast "Script created" + navigation back to `/scripts`. Defaulted to partner-wide (`orgId: null, partnerId: …`), matching the partner-wide-first convention.
- ✅ Edit: rename → **Save Changes** → `PUT /api/v1/scripts/:id` **200** + toast "Script saved" + navigation back.
- ✅ Delete: row action opens a **Delete Script** confirmation ("Are you sure you want to delete Clear Package Cache? This action cannot be undone.") → Delete → an `undo` toast ("Deleting \"Clear Package Cache\"… Undo") then `DELETE /api/v1/scripts/:id` **200** `{"success":true}` and a `success` toast; the row disappears.
- ✅ Cmd+K opens the global palette ("Search devices, scripts, alerts, users, settings…"), matches the script, and Enter navigates to it.
- Measurement, not a finding: a `POST /api/v1/scripts` that returned **502** (the API container restarted mid-sweep; `/health` recovered on its own) *was* surfaced — inline "Failed to save script" via `ScriptEditPage.tsx:134-139`, which handles the non-JSON proxy body. Not a silent failure.
- ⚠️ UI/UX (pre-existing, same class as paper cut 2): a successful **library import** produces no toast at all — the only feedback is the card vanishing from the modal (`ScriptsPage.tsx:295-321`); import and delete both bypass `runAction`, and `ScriptsPage.tsx` is in neither the `no-silent-mutations` adopted set nor `runActionAllowlist.ts`.

#### [#4083 / #4117 / #4133 / #4140 / #4145 smoke] — N/A (backend only)
- ✅ Post-#4117 smoke: login (three times, incl. two forced re-logins), `/alerts` renders 2 active alerts with severity rollups, `/approvals` renders, AI Agents + Agent Runs pages all load and mutate cleanly. No eventing regression visible from the UI.
- Note: one forced logout landed on `?reason=session-expired` after a **plain post-login navigation** (no org switch) — network shows two concurrent `POST /auth/refresh` (one `ERR_ABORTED`, two `401`) tearing the session down, i.e. the same refresh-family race as paper cut 11 but off the org-switch path. Confound: a second idle `/login` tab was open in this browser context, so this is recorded as corroboration for 11 rather than a separate finding.

**Fixtures inserted** (Default Organization `4f62e5d3-8914-4d1e-8ba5-87de93ffee92`), for anyone re-running: 2 rows in `ai_agent_runs` (`11111111-1111-4111-8111-111111111111` completed/remediated with executed+proposed+denied outcome entries, `22222222-2222-4222-8222-222222222222` failed/needs_attention with all three budget flags), 1 row in `action_intents` (`33333333-3333-4333-8333-333333333333`, `source='ai_agent'`, pending_approval, linked back via `ai_agent_runs.intent_ids`). Left behind in the UI: AI agent **QA Partner Baseline Triage** (partner-wide, act, enabled) — the org-scoped **QA Sweep Triage Agent** was disabled at the end of the run; scripts **QA Sweep Test Script (edited)** (partner-wide) and **Migrate Agent Edition (Windows)** (imported).

### [Group 4+5 — devices / remote / enrollment / alerts / everyday] — opus sweep agent, 2026-08-28 20:35

#### [#4167 fix re-test — org switch] — PASS (fix verified)
- ✅ **The logout is gone.** Default → QA Source B → Default → QA Source B, 4 switches ≥10 s apart, each followed by a real navigation. No `?reason=session-expired`, no `/login`, session survived all four; the switcher label tracked the target org every time.
- ✅ Refresh statuses, from `browser_network_requests` **and** the API access log for the window: **8 × `POST /api/v1/auth/refresh` → `200`** (durations 113–646 ms). **Zero `409`, zero `401`, zero logout.**
- ✅ The raced path itself is fixed, proven directly rather than inferred (the natural race did not fire at this stack's current low refresh latency): an independent curl session, two concurrent `POST /auth/refresh` on one refresh cookie → `200` + **`401 {"error":"Refresh already in progress","reason":"refresh_raced"}`**. That is the shape `stores/auth.ts:474` retries. Pre-fix this was `409 {"error":"Authentication issuance unavailable"}`, which the store mapped straight to `session-expired`.
- Both halves of the fix are on this branch: `1e303bf58` (web treats a 409 from `/auth/refresh` as raced) and `aa23045a6` (API answers a lost issuance lease with `refresh_raced`, not a bare 409). Paper cut 11 can be marked fixed.

#### [#4126 enrollment defaults] — PASS
- ✅ Devices → **Add Device** → Download Installer tab: `[data-testid="device-count"]` value **`50`**, `[data-testid="link-ttl"]` value **`43200`** rendering **"30 days"**. Both are the #4126 product defaults (was 1 / 24 h).
- ✅ **CLI Commands** tab: installation token renders, full install command renders for both Windows and Linux/macOS (curl + `--server` + `--token` + `--enrollment-secret`), footer reads **"Valid for 50 device enrollments"** and **"expires on 9/27/2026"** — exactly 30 days out. DB confirms: `Onboarding token (2026-08-28)` `max_usage=50`, `expires_at=2026-09-27`.
- ✅ **Generate Link** → "Share this link…" + **"Valid for 50 downloads. No login required."**; DB row `… (link x50)` `max_usage=50`, 30-day expiry.
- ✅ Revoke: Settings → Enrollment Keys → **Delete** → typed-free confirm modal naming the key ("Are you sure you want to delete QA Sweep Key G4?… Agents will no longer be able to enroll using this key.") → `data-toast-type="success"` **"Enrollment key \"QA Sweep Key G4\" deleted"** and the row disappears.
- ✅ Env-fallback half of #4126 confirmed on the *other* surface too: Settings → Enrollment Keys → **Create Key** with the expiry field left blank produced `expires_at = 2026-09-27` (30 days, `ENROLLMENT_KEY_DEFAULT_TTL_MINUTES=43200`), not the old 60 min.
- ⚠️ UI/UX (paper cut 32): the **Create Key** form's *device* half does **not** get the new default and contradicts its own placeholder — "Max Usage (optional)" is blank with placeholder **"Unlimited"**, but leaving it blank yields `max_usage = 1` (`routes/enrollmentKeys.ts:1062` `data.maxUsage ?? 1`, unchanged by #4126 and present on `origin/main`). Measured: `QA Sweep Key G4` → `max_usage=1`, list renders **"0 / 1"**. This is precisely the single-use-key failure #4126 was written to prevent, still reachable one screen over.
- ⚠️ UI/UX (paper cut 33): **Generate Link writes two enrollment keys per click**, and the one that is not the link reads as single-use. `AddDeviceModal.tsx:585-593` creates the parent with no `maxUsage`/`ttlMinutes`, so the list shows `Add device link (2026-08-28)` at **"0 / 1"** directly above `Add device link (2026-08-28) (link x50)` at "0 / 50".

#### [#4161 RemoteTerminal] — PASS (offline half) / BLOCKED (reconnect half)
- ✅ `/remote/terminal/e65460f3-…` (offline Windows fixture): **exactly one `.xterm` instance** (`document.querySelectorAll('.xterm').length === 1`, one `.xterm-screen`) — no double-init. `initStartedRef` guard present at `RemoteTerminal.tsx:117`.
- ✅ Offline messaging is explicit, not a blank pane: the terminal body prints "Connecting to E2E Windows Test Device…" then **"Error: Device is not online"**, the header pill reads **Failed**, and a **Retry** button is offered.
- ✅ **The #4152 repro path is clean**: Terminal → **File Browser** → back to **Terminal** left `.xterm` count at **1** (0 while on File Browser, 1 on return) and the returning terminal re-ran its connect attempt and re-printed the offline error — no dead/disposed instance, no second instance.
- ✅ **Retry** on the failed terminal reuses the same instance (still 1 `.xterm`) and appends one more "Error: Device is not online" line.
- ✅ File Browser's own offline copy is explicit too: **"The device is offline."** + Retry.
- ✅ Console carries **no duplicate-init logs**; one `POST /api/v1/remote/sessions` → `400` per Terminal mount (2 mounts = 2 calls, correct), plus the expected `503`s from the file endpoints.
- ✅ Locale half: `remoteTerminal.connectedBang` is present in **all 8 locales** (en/de-DE/es-419/fr-CA/fr-FR/it-IT/pt-BR/tr-TR) and wired at `RemoteTerminal.tsx:342`.
- BLOCKED: the actual **reconnect-and-print-"Connected!"** behaviour needs a live WS agent. These fixtures are offline, so the success path — and the runtime rendering of `connectedBang` — could not be executed. Everything reachable without an agent passes.

#### [#4099 alert resolve CAS + alerts/channels] — PASS
- ✅ **First resolve**: `/alerts` → row → drawer → **Resolve** → resolution-note step → **Resolve Alert** → toast `data-toast-type="success"` **"Alert Resolved"**; row status flips Active → **Resolved**, its actions collapse to Dismiss only, and the header counters move **5 → 4 active / High 1 → 0**.
- ✅ **The CAS second resolve is graceful, and it is the real stale-drawer case** (second tab left on the pre-resolve list, drawer opened from the stale row still showing **Active**): `POST /api/v1/alerts/aaaa1111-…-000000000001/resolve` → **`409`** body **`{"error":"Alert is already resolved"}`**. UI actual: toast `data-toast-type="error"` **"Alert is already resolved"**, drawer closes, and the stale row **self-corrects to Resolved**. No 500, no silent no-op, no duplicate success.
- ✅ **Exactly one winner, proven server-side**: after two resolve attempts the DB has one `resolved_at`/`resolved_by`, and `audit_logs` for the window contains **one** `alert.resolve` row — the #4083/#4099 compare-and-swap doing what it says.
- ✅ **Acknowledge**: row **Ack** → toast success "Alert Acknowledged", status → **Acknowledged**.
- ✅ **Suppress**: row **Mute** → duration modal (1 hour / 8 hours / 24 hours / 7 days / Forever) → **Suppress** → `data-toast-type="undo"` **"…suppressed" with an Undo action**, status → **Suppressed**.
- ✅ **Alert rules**: the Alerts page's **Rules** tab correctly routes to `/configuration-policies` (matching its own "Rules are managed in Configuration Policies" copy); empty state renders with a CTA, and **Breeze Defaults** (`/configuration-policies/defaults`) renders all six read-only feature cards with "Not enforced" + "Create override policy".
- ✅ **Notification channel create → test → delete**: **New Channel** → Webhook type reveals URL/Method/Headers/Auth → Create → toast success **"Channel Created"**, list `1 of 1 channels`. **Test** against `https://example.invalid/hook` gives a **visible pass/fail both places**: card badge **Failed** + "Last test: Just now" + "Reason: getaddrinfo ENOTFOUND example.invalid", plus an error toast. **Delete** → confirm → toast success **"Channel \"QA Sweep G4 Webhook\" deleted"**, list back to `0 of 0`.
- ⚠️ UI/UX (paper cut 34): the delete-channel confirm body is a **title-cased key echo, not a sentence, and drops the interpolation it is handed** — `NotificationChannelsPage.tsx:724` calls `t('notificationChannelsPage.deleteChannelConfirm', { name: selectedChannel.name })` but `en/alerts.json:934` is `"Delete Channel Confirm"` (no `{{name}}`), so the modal reads "Delete Notification Channel / Delete Channel Confirm / Cancel / Delete" and never names the channel. Machine-translated the same way in de-DE ("Kanal löschen Bestätigen"), es-419, it-IT, pt-BR, tr-TR; only fr-CA/fr-FR read as real sentences. Contrast the enrollment-key delete one screen over, which names the key and states the consequence.

#### [backend-only smoke] — PASS
- ✅ **Login** works (fresh `POST /auth/login` → `200` with tokens; browser session also survived four org switches and a sign-out/sign-in cycle).
- ✅ **Alerts list** loads and every mutation path works end to end (resolve / already-resolved 409 / ack / suppress) — see the #4099 block. Alert, automation and webhook eventing are the regression surface for **#4117**'s BullMQ ingress rework and nothing regressed: the resolve produced exactly one `alert.resolve` audit row and one `alert.resolved` transition.
- ✅ **Audit trail** (`/audit`) loads, paginates (`Showing 1-25 → 26-50 of 207`, pages 1-7, Next advances) and filters (action-type `delete` → 4 rows, `Showing 1-4 of 4`). Every mutation this sweep made is recorded with actor, resource name and details.
- ✅ **Health details**: `GET /health` → `{"status":"ok","version":"0.82.0","uptime":…}` (the version string is this worktree stack's `BREEZE_VERSION`, correctly threaded via `docker-compose.yml:215 APP_VERSION: ${BREEZE_VERSION:-dev}` — not a defect). `GET /health/live` → `200`. `GET /health/ready` → `{"status":"ready","checks":{"database":"ok","redis":"ok"}}`. Note `GET /api/v1/health` does **not** exist (`404`) — health is served at the root, by design (`apps/api/src/index.ts:420-435`).
- ✅ **Webhooks** (`/settings/webhooks`, #4098's indirect surface) loads clean with a proper empty state and the delivery-history panel prompt; nothing degraded.
- ✅ No 502 bursts, no API restarts, and no degradation observed across the whole run.
- Verdict for the api-only cluster **#4083 / #4098 / #4117 / #4091 / #4133 / #4138 / #4140 / #4145**: no browser-reachable regression. #4083's compare-and-swap is the one of these with hard positive evidence (single audit row under a real double-resolve).

#### [devices everyday] — PARTIAL (one real bug)
- ✅ **Search**: `windows` → `Showing 1 to 1 of 1`; a no-match term → "No devices found. Try adjusting your search or filters."; clearing restores `1 to 2 of 2`.
- ✅ **Status filters**: **Offline** → 2 of 2, **Online** → empty state, both round-tripping through `#filtersV2=` hash state (the repo's URL-state convention). **OS filter** (`osType equals windows`) → `Showing 1 to 1 of 1`, correct.
- ✅ **Page size**: 10/25/50/100/200/500 selector applies and the count line follows.
- ✅ **Sort**: clicking the DEVICE header reverses the order and back again, with `aria-sort` maintained (`descending`).
- ✅ **Bulk select**: header checkbox selects both rows, the toolbar switches to "2 selected · Bulk Actions · Clear selection", and Bulk Actions offers Reboot / Run Script / Deploy Software / Enable-Disable Maintenance / Wake / Compare / Link as multi-boot / Link as VM host / Remove.
- ✅ **Tag a device**: row menu → **Settings** → Tags → type + **Add** → **Save Changes**; chip renders in the modal and the write lands (`devices.tags = {qa-sweep-g4}` in Postgres). No success toast, but `DeviceSettingsModal.tsx` is a recorded exception in `runActionAllowlist.ts:23` and its failure path sets inline modal error text — correct per the repo contract, not a finding.
- ✅ **Device detail**: all tabs load with no errors — Overview, Details, Performance, Alerts, Anomalies, Tickets, Event Log, plus Hardware / Software / Patches under **More** — each with its own `#hash` and a real empty state rather than a blank pane.
- ✅ **Offline gating on device detail is explicit and correct**: Run Script / Connect Desktop / Remote Tools / Power are all `disabled` with `title="Device is offline"`; **Wake** stays enabled (right call) and returns a genuinely useful error — toast `error` **"e2e-windows.local: No MAC address on file. The agent must check in at least once before Wake-on-LAN is available."**
- ✅ **The row action menu has the best offline copy in the app**: Remote Terminal disabled, Run Script / Reboot / Wake enabled, footnote **"Device is offline — Remote Terminal needs a connected agent. Queued commands still run on reconnect."**
- ❌ **BUG: the "Untagged" quick filter — and every no-value operator — silently matches nothing and shows the whole fleet.** Repro: tag `e2e-macos.local` with `qa-sweep-g4` (persisted, verified in Postgres), hard-reload `/devices`, click **Untagged**. UI actual: chip renders active, hash becomes `#filtersV2=…{"field":"tags","operator":"isEmpty","value":""}`, and the list still reads **"Showing 1 to 2 of 2"** with the **tagged** device listed. API actual: **`POST /filters/preview` is never sent** — the only device fetch is the unfiltered `GET /api/v1/devices?…limit=200` `200`. Asked directly, the server is right: `POST /api/v1/filters/preview {"conditions":{…"tags","isEmpty",""},"idsOnly":true}` → `200 {"data":{"totalCount":1,"deviceIds":["e65460f3-…"]}}`. Cause: `apps/web/src/hooks/useAdvancedFilterIds.ts:10-15` — `hasValidConditions()` requires `c.value !== ''`, but `isEmpty`/`isNotEmpty`/`isNull`/`isNotNull` legitimately carry `value: ''`, so the hook returns `ids = null` and `DevicesPage.tsx:307` reads null as "show everything". The same file set already declares `NO_VALUE_OPERATORS` twice (`filterMigration.ts:59`, `FilterValueEditor.tsx:28`) — this hook just doesn't consult it. Present on `origin/main`; a value-bearing filter (`osType equals windows`) works, so the blast radius is exactly the four no-value operators wherever they are used (the Untagged chip plus anything built in **Advanced**). Silent wrong answer with no error, which is the dangerous shape: an operator filtering for untagged machines gets handed the whole fleet.
- ⚠️ UI/UX (paper cut 35): **three surfaces disagree about whether you may command an offline device, and the bulk one is both the most permissive and the least honest.** Device detail: **Power disabled**. Row menu: **Reboot enabled**, with copy explaining commands queue until reconnect. **Bulk Actions → Reboot Selected** on two offline devices: no confirmation at all, `POST /devices/bulk/commands` → `201 {"commands":[2 × status:"pending"],"failed":[],"skipped":[]}`, toast `success` **"Bulk Command Sent"** — while the device's own Activity feed calls the very same record **"Command queued — reboot"**. A fleet-wide reboot is one unconfirmed click, and "Sent" overstates what the system itself calls "queued".

#### [search/reports/audit/theme/profile/partner settings] — PARTIAL (one real bug)
- ✅ **Cmd+K** opens with the input focused; `e2e-windows` → **DEVICES / E2E Windows Test Device / e2e-windows.local · offline**, with the arrow/Enter/Esc hint. Esc closes.
- ❌ **BUG: saving a report on `/reports/builder` succeeds silently — 201 with zero feedback of any kind.** Repro: `/reports/builder` → name the report → **Save report**. API actual: `POST /api/v1/reports` → **`201 Created`**, row lands in Postgres (`reports.name = 'QA Sweep G4 Device Report'`) and shows up under Saved Reports on `/reports`. UI actual: **no toast, no redirect, no inline success, no form reset** — the builder sits exactly as it was, so the natural next move is to click Save again and mint duplicates. Cause: `pages/reports/builder.astro:20` renders `<ReportBuilder client:load mode="builder" />` with **no `onSubmit` prop**, and `ReportBuilder.tsx:1309`'s only success path is `onSubmit?.(values)` — an optional call that is simply skipped. Failure is handled (`setError` at :1311), success is not. The file is in **neither** the `no-silent-mutations` adopted set **nor** `runActionAllowlist.ts`, so nothing guards it. Present on `origin/main`.
- ✅ **Reports** otherwise fine: `/reports` empty state → CTA; `/reports/templates` renders the curated set (Security & Compliance Posture, Executive Summary, Device Health, Patch…) each with cadence/format/default-range and a **Use template** button; the builder renders report-type cards, data-source scoping, record filters, chart types, schedule cadence, and **PDF / CSV / Excel** export-format controls. `/reports` after the save lists the report with a real **LAST GENERATED** timestamp.
- ✅ **Audit trail**: loads, paginates (`Showing 1-25 → 26-50 of 207`, pages 1-7), and the action-type filter narrows correctly (`delete` → `Showing 1-4 of 4`). Every mutation from this sweep is present with actor, resource name and details.
- ✅ **Theme toggle**: the header Theme menu offers **Light / Dark / System** plus an INTERFACE DENSITY group (Comfortable / Compact / Dense). All three themes apply instantly and persist — measured `documentElement.className` `'' → 'dark' → ''`, `localStorage.theme` `light → dark → system`, `body` background `rgb(249,250,251) → rgb(13,16,23) → rgb(249,250,251)`.
- ✅ **Profile menu**: renders name/email and all four groups (ACCOUNT: Profile, Settings · SECURITY: API Keys, Service Principals, Trusted devices, Connected apps · BILLING & SUPPORT: Billing, Contact support, Activity Log · Sign out) with correct hrefs.
- ✅ **Sign out + back in**: Sign out lands on `/login` cleanly; re-login with the same credentials lands on `/` (Dashboard) with the org context reset to **Default Organization** and live tiles (Total Devices 2, Critical 1, Patch compliance 33%). No 429, no stuck state.
- ✅ **Partner settings, valid save**: Company tab → contact name / email / website → **Save Settings** → `PATCH /api/v1/orgs/partners/me` **`200`** + toast `success` **"Partner settings saved"**; verified persisted (`partners.settings->'contact'`).
- ✅ **Partner settings, `javascript:` URL is rejected**: `javascript:alert(document.cookie)` in Website → **inline error "Website must be a full http:// or https:// URL"** and **no network request is made at all** (the PATCH never fires). Correct client-side gate.
- ⚠️ UI/UX (paper cut 36): **Cmd+K cannot find almost any setting, though the placeholder promises it can.** `webhook`, `sso`, `notification`, `theme`, `partner` all return "No results found. Try a different query." while the input reads "Search devices, scripts, alerts, users, **settings**…". The settings index is a hardcoded three-row array — `apps/api/src/routes/search.ts:17-19`, only Profile / Security / User management (`profile` does return a SETTINGS hit, so the category works). Every other settings page — Enrollment Keys, Webhooks, SSO, Notification Channels, Organizations, Partner, AI Agents, Integrations, LLM Provider Catalog — is unreachable from the palette.
- ⚠️ UI/UX (paper cut 37): partner-settings field validation is **asymmetric and leaks the server field path**. Website is validated client-side with a clear inline message; email is not validated client-side at all, so `not-an-email` posts and comes back as a toast reading **`settings.contact.email: Invalid email address`** — raw dotted path, no inline marker on the offending field. Same shape as paper cut 5, now also on `/settings/partner`.

**Fixtures inserted / state left behind** (Default Organization `4f62e5d3-8914-4d1e-8ba5-87de93ffee92`), for anyone re-running:
- 3 rows in `alerts` — `aaaa1111-0000-4000-8000-000000000001` "QA sweep G4: CAS resolve target" (now **resolved**), `…000000000002` "QA sweep G4: ack target" (now **acknowledged**), `…000000000003` "QA sweep G4: suppress target" (now **suppressed until 2026-08-29 20:41 UTC**). The two pre-existing `E2E fixture:` alerts are untouched and still active.
- 3 `enrollment_keys` left from the #4126 checks: `Onboarding token (2026-08-28)` (50 uses, 30 d), `Add device link (2026-08-28)` (1 use — the stray parent, paper cut 32), `Add device link (2026-08-28) (link x50)` (50 uses). `QA Sweep Key G4` was created and then deleted as the revoke test.
- Device tag **`qa-sweep-g4`** added to `e2e-macos.local` (this is what exposes paper cut 35 — remove it to restore a fully untagged fleet).
- 2 **pending `reboot` device_commands** queued against both offline fixtures by the Bulk Actions test; they will never execute (no live agent) but will show in each device's Activity feed.
- Saved report **"QA Sweep G4 Device Report"** (`reports.id bc99568a-…`, device_inventory, weekly/PDF).
- Partner contact settings populated: `QA Sweep Contact` / `qa-sweep@example.com` / `https://qa-sweep.example.com`.
- Notification channel "QA Sweep G4 Webhook" was created, tested and **deleted** — channels list is back to `0 of 0`.
- Session left **logged in on Default Organization**, theme back on **System**, devices page size left at 10.

## UI/UX paper cuts

| # | Where | Observation | Severity | Disposition (fixed <sha> / issue # / noted) |
|---|---|---|---|---|
| 1 | `/admin/llm-provider-catalog` → Verify | Verification renders a bare **"Failed"** pill; the API's three-step verdict (`steps[].name`/`.detail`, incl. the real HTTP status) is discarded at `LlmProviderCatalog.tsx:153,357`. Operator cannot tell bad URL from bad key from non-compatible endpoint. | high | noted |
| 2 | `/settings/sso` (all mutations) | Save / create / delete all return 200 with **no success toast** — only the drawer closing and the list re-rendering. One `runAction` in the file (no `successMessage`); delete and activate/deactivate bypass `runAction` entirely with inline-`setError`-only failure handling. File is in neither the `no-silent-mutations` adopted set nor `runActionAllowlist.ts`. | med | noted |
| 3 | Sidebar → Workspace Dashboard / Sources | Extension nav hrefs double their own prefix — registry serves an already-qualified `navigation[].path`, `useExtensionNavigation.ts:61` prepends `/extensions/<name>` again, so the link is `/extensions/workspace/extensions/workspace/dashboard`. The clean URL 404s, so the doubled one is the working one. | med | noted |
| 4 | `/settings/ai-usage` | `<main>` stays **completely empty for ~9.3 s** on load — no skeleton, no spinner, no heading — then the whole page appears at once. | med | noted |
| 5 | Catalog + Partner AI Provider forms | Server validation errors are toasted with their raw field path attached: `"modelMap.claude-opus-4-8.providerModel: Too small: expected string to have >=1 characters"` (a raw Zod message) and `"apiKey: Enter a valid Anthropic API key."` | med | noted |
| 6 | `/admin/llm-provider-catalog` rows | **Activate** and **List** are disabled with no tooltip or inline reason for the prerequisite (an active/verified revision); the only hint is a "Not verified" badge elsewhere in the row. | low | noted |
| 7 | App-wide (any page) | A `429` from the global rate limiter (300 req/60 s per IP) renders as a permanent hard-error/stalled-loading state with no retry and no indication it is a rate limit — "Failed to fetch SSO providers", "Failed to load", or a stuck "Loading reports…". The sidebar's ~8 badge/config fetches per page load make this easy to reach when clicking quickly. | low | noted |
| 8 | `/settings/organizations` → Archive | An org being archived is in **neither list** for the whole drain window — `status='offboarding'` drops it from the active list instantly, but the Archived section keys on `status='archived'` and says "No archived organizations." until `offboardingDrainReaper` (5-min cadence, `OFFBOARDING_DRAIN_WINDOW_HOURS` ceiling) finalizes. Measured 3 min on an agentless org; hours on a real tenant. Not listed, not searchable, not restorable — during exactly the window the modal calls "being archived". | high | noted |
| 9 | `/settings/organizations` → Archive (audit) | `org.archive.requested` records `"purgeAt": {}` — the `Date` serializes to an empty object, so the retention decision is missing from the immutable record even though the API response carries it correctly. Two rows are also written per request. | high | noted |
| 10 | Merge failure (`/audit`) | `org.merge.failed` is written with `org_id = NULL` (deliberate, `orgMerge.ts:927`), so it renders in neither the org-scoped audit view nor the **All organizations** fleet view. The only place a failed merge is ever visible is the modal the user can close. | med | noted |
| 11 | Top-bar org switcher | Switching organizations logs the user out on the next navigation (4/4 attempts): the switch's token re-mint races the page's own refresh — captured `POST /auth/refresh` `200 (4791ms)` alongside `409 (13999ms)`, then `401` → `?reason=session-expired`. Loser of the family conflict kills the session instead of adopting the winner's token. | high | **fixed `1e303bf58` (web) + `aa23045a6` (api) — re-verified by the Group 4+5 sweep: 4 switches, 8/8 refreshes `200`, no logout; a direct concurrent-refresh repro now returns `200` + `401 {"reason":"refresh_raced"}` instead of `409`** |
| 12 | `/settings/organizations/:id` for an archived org | Renders the **full editable** settings page (name editable, Save enabled, all tabs, no archived badge or banner), contradicting the "archived and read-only" notice on the list page. The write is refused server-side but with wrong copy: `PATCH` → **404** `{"error":"Organization not found"}` for an org whose name is on screen. | med | noted |
| 13 | `/settings/organizations` → Archive modal | Archiving the org you are **currently switched into** is allowed with no warning; afterwards the switcher still reads the org name with an **Active** badge, `currentOrgId` still points at it, and `/devices` renders a completely empty `<main>` with no message. Nothing auto-switches to a surviving org. | med | noted |
| 14 | `/settings/organizations` → Archive modal | Archive has **no typed-name confirmation** (merge does), yet one click uninstalls the agents from every device in the org — and Restore explicitly does not undo that ("must be re-enrolled"). | med | noted |
| 15 | Top-bar org switcher | Goes stale after an org rename — `OrgSwitcher.tsx:83-85` calls `fetchOrganizations()` in a mount-only effect and nothing invalidates it, so the panel keeps showing the old name until a full page load while the org list beside it is already correct. | med | noted |
| 16 | Merge modal preview | `organizationsPage.merge.totalMovableRows` = `"{{count}} rows will move"` with no `_one` variant → a one-row merge reads **"1 rows will move"**. The same locale file already uses `_one`/`_other` for 16 other keys. | low | noted |
| 17 | Merge failure panel | Surfaces the raw internal instruction to the operator: "…add one to `orgMergeRegistry.ts` (the registry has no default)". | low | noted |
| 18 | `/settings/ai-agents` → act-mode editor | The validation panel is stale: after filling in the name it still reads "Give the agent a name." (only recomputed inside `save()`, which the ack gate prevents from running). Meanwhile the real blocker — the unticked act acknowledgement, ~900px up the form — has no message anywhere near the disabled **Save**. | med | noted |
| 19 | `/settings/ai-agents` → Notifications | The recipient checklist offers all ten roles regardless of membership, but the act prerequisite resolves roles to live users (`recipients.ts:218-247`). Picking a role with no members re-fails with the identical "Add at least one notification recipient before enabling act mode." and no hint that the role resolves to nobody. | med | noted |
| 20 | `/ai-agents/runs/:id` → Execution trace | `actOpKey` and `actTargetName` are mapped into the DTO (`runTrace.ts:110-111`) but never rendered — an executed entry reads "manage_services / restart" without naming *which* service, on the one screen meant to audit unattended execution. | med | noted |
| 21 | `/ai-agents/runs/:id` → Exposure budget | `allowance = floor(devices × pct / 100)` with no `max(1, ·)` (deliberate), so a small fleet shows **"0 of 0 devices"** — indistinguishable from "nothing used yet" though it actually means the agent can never act unattended at this fleet size. No copy explains it. | med | noted |
| 22 | Act mode + policy-decide copy | Both sections promise "the kill switch", but `ai_kill_state` has no route and `bumpAiKillState` has zero callers (its own header says so). The emergency stop for unattended execution is reachable only by hand-editing the row in Postgres. | high | noted |
| 23 | `/ai-agents/runs` → Agent filter | Options come from `GET /ai/agents` (live agents only), so once an agent is disabled its historical runs — still listed correctly — can no longer be filtered to. | med | noted |
| 24 | `/ai-agents/runs/:id` → Linked approvals | Intent status prints raw (`pending_approval`) while run statuses two sections above are humanised; "Review in Approvals" is a bare `/approvals` link with no filter or deep-link to the intent. | low | noted |
| 25 | Script library modal + scripts table + Execute modal + Cmd+K | "Migrate Agent Edition (Windows)"'s ~900-char runbook description is rendered unclamped in all four surfaces — in Cmd+K it fills the entire palette (`.playwright-mcp/g3-cmdk-migrate-script-description-overflow.png`). Every other library entry is one line. | med | noted |
| 26 | Execute Script modal | Empty device list says "No compatible devices found. This script requires Windows." when the real cause is `statusFilter` defaulting to `'online'` (`ScriptExecutionModal.tsx:55`) and the only Windows device being offline — switching Status → Offline reveals it immediately. | med | noted |
| 27 | `/settings/ai-agents` (create) | An **org-only** agent shows "Act … · Enabled" but can never run: `resolveEffectiveAgentInner` returns null with no partner-wide baseline for that kind (`effectivePolicy.ts:309`), so `GET /ai/agents/exposure-budget` 404s and the run detail says "No active agent policy for this run's organization." The form defaults to "This organization only" whenever the session is pinned to one org. No warning anywhere. | high | noted |
| 28 | `/ai-agents/runs` → filters | Agent/status filters are not reflected in the URL, so a filtered view is neither shareable nor survives a reload — the repo convention for this UI state is `window.location.hash`. | low | noted |
| 29 | Merge into an archived org | Refused with **403** `{"error":"Access denied to the surviving organization"}` — reads as a permissions problem when the real reason is that the survivor is being archived. | low | noted |
| 30 | `/settings/sites/:id` | `SiteDetailPage.tsx:97` hardcodes `const REFERENCE_TIME = '12:00'` as the initial `lastSavedAt`, so a freshly opened site page always claims **"Saved at 12:00"** before anything has been saved in that session. | low | noted |

| 31 | `/settings/enrollment-keys` → Create Key | "Max Usage (optional)" has placeholder **"Unlimited"** but leaving it blank yields `max_usage = 1` (`routes/enrollmentKeys.ts:1062` `data.maxUsage ?? 1`) — the list then reads "0 / 1". Placeholder promises the opposite of what happens, and a single-use key is exactly the mass-deploy failure #4126 was written to prevent. #4126 raised the *TTL* default here (blank expiry → 30 days ✅) but not the device count. | med | noted |
| 32 | `/devices` → Add Device → Generate Link | Each click writes **two** enrollment keys — `AddDeviceModal.tsx:585-593` creates the parent with no `maxUsage`/`ttlMinutes`, so the Enrollment Keys list shows `Add device link (2026-08-28)` at **"0 / 1"** immediately above `… (link x50)` at "0 / 50". The one that looks single-use is the one the operator will read first. | low | noted |
| 33 | Devices list → Bulk Actions vs. device detail vs. row menu | **Three surfaces disagree on whether an offline device can be commanded, and the bulk one is the most permissive and the least honest.** Device detail: Power/Run Script **disabled** (`title="Device is offline"`). Row menu: Reboot **enabled**, with the app's best copy — "Queued commands still run on reconnect." Bulk Actions → **Reboot Selected** on two offline devices: no confirmation, `POST /devices/bulk/commands` → `201` with two `status:"pending"`, toast `success` **"Bulk Command Sent"** — while the device's own Activity feed calls that record **"Command queued — reboot"**. A fleet reboot is one unconfirmed click and "Sent" overstates "queued". | med | noted |
| 34 | `/alerts/channels` → Delete | Confirm body is a title-cased key echo that **drops the interpolation it is passed**: `NotificationChannelsPage.tsx:724` calls `t('…deleteChannelConfirm', { name })` but `en/alerts.json:934` is `"Delete Channel Confirm"` — no `{{name}}`, no sentence, no consequence. Machine-translated the same way in de-DE/es-419/it-IT/pt-BR/tr-TR; only fr-CA/fr-FR read as real sentences. The enrollment-key delete one screen over names the key and states the consequence. | med | noted |
| 35 | `/devices` → **Untagged** quick filter (and Advanced → any no-value operator) | **Silently matches nothing and returns the whole fleet.** `useAdvancedFilterIds.ts:10-15` gates on `c.value !== ''`, but `isEmpty`/`isNotEmpty`/`isNull`/`isNotNull` legitimately carry `value: ''`, so `POST /filters/preview` is never sent and `DevicesPage.tsx:307` reads the null id-set as "show everything". Server is correct when asked directly (`200 {"totalCount":1,…}`). `NO_VALUE_OPERATORS` is already declared twice elsewhere (`filterMigration.ts:59`, `FilterValueEditor.tsx:28`) — this hook just doesn't consult it. Filed as the ❌ in the Group 4+5 log; listed here too because it is a one-line fix. | high | noted |
| 36 | Cmd+K palette | Cannot find almost any setting despite the placeholder "Search devices, scripts, alerts, users, **settings**…". The settings index is a hardcoded three-row array (`apps/api/src/routes/search.ts:17-19`: Profile, Security, User management). `webhook` / `sso` / `notification` / `theme` / `partner` all return "No results found." | med | noted |
| 37 | `/settings/partner` → Company | Validation is asymmetric and leaks the server field path: Website is checked client-side with a clear inline message (and correctly rejects `javascript:`), but email is not, so `not-an-email` round-trips to a toast reading **`settings.contact.email: Invalid email address`** — raw dotted path, no inline marker on the field. Same shape as paper cut 5. | low | noted |
| 38 | `/reports/builder` → Save report | **201 Created with zero feedback** — no toast, no redirect, no inline success, no form reset, so the natural next click mints a duplicate report. `pages/reports/builder.astro:20` renders `<ReportBuilder mode="builder" />` with no `onSubmit`, and `ReportBuilder.tsx:1309`'s sole success path is the optional `onSubmit?.(values)`. Failure is handled (`setError`), success is not; the file is in neither the `no-silent-mutations` adopted set nor `runActionAllowlist.ts`. Filed as the ❌ in the Group 4+5 log. | high | noted |

## Fixes applied

| Commit | PR/area | What | Test |
|---|---|---|---|
| `80f43722a` | #4113/#4116 compose parity | Map `LLM_PROVIDER_CATALOG_ENABLED` in `docker-compose.yml` + `deploy/docker-compose.prod.yml`, document in `.env.example` | `envComposeParity.test.ts` +3 (RED with mapping removed, GREEN 14/14). Note: parity test is one-directional (`.env.example` → compose); ~24 validated-but-undocumented keys remain (hosted-only `STRIPE_*`, `QBO_*`, …) — separate initiative. |
| `b9195a370` | #4113/#4130 catalog verify | Render `steps[]` (✓/✗ + name + detail) under the verify verdict pill instead of discarding them | `LlmProviderCatalog.test.tsx` +1 (RED: text not found; GREEN 13/13); tsc clean |
| `a032bcb62` | /settings/sso silent mutations | Delete + status-toggle through `runAction` with `successMessage`; save gains `successMessage`; test-connection/preflight marked `runaction-exempt` (feed a modal); page added to no-silent-mutations adopted set; 8 locales | guard RED (5 raw fetches) → GREEN; `SsoProvidersPage.test.tsx` +3 (RED→GREEN); localeParity green; tsc clean |
| `779b5cf0e` | /settings/ai-usage slow render (paper cut) | `effective-settings` fetched in the same `Promise.all` as usage+sessions (was sequential; 6.9 s cold on dev). Follow-up: the endpoint itself is slow cold — API-side, not fixed here | `AiUsagePage.test.tsx` +1 (RED: 3rd URL absent; GREEN 6/6); tsc + eslint clean |
| `8b6fda259` | #4132 archived org detail (paper cut #12) | `OrgSettingsPage` shows an archived banner (purge date) and disables name/type Save when the GET returns `archived:true` — PATCH was 404ing with "Organization not found" | `OrgSettingsPage.test.tsx` +2 (RED→GREEN 20/20); localeParity 79/79; eslint clean; tsc skipped (>4 min) |
| `cd10a2135` (+ revert commit) | #4128 audit field (paper cut #9) | `org.archive.requested` emits `purgeAt` as ISO (was `{}`). The `org.merge.failed` survivor-attribution half was **reverted** after CI: `orgMerge.integration.test.ts:1275-1279` pins the row org-less by design ("an audit scoped to the loser would be destroyed if a retried merge succeeds"); the resulting invisibility in every audit view (paper cut #10) stays a design question on #4165 | `routes/orgArchive.test.ts` +1 (+1 corrected) RED→GREEN; built in an isolated worktree, cherry-picked |
| `1e303bf58` + `aa23045a6` | #4097 regression → #4167 (p0) | web: `refreshFetchOnce` treats `409` from `/auth/refresh` as `{raced:true}` so the existing retry-once path applies; api: `/refresh`-only `refreshIssuanceAdmissionError` maps retryable `AuthIssuanceConflictError` to the established `401 {reason:'refresh_raced'}` (binding-unavailable / capability 409s unchanged; `/login`, `mfa`, `passkeys` untouched) | web `src/stores/auth` 4 files/102 tests (RED: `AuthSessionExpiredError` reproduced → GREEN); api `src/routes/auth` 24 files/588 (RED `expected 409 to be 401` → GREEN); eslint + tsc clean both packages; opus review: **ship** (scope verified `/refresh`-only; retry idempotent, cannot trigger reuse-detected — 200 ms retry inside the 15 s grace; loop bounded ≤6 calls). Follow-up landed: second consecutive race → transient backoff ladder + 409→409 cap test |
| `5a5b01f02` | #4157 run detail (paper cut #20) | Execution-trace entries now render `actOpKey · actTargetName` when present (runTrace mapped them; the page never showed them) | `RunDetailPage.test.tsx` +1 (RED→GREEN 8/8); parity 79/79; eslint clean |
| `f9f3e460c` | Scripts → Execute modal (paper cut #26) | Empty state distinguishes "N compatible devices are hidden by the status filter" (+ inline "Show all devices" → existing `setStatusFilter('all')`) from the true "requires <OS>" case; 8 locales | `ScriptExecutionModal.test.tsx` +1 (RED→GREEN 12/12); i18n suites 110/110; eslint clean |
| `0e7ce6248` | #4167 review follow-up | A second consecutive raced `/auth/refresh` (409 or `refresh_raced`) is treated as transient and enters the existing bounded backoff ladder instead of `auth-failed`; max 6 refresh calls per `requestTokenRefresh` | `auth.test.ts` +2 (409→409→200 applies token; always-raced gives up within cap) RED→GREEN 4 files/104; eslint clean |
| `9aed550d9` | Devices Untagged / no-value filters (paper cut #35, ❌) | `hasValidConditions` accepts `NO_VALUE_OPERATORS` (exported from `filterMigration.ts`) regardless of `value`, so the preview request is sent instead of falling back to the whole fleet | `useAdvancedFilterIds.test.ts` +1 (RED: fetch never called → GREEN 6/6); eslint + tsc clean |
| `614c405a5` | #4126 Create Key form (paper cut #31) | Blank device-limit field now sends `maxUsage: 50` (`PRODUCT_DEFAULT_ENROLLMENT_DEVICE_COUNT`) and the placeholder reads "50 (default)" — it said "Unlimited" while the API defaulted an omitted value to 1 (deliberate for the installer flow, #2992, so untouched); 8 locales | `EnrollmentKeyManager.test.tsx` +1 (RED: `undefined` → GREEN 22/22); parity 101/101; eslint + tsc clean |
| `6b85dbf88` | /reports/builder Save (paper cut #38, ❌) | Non-adhoc save goes through `runAction` with a success toast; navigates to `/reports` when the mount passes no `onSubmit` (builder.astro), unchanged for callers that do; adhoc/live-preview fetches `runaction-exempt`; page added to the no-silent-mutations adopted set; 8 locales | guard RED (3 raw fetches) → GREEN; `ReportBuilder.test.tsx` +2 (RED→GREEN 8/8); i18n 221; eslint + tsc clean |

## Issues filed

| Issue | Title | From row |
|---|---|---|
| #4164 | [UI] Extension web assets require a bearer header the dynamic import() cannot send → every enabled extension's UI shows "unavailable" (401) | Group 1 / #4112 (workspace) |
| #4165 | [API] Org merge fails whenever an enabled extension owns org-scoped tables — no merge policy hook for extension tables (workspace_org_settings); + org.merge.failed audit rows org-less | Group 2 / #4111, #4114 |
| #4166 | [API] Archiving org (status=offboarding) vanishes from the organizations list for the whole drain window — unlisted, unsearchable, Restore unreachable | Group 2 / #4132, paper cut #8 |
| #4167 | [Auth] Org switch logs the user out — raced /auth/refresh returns a 409 the web store treats as session-expired (regression from #4097, unreleased; **p0 release blocker**) | Group 2 / #4097 |
| #4169 | [AI agents] "Kill switch" referenced in act-mode/policy-decide copy has no route and no caller — emergency stop unreachable from the product (high) | Group 3 / #4148, #4155, paper cut #22 |
| #4170 | [AI agents] Org-only agent shows "Act · Enabled" but is inert without a partner-wide baseline — resolver returns null, exposure-budget 404s, no warning | Group 3 / #4146–#4155 |

## Summary

Inventory: 40 PRs (39 merged since `v0.108.0` + #4163 merged during the gate). No row left `TODO`.

| Group | PASS | PARTIAL | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|
| Env / stack bring-up (main session) | — | — | 2 (both fixed) | — | — |
| 1 · Settings/Partner + SSO + BYO-LLM | 2 | 2 | 0 | 4 | 0 |
| 2 · Organizations lifecycle | 1 | 2 | 2 | 0 | 1 |
| 3 · AI agents / act / policy-decide / runs / scripts | 5 | 1 | 0 | 0 | 5 |
| 4+5 · devices / remote / enrollment / alerts / everyday | 5 | 2 | 0 | 1 | 12 |
| **Inventory rows** | **11** | **5** | **2** | **4** | **18** |

Paper cuts: **38** (8 high · 20 med · 10 low). Issues filed: **7** (#4164–#4167, #4169, #4170 + this doc). Fix commits on `qa/sweep-post-v0.108.0`: see *Fixes applied*.

**Top findings (systemic first):**
1. **A regression that would have shipped in v0.109.0** — #4097's issuance lease turned the benign refresh race into a `409`, and the web store treated it as session-expired, so every org switch logged the user out (#4167, p0). Fixed on this branch (web + api), reviewed, and re-verified in the browser (8/8 refresh `200`). Lesson: `authIssuanceAdmissionError` flattened a `retryable` error into a non-retryable status — any new admission/lease path needs a client-contract test, not just a server test.
2. **Registration-list gaps keep shipping** — `LLM_PROVIDER_CATALOG_ENABLED` validated but never mapped in compose (fixed); extension org-tables have a cascade hook but no merge-policy hook, so org merge is dead with the workspace extension on (#4165). Same shape as the five cascade-list misses in CLAUDE.md: the contract tests catch what review doesn't, and both of these had no contract test.
3. **Silent-wrong-answer UI** — Untagged/no-value filters return the whole fleet (fixed); report Save 201s with no feedback and duplicates (fixed); SSO provider mutations toasted nothing (fixed); catalog verify discarded its `steps[]` (fixed). Four instances of "the API was right, the UI said nothing or the opposite".
4. **Advertised controls that don't exist** — the act-mode/policy-decide "kill switch" has no route and no caller (#4169); an org-only AI agent reads "Act · Enabled" but is inert (#4170). Both are AI-agents wave 4–6 surfaces, unreleased, both should be resolved before the flag goes on anywhere.
5. **Every enabled extension's web UI is dead** — assets are gated behind bearer `authMiddleware` but loaded via bare `import()` (#4164). Latent (workspace is off in prod) but blocks the whole `ee/workspace` front end.
6. **Dev-stack sharp edges** (fixed on this branch): `docker-compose.override.yml.dev` mounted `ee/workspace/src` but not `packages/extension-sdk/src`, so an SDK export added on main crashed a non-rebuilt API at boot.

**Oldest PR reached:** #4066 (every PR since `v0.108.0`; nothing older was in scope).

**Before the release cut:**
- Merge this branch's fixes — the #4167 fix is the release blocker; without it v0.109.0 logs users out on org switch.
- Decide on #4169 (kill switch) and #4170 (inert org-only agents) before `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED` is set anywhere; #4164 and #4165 before `BREEZE_WORKSPACE_ENABLED` is flipped on (both off in prod today).
- Self-hosters: `LLM_PROVIDER_CATALOG_ENABLED` is now mapped in compose and documented in `.env.example`; note it in the release body.
- BLOCKED rows needing prerequisites to re-test: #4080 (real IdP bound to an active **partner-axis** SSO provider), #4116/#4118 (publicly reachable HTTPS OpenAI-compatible endpoint + key — the egress guard blocks loopback), #4112 (blocked by #4164), #4161 live reconnect (a live agent).
- Stack left running for inspection: `http://localhost:32873` (`breeze-wt-qa-sweep-post-v0-108-0`); `pnpm wt-stack down` to tear down. Fixtures/orgs left behind are itemised per group above.
