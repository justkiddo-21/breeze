# Pre-release sweep — v0.109.0 → origin/main (2026-09-05)

| | |
|---|---|
| Range | `v0.109.0` (`2d35e5f5dd`, 2026-09-01) → `origin/main` `78eae279f` |
| Gate | PRs waited on: none (open non-draft #4902 #4903 #4904 are other-session, #4624 held); main CI green on 78eae279f; known-noise: none dismissed |
| Stack | `pnpm wt-stack up` on `qa/sweep-post-v0.109.0` @ `78eae279f`, baseUrl `http://localhost:33028` (project `breeze-wt-qa-sweep-post-v0-109-0`, 673 migrations applied, admin promoted to platform admin) |
| Flags enabled | `BREEZE_WORKSPACE_ENABLED=true`, `BREEZE_AI_AGENTS_ENABLED=true`, `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED=true`, `LLM_PROVIDER_CATALOG_ENABLED=true` (api recreated after inventory) |
| Driver | skill `pre-release-sweep`; sweep agents: opus (browser), sonnet (inventory, fixes) |

## Pre-sweep findings (before any browser work)

- **RLS-blind migrations in range (3):** `2026-10-06-100101-authenticator-attestation-state.sql`, `2026-10-08-100600-audit-retention-manage-permission.sql`, `2026-10-08-100700-audit-retention-policies-org-unique.sql` run DML on FORCE-RLS tables with no `set_config('breeze.scope','system',true)`. Silent 0-row on managed PG (doadmin). Same class as #4483. Fix-forward replay migration `2026-10-09-000600-rls-scoped-replay-v0110.sql` + `REPLAYED_BY` assertion in the existing contract test (#4866): **PR #4926**. **Premise downgrade:** `apps/api/scripts/check-migrations-nonsuperuser.ts` models `doadmin` WITH `BYPASSRLS` (measured: without it the set aborts at `2026-05-22-snmp-multi-vendor-templates.sql` 42501), and 122 unscoped-INSERT migrations have never aborted hosted prod — so on hosted the replay will print all-zero counts. It still protects self-hosters with a NOBYPASSRLS migrator. Not a release blocker; merge on green for the evidence trail.
- **Possible v0.109.0 regression, same class:** `2026-09-25-b-cross-site-restore-permission.sql` and `2026-09-27-technician-ticket-write-permissions.sql` use unscoped `INSERT ... SELECT FROM roles`. Read-only prod check script at `~/Documents/breeze-release-checks/prod-rls-grant-check.sql` (reports `rolbypassrls` + missing grants); replayed by #4926 regardless.

## Change inventory

Status ∈ `TODO | PASS | PARTIAL | FAIL | BLOCKED | N/A`. Every row must leave `TODO`.

### New / changed environment variables

Sourced from `git diff v0.109.0 origin/main -- apps/api/src/config .env.example docker-compose.yml`:

| Variable | Default | Notes |
|---|---|---|
| `BREEZE_AUTHENTICATOR_ATTESTATION_ENFORCED` | unset (off) | Optional boolean (`true/false`, `1/0`, `yes/no`, `on/off`). L4 platform-bound-passkey attestation gate (#1374). Malformed value throws at startup. |
| `CADDY_LOCAL_CERTS` | unset (Let's Encrypt) | Set to literal `local_certs` when `BREEZE_DOMAIN` is unreachable by Let's Encrypt; Caddy issues from its internal CA instead. |
| `SMTP_TIMEOUT_MS` | `30000` | Inactivity timeout for the SMTP transport (#3905). Range 1000–600000ms. |
| `MAILGUN_TIMEOUT_MS` | `120000` | Whole-request deadline for the Mailgun API call (includes attachment upload). |
| `PARTNER_API_ENROLLMENT_KEY_MAX_TTL_MINUTES` | unset (365-day ceiling) | Partner API enrollment-key mint TTL cap; may only narrow, never widen. |
| `PARTNER_API_ENROLLMENT_KEY_WRITE_RATE_LIMIT` | `10`/hr | Per-service-principal mint rate limit. |
| `PARTNER_API_ENROLLMENT_KEY_WRITE_PARTNER_RATE_LIMIT` | `100`/hr | Partner-wide mint rate limit (charged alongside the per-principal bucket). |
| `AGENT_LOG_RETENTION_DAYS` | `7` (clamped 1–365) | `agent_logs` row pruning window (#3109 redaction hardening). |
| `AGENT_LOG_RETENTION_BATCH_SIZE` | `10000` | Prune batch size. |
| `AGENT_LOG_RETENTION_MAX_BATCHES` | `200` | Prune batches per run. |
| `TICKET_OUTBOX_RETENTION_DAYS` | `14` | Outbox retention (#4210). |
| `TICKET_OUTBOX_RETENTION_BATCH_SIZE` | `5000` | |
| `TICKET_OUTBOX_RETENTION_MAX_BATCHES` | `50` | |
| `INTENT_OUTBOX_RETENTION_DAYS` | `14` | |
| `INTENT_OUTBOX_RETENTION_BATCH_SIZE` | `5000` | |
| `INTENT_OUTBOX_RETENTION_MAX_BATCHES` | `50` | |
| `METRIC_ANOMALY_INCIDENT_RETENTION_DAYS` | `14` | |
| `METRIC_ANOMALY_INCIDENT_RETENTION_BATCH_SIZE` | `5000` | |
| `METRIC_ANOMALY_INCIDENT_RETENTION_MAX_BATCHES` | `50` | |
| `QBO_CLIENT_ID` / `QBO_CLIENT_SECRET` | unset (dark) | QuickBooks Online OAuth app credentials (Phase C/D). |
| `QBO_REDIRECT_URI` | unset | Must match the Intuit app's registered redirect URI; must be this deployment's public API URL. |
| `QBO_ENVIRONMENT` | unset (`production` when set) | `sandbox` or `production` Intuit API host. |
| `QBO_WEBHOOK_VERIFIER_TOKEN` | unset | Without it, `POST /api/v1/webhooks/quickbooks` answers 503 and payment pull-back relies solely on the 15-minute sweep. |

Also changed (not a new app-facing var, but a default-safety fix worth flagging for self-hosters): the M365 executor JWK secret file default moved from `/dev/null` to a tracked empty file `./docker/secrets/.empty-jwk` (#2991) — bind-mounting `/dev/null` read-only broke `docker compose up` on some kernel/storage-driver combos.

---

### Group 1 — Billing, Quotes, Contracts & Accounting (11 rows)

| # | PR(s) | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 1 | Device-set billing #3205 (W01–W07): #4585, #4694, #4745, #4808, #4854, #4731, #4838, #4901, #4706 (docs) | web | Settings → Organizations → an org → **Contracts** tab (`/settings/organizations/[id]#contracts`) → open/add a contract line billed by **device role** or **device group**; then Billing → Contracts → open a contract → generate an invoice. | Contract line editor shows a device-role/device-group picker and included-quantity/overage fields; toast **"Line updated"** on save. Generating an invoice with uncovered devices shows the toast **"Invoice generated, but N device(s) are not billed by any line on this contract: …"**; the contract detail page renders a coverage warning (`DeviceCoverageNotice`, testid `contract-coverage-warning`) with deep links into Devices filtered by role. Quote editor (Billing → Quotes) shows device-set lines with a **"Refresh counts"** button and a drift toast ("N line(s) no longer match the live device count"). | None — seeded org with devices + a contract works. | PARTIAL |
| 2 | QuickBooks Phase C + D: #4492 (Phase C invoice push), #4531 (Phase D payment pull-back), #4800, #4795, #4827, #4537 (docs), #4509 (docs) | web | Settings → **Integrations** (`/integrations`) → Accounting tab → **quickbooks** sub-tab (`QuickbooksIntegration.tsx`); also Billing → Invoices → select unsent invoices → bulk action **"Push to QuickBooks"**. | Integrations page shows the QuickBooks connect/reconcile card with a **"Payment sync"** control (`quickbooksIntegration.pullPayments`); bulk-pushing invoices shows the toast **"1 queued for QuickBooks"** (or the bulk-limit warning **"Select up to N at a time."**); invoice detail shows **"Invoice pushed to QuickBooks."** on success. | **BLOCKED without live QuickBooks OAuth app** (`QBO_CLIENT_ID`/`QBO_CLIENT_SECRET`/`QBO_REDIRECT_URI` unset ⇒ integration reports "not configured"). Reconcile-skip-reason and voided-invoice-block logic can be checked without a live connection by inspecting the invoice detail UI copy. | BLOCKED |
| 3 | #4802 | web | Billing → Quotes or Invoices → open a **draft** quote/invoice → currency selector/dialog. | Currency-change dialog opens (`copy.title`); on confirm, toast **"Quote currency changed to {currency}."** / **"Invoice currency changed to {currency}."** | Draft-status quote/invoice only (change-currency UI is gated off non-draft docs). | PASS |
| 4 | #4819 | web | Billing → Quotes → open a quote → reload mid-edit (force a re-render, e.g. resize/hot-reload). | Draft line values are reseeded during React render, not from a passive `useEffect` — no stale/duplicated line values after a re-render. No new visible text; regression-only. | None | PASS |
| 5 | #4816 | web | Billing → Quotes → open a superseded quote (`GET` after acceptance); Tickets → open a ticket → Resolve dialog note field. | No visible UI change — deflakes two existing tests (`QuoteWorkspace` superseded-GET race, `TicketWorkbench` resolve-note prefill race). Verify: opening a superseded quote still shows correct read-only state; ticket resolve-note field still prefills. | None | PASS |
| 6 | #4698 | web | Billing → Quotes → open a quote with several lines at browser width **1280px** with the right-hand sidebar open. | Quote editor's line grid no longer clips/scrolls sideways at 1280px with the sidebar open — full grid visible without horizontal scroll. | Resize viewport to exactly 1280px wide. | PASS |
| 7 | #4397 | web | Billing → Quotes → open a quote → add a new line while a background quiet-refetch is in flight, then force that refetch to fail (e.g. toggle network offline briefly). | Instead of losing the newly-added line, shows the warning toast **"Line added, but the list could not refresh. Reload the page to see it."**; a successful catalog save shows **"Saved to catalog"**. | None | PASS |
| 8 | #4408 | web | Billing → Quotes → line item image upload → pick a `.webp` file. | Upload is rejected client-side (and server-side) with a validation error; no WebP image reaches the quote line. | A `.webp` test image. | PASS |
| 9 | #4639 | web | Settings → **Catalog** (`/settings/catalog`) → catalog item → image upload → pick a `.webp` file. | Same WebP rejection as quote images, applied to catalog item images at the upload boundary. | A `.webp` test image. | PASS |
| 10 | #4634 | web | Billing → Quotes/Invoices → with a partner-level currency set, open a quote/invoice, then trigger a partner currency reset (Settings → org's Billing currency) while the doc is open. | The mounted approximate-total line refetches and reflects the new currency instead of showing a stale total. | Two browser contexts or a quick org-currency change while the quote tab stays open. | PARTIAL |
| 11 | #4636 | web | Settings (`/settings`) → left sidebar. | **"Billing"** now appears as a section in the Settings sidebar (previously missing/orphaned link). | None | PASS |

### Group 2 — Devices, Patching & Reboot (9 rows)

| # | PR(s) | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 1 | Reboot deferral #3207 (W1–W5): #4686, #4699, #4728, #4733, #4737, #4666 (docs) | web (+agent) | Configuration Policies (`/configuration-policies/[id]`) → **Patch** tab → toggle **"Let users postpone the restart"**; then Devices → a device with a pending reboot → device console. | Toggling on reveals **"Maximum postponements"** and **"Postpone by (minutes)"** number fields. On a device with a scheduled restart, the console renders a `RebootScheduledBadge` showing deferrals-used/max and the scheduled time. | **BLOCKED for the end-user prompt itself** — the actual Windows/macOS/Linux reboot-prompt-with-deferral UI (W2–W4) requires a live enrolled agent to receive and act on the reboot command; only the policy-settings UI and the heartbeat-driven console badge (W1, W5) are checkable from a seeded stack alone. | **PARTIAL** — W1 policy UI ✅; W5 console badge ❌ (5 reboot fields dropped by `DeviceDetailPage.tsx` whitelist transform); W2–W4 need a live agent |
| 2 | Unrated patch severity handling: #4721 (web badge), #4787 (api evaluator guards) | web | Fleet → **Patches** (`/patches`) → severity filter dropdown; a patch with no CVSS rating. | Severity filter offers an **"Unrated"** option; a null-severity patch row shows an **"Unrated"** badge instead of being coerced to "Low," with a note that it will not auto-approve. Sorts last after Low. | A patch with `severity: null` in the seed data. | **PASS** |
| 3 | Windows-installer hardening: #4730 (force TLS 1.2), #4688 (Windows 10/Server 2016 OS floor) | web | Devices → **Add Device** / enrollment flow → copy the generated Windows PowerShell install one-liner. | The generated `Invoke-WebRequest` one-liner now pins `[Net.ServicePointManager]::SecurityProtocol = Tls12` and declares Windows 10 / Server 2016 as the supported floor in the accompanying copy. | None (just inspect the generated command text) | **PASS** |
| 4 | #4842 | web | Devices → a device → **Activities** tab. | Activity/audit list is capped at 10,000 rows instead of unbounded — on a device with a very large audit history, the tab loads instead of timing out. | A device with 10k+ audit rows to see the cap trigger; otherwise just confirm the tab loads normally. | **PASS** — `total:10000, totalIsLowerBound:true`, UI shows `10,000+` |
| 5 | #4832 | web | Devices → a device with an existing **live remote session** → click **"Connect Desktop"** to start a *second* session. | Starting a new Connect Desktop session no longer silently revokes every other live remote session for that device — only the superseded session (if any) is affected. | Two remote sessions open concurrently (or two browser tabs) against the same device. | **PASS** — no `DELETE /remote/sessions/stale`; concurrent terminal session stayed `active` |
| 6 | #4783 | web | Devices (`/devices`) → **Advanced filter** → author a filter, then force the preview call to fail (e.g. malformed filter or backend hiccup). | Instead of silently showing a stale/wrong device list, shows the toast **"Advanced filter failed to load — showing no devices. Edit or clear the filter to try again."**, and the list fails closed (empty) rather than open. | None | **PASS** — exact toast + fails closed (0 devices) + inline pill |
| 7 | #4701 | web | Devices → a device → **Patches** tab. | Tab shows a **"Manage patches"** link to `/patches`, and — when the device has an assigned patch policy — a second link with the policy's own name (e.g. "Standard Patch Ring") to `/configuration-policies/{policyId}`. Falls back to the raw policy id as the link label if the name is missing; no crash if the effective-config fetch fails. | Device with (and without) an assigned patch policy, to see both branches. | **PASS** — both branches (with / without assigned policy) |
| 8 | #4695 | web | Devices → a device detail page → breadcrumb showing the org name. | Org name in the device breadcrumb is now a link to `/settings/organizations/{orgId}` instead of static text. | None | **PASS** |
| 9 | #4394 | web | Devices → a device → row action **"Permanent Delete"** → confirm, on a device the agent can't currently reach. | Toast surfaces the agent-unreachable warning explicitly instead of a generic failure. | A device whose agent is offline/unreachable. | **PASS** — warning toast surfaced verbatim |

### Group 3 — Tickets, AI Agents & Workspace (12 rows)

| # | PR(s) | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 1 | AI agents phase 2 — graduation & impact #4187: #4490, #4533, #4565, #4486, #4504, #4581 (docs), #4595 (fix) | web | Settings → **AI Agents** (`/settings/ai-agents`) → edit an agent → **Graduation** panel; then AI Agents → **Impact** (`/ai-agents/impact`). | Graduation panel section titled **"Graduation"** lists per-operation evidence with a **"Promote"** button (enabled once `state === 'eligible'`) and a **"Revoke"** button on promoted rows (requires a reason). Impact page shows tiles: "Alerts judged," "Flagged as noise," "Tickets triaged," "Drafts sent," "Fixes executed," "Estimated time saved," "LLM spend," plus a **"Promotion-eligible operations"** tile and a PDF export. | manage_ai_agents permission; feature is fail-closed if `policyDecideEnabled` is off (Promote button hidden, "Pre-authorized execution is turned off for this deployment; these figures are informational" note shown instead). Four-eyes promote requires a second approver account. | PASS |
| 2 | AI budget alerts #4388 (W01–W04): #4487, #4496, #4517, #4529, #4805 (render-seed fix) | web | Settings → **AI Usage** (`/settings/ai-usage`) → **Budget alert thresholds** section; also visible on `/ai/usage`. | Threshold-ladder inputs render pre-seeded from server data on first paint (no flash of defaults); usage page shows **"Breeze AI credits remaining"** label and partner-level threshold fields with placeholder **"Not set, orgs configure individually"** when unset. | Partner-level AI usage/budget feature must be enabled for the org/partner. | PARTIAL |
| 3 | AI agents UI critique passes: #4667, #4729, #4822 | web | AI Agents → **Impact** page and **Runs** list (`/ai-agents/runs`). | Impact page: refresh control aria-label toggles between **"Refresh"** and **"Rebuilding…"**; a **"More actions"** menu; six effective weights shown in an accessible always-reachable disclosure (not a hover-only `title=` tooltip) — toast **"The impact report has been rebuilt."** on refresh. Runs list: disable-agent confirm dialog titled **"Disable {name}?"**; a banner when **"All agents are disabled"**; re-enabling shows toast **"Agent re-enabled"**. Mobile runs list and policy-key labels also corrected (verify visually only; no separate route). `c715d99c20` is test-scoping only — no product change. | None | PASS |
| 4 | #4704 | web | Platform admin → `/admin/ai-kill-switch`. | Page titled **"AI Kill Switch"**; flipping it shows toast **"AI actions killed platform-wide."** / **"AI actions restored platform-wide."**; a confirm dialog requires a reason (**"Describe the incident or reason for this change…"**); non-platform-admins see **"Platform-admin access required"** instead of the control. | **Platform-admin role required.** | PASS |
| 5 | #4691 | web | Tickets → AI triage batch view → a ticket the AI could not draft for. | Skip reasons are now surfaced on the card instead of the ticket silently disappearing from the batch; drifted cards auto-deselect from the batch action. | An org with AI ticket-triage enabled and at least one triage-ineligible ticket. | PARTIAL |
| 6 | #4683 | web | Tickets → a ticket with an AI-drafted reply → click the draft card's action (approve/discard) rapidly / from two tabs. | The action lock is now scoped to that draft's own id — acting on one draft no longer locks/blocks the action button on a different draft card. | Two AI draft cards on different tickets (or two tabs on the same ticket). | PASS |
| 7 | #4702 | web | AI **Workspace** (`/workspace`) → chat tab. | Double-clicking a chat tab lets you rename it (aria-label **"Rename chat"**, hint **"Double-click to rename"**). | None | PASS |
| 8 | #4687 | web | AI Agents → Shadow mode view; Approvals inbox. | Shadow-mode partner-key rows show a ceiling-hint tooltip; the approvals inbox collapses multiple links pointing at the same intent into one; a client-side guard caps batch-approve size (`BATCH_MAX`). | Partner-level AI agent key configured in Shadow mode. | PARTIAL |
| 9 | #4705 | web | Alerts (`/alerts`) → an alert with existing operator feedback (thumbs up/down). | Alert verdict badge now shows **who** gave the feedback (`feedbackByName`) directly on the badge instead of requiring a hover; badge omits the feedback-by label if the name can't be resolved (e.g., deleted user). | An alert that already has feedback recorded. | PASS |
| 10 | #4868 | web | Approvals inbox (wherever AI-agent approvals surface, e.g. Workspace sidebar). | Refactor only — batch-grouping rule and inbox helpers moved to `@breeze/shared`. No visible UI change; verify the approvals inbox still groups/renders identically to before. | None | PASS |
| 11 | #4875 | web | Anywhere `SweepProposalReason` renders (alerts/AI sweep proposal copy). | Refactor only — type promoted to `@breeze/shared`. No visible UI change; spot-check that sweep-proposal reason text still renders correctly wherever it appears. | None | PASS |
| 12 | iOS papercuts #4916 (defect #5 of 7 in the PR is the only browser-testable piece; defects 1-4/6/7 are mobile-only, see "Not browser-verifiable") | web | AI **Workspace** (`/workspace`) chat → send a message that triggers an approval-required tool (e.g. `execute_command`) → approve/deny the pending request from a second session (mobile app or a direct API call) while the web chat tab stays open. | `processStreamEvent` now clears `pendingApproval` when the `tool_result` for the awaited tool arrives over the open SSE stream — the web chat's pending-approval card disappears once the OTHER session decides, instead of staying stuck showing a decision that already happened. | **BLOCKED for full verification without a second session/device to approve from** — needs an approval-required AI action plus a way to resolve it out-of-band. | BLOCKED |

### Group 4 — Org, Contacts, Settings, Audit & Trust (12 rows)

| # | PR(s) | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 1 | Org contacts #3258 (W02–W04 + follow-ups): #4530, #4557, #4546, #4590, #4593, #4594 | web | Settings → Organizations → an org → **Contacts** tab (`/settings/organizations/[id]#contacts`). | Tab titled **"Contacts"** with buttons **"Add contact"** and **"Import CSV"**; imported/added contacts list with **"Edit"**/**"Delete"** row actions, save button **"Save contact"**. Sending a portal invite to an email matching multiple contacts shows the warning toast (ambiguous-contact copy); a clean match shows toast **"Invite sent"**. | None | **PASS** |
| 2 | Audit log retention #4633/#4824: #4825, #4900 | web | Settings → Organizations → an org → **Audit Retention** tab (`/settings/organizations/[id]#audit-retention`). | New nav entry **"Audit Retention"** ("How long audit logs are kept"); saving settings shows toast **"Audit retention policy saved."**; a newly-created org already has a seeded retention policy (no need to configure it before it takes effect). | None | **PASS** |
| 3 | Partner trust probation #4549 (W02–W07): #4567, #4588, #4599, #4603, #4602, #4604, #4558 (docs) | web | Platform admin → `/admin/trust-queue` (**"Partner Trust Queue"**); a probation email link → `/admin/trust/act?token=…` (**"Partner trust action"**). | Trust queue lists partners in probation with identity/billing evidence tables ("User name," "Cardholder name," "Names match," "Billing region," etc.); acting on a queued partner shows toast **"Partner suspended"** or generic **"Trust action completed"**, and the standalone action page renders **"Trust action completed"** with a partner summary (name, slug, plan, trust state, signup IP class). | **Platform-admin role required** for the queue; the tokenized `/admin/trust/act` page is reachable without login (magic-link style) but needs a real probation record seeded. | **FAIL** — `/admin/trust-queue` 500s: ambiguous `"id"` in the deviceCount subquery (`routes/admin/trust.ts:169`) |
| 4 | Portal visibility #4562 (W01–W10 + QA/critique fixes): #4626, #4627, #4647, #4740, #4744, #4749, #4750, #4734, #4804, #4815, #4598 (docs), #4836 (QA fix), #4850 (critique) | portal | Log into the customer **Portal** (`apps/portal`, separate origin from `apps/web`) → `/dashboard`, `/security`, `/backups`, `/devices`, `/tickets`, `/reports`. | Dashboard (`PortalLayout title="Dashboard"`) shows tiles: "Security score," "Devices protected," "Patches applied this month," "Last backup verified," "Support," "Action items," "Awaiting you," with lede "Current as of …". Security page (`h1: "Security"`) lists per-device AV/firewall/encryption columns. Backups page lists "Last restore point," "Last test restore," "Open breaches," "Readiness" per device. Devices page adds "Last patch," "Protection," "Encryption," "Last backup," "Warranty ends" columns + CSV export. Tickets show an SLA badge (testid `portal-ticket-sla-badge`) and a "Support usage" panel with "Billed"/"To be billed"/"Covered by contract"/"Pending review." Reports tab (`title="Reports"`) lists/generates/downloads PDF/CSV with an empty state **"No reports yet."** All of it must be **fail-closed per-toggle**: MSP disables a visibility toggle in Settings → Organizations → org → **Customer Portal** tab (`#portal`, `OrgPortalSettingsEditor`) and the corresponding portal page/nav item must disappear for that org's portal users, not just hide client-side. | A seeded portal user account + org with backups/security/ticket data. MSP-side visibility toggles must be exercised from `/settings/organizations/[id]#portal` in the main web app to confirm fail-closed behavior. | **PARTIAL** — all pages + fail-closed 403s ✅; disabled Security/Backups/Dashboard pages render a misleading "couldn't load" shell instead of redirecting like `/reports`; `/portal/devices` gated on `enableSelfService` |
| 5 | Auth rejection error-code hardening: #4739, #4651, #4820, #4782 | web | Login/Account pages: change-password form with wrong current password; account-deletion confirm with wrong password; MFA challenge with a bad code; repeated wrong-password guesses. | Wrong current password on change-password / account-deletion now returns **400 with a stable error code** (not a 401 that could be mistaken for a dead session); a rejected MFA proof is 400 + stable code, not 401; repeated guesses are rate-limited rather than just repeatedly 401ing. | None | **PASS** |
| 6 | #4860 | web | `/login` at a partner that enforces SSO. | Login page collapses the password form behind the SSO button when the partner's `enforceSSO` is true, with an explicit way to reveal the password form if needed. | An organization/partner configured with `enforceSSO: true` in seed data. | **PASS** |
| 7 | #4853 | web | `/login` → trigger an SSO failure (e.g. cancel the IdP flow) right as a token refresh is in flight. | The SSO failure notice now survives (isn't clobbered by) a concurrent refresh-token eviction — the error message stays visible instead of silently disappearing. | Ability to force an SSO failure + concurrent refresh (timing-sensitive; may need to simulate). | **PASS** |
| 8 | #4869 | web | Settings → Organizations list (`/settings/organizations`) → an org mid-archive/offboarding. | Archiving org stays visible in the list for its entire drain window with an **"Archiving…"** badge, then **"Archived"** once complete — instead of disappearing early. | An org actively mid-archive (may need to trigger org offboarding first). | **PASS** — "Archiving…" → "Archived" transition observed end to end |
| 9 | #4877 | web | Security → **PAM** rules tab → a PAM rule pinned to a risk tier no longer offered by the current tier set. | Rule row shows a **"Stale"** badge with a tooltip explaining the pinned tier no longer matches anything selectable. | A PAM rule seeded against a risk tier that's since been removed/renamed. | **PASS** |
| 10 | #4870 | web | Settings → **Extensions** → an extension detail page (`/extensions/[name]`) that ships a dynamically-imported asset. | Extension asset URLs are signed so `import()` can load them; previously-broken dynamic imports now load without a console/network error. | An installed extension with a dynamic-import asset. | **BLOCKED** — no installed extension with a web asset (`installed_extensions` empty, workspace flag off); asset route confirmed bearer-free + no-oracle 404 |
| 11 | #4646 | web | Account → Security → MFA → rotate recovery codes, in a second browser tab holding the same session. | Rotating recovery codes re-issues the caller's own session so the tab that rotated them isn't logged out. | Two tabs signed into the same account. | **PASS** |
| 12 | #4405 | web | Settings → Organizations → org → **Audit Retention**/Activity log view → look at an `agent.command.*` audit row right after dispatch (before completion). | Audit row for a just-dispatched agent command no longer shows "success" prematurely at dispatch time — status reflects actual completion. | A command dispatched to a live/simulated agent, checked immediately after dispatch. | **FAIL** — audit row is `dispatched` ✅ but device Activities still badges it **Success** (`DeviceEventLogViewer.tsx:493` falls back to `resultConfig.success`) |

### Group 5 — Scripts, Custom Fields & Remote Tools (8 rows)

| # | PR(s) | Surface | Click-path | Expected outcome | Prereqs | Status |
|---|---|---|---|---|---|---|
| 1 | Script custom-field write-back: #4917 (api validate), #4692 (api ingest), #4722 (web toggle + panel), #4703 (help tooltip) | web | Scripts → a script → **Parameter Binding** section (bind a parameter to "From a device custom field"); also Settings → **Custom Fields** (`/settings/custom-fields`) → a field's **Field Key** input. | A hover-independent help affordance (`HelpTooltip`, role="tooltip") next to the binding/Field Key explains: "Read: … delivered to the script like a normal parameter… Write: … call the device's custom-fields API with a PATCH request and an API key." Run-detail panel shows the write-back toggle. | None | **PASS** — both `HelpTooltip`s (role=tooltip, click-toggle) carry the exact Read/Write text; `Script write` badge + write-back toggle; run-detail "Custom field writes" panel renders applied/rejected |
| 2 | Script cancellation #3525 (W01-W02): #4788, #4799 | web | Scripts → **Executions** → start/observe a running execution → cancel action; execution history list/detail status badges. | Status badges/maps now cover every execution and run status including **Cancelling** (amber, spinner icon) and **Cancelled** (muted, Ban icon) — previously some statuses fell through to a default. A `script_cancel` command is now registered so cancelling actually asks the agent to stop the run, not just marks it cancelled server-side. | **BLOCKED for the actual stop-in-progress behavior** — confirming the running script process is killed requires a live connected agent; the status-badge/map UI (W01) is checkable without one. | **PARTIAL** — W01 badge maps PASS for all 8 statuses (Stopping… amber+spinner, Cancelled muted+Ban); no cancel affordance exists yet (#4799 is inert by design). `resolveExecutionStatusLabel` is dead code — API never returns `cancelState` |
| 3 | #4906 | web | Scripts → run a script → run picker. | Run-context controls show labels **"Run as"** and **"Target session"** wherever a run context is selectable (previously inconsistent/hidden in places). | None | **PARTIAL** — Test Run + execute modal + detail chip all correct; device **Run Script** picker not converted: `script-session-target` has no label at all, `script-run-as` no accessible name (`ScriptPickerModal.tsx:291-315`) |
| 4 | #4897 | web | Scripts (`/scripts`) → a script row → row action. | Row action titled **"Duplicate script"** clones the script; failure shows toast **"Failed to duplicate script"**. | None | **PASS** — `POST /clone` 201, name gains " (copy)", navigates to the clone |
| 5 | #4895 | web | Scripts → **Executions** → a past execution → click **"Run again"**. | Re-opens the execute flow pre-filled with the same device(s) and parameters from that past execution; on submit, navigates to the new execution and shows a success toast. | A past script execution to re-run from. | **PARTIAL** — pre-fill + submit + in-modal "1 of 1 admitted" all PASS; but no toast and **no navigation to the new execution** — #4886 is wired in `ScriptsPage.tsx:193-195` only, not `ScriptExecutionsPage.tsx` |
| 6 | #4893 | web | Remote → **Terminal** on a cold/first connection where the xterm stylesheet preload fails (e.g. throttled network). | Terminal still mounts and is usable (console warns "Terminal stylesheet failed to load; continuing with an unstyled terminal") instead of crashing the pane. | Throttle network in devtools to force the stylesheet preload to fail/timeout. | **PASS** — CSS now inlined (`<style id="xterm-css">`, 7112 chars, zero xterm `<link>`s), so the throttle repro is unreachable by design; cold mount straight to `#terminal` yields a live pane + Retry |
| 7 | #4641 | web | Remote → **Tools** (`/remote/tools`) → switch between sub-tabs (Terminal/Files/etc.), then reload the page. | The active Remote Tools sub-tab persists in the URL hash across a reload; the terminal pane fills the available viewport instead of leaving dead space. | A connected/simulated remote session. | **PASS** — `#terminal` persists across a full reload; `.xterm` measures 637px in a 900px viewport |
| 8 | #4791 | web | Configuration Policies → a **partner-wide** custom-field policy tab; a dropdown-type custom field's options editor. | Partner-wide custom-field UI is now properly gated (visible/editable only where appropriate); dropdown option entries save with `{label, value}` pairs correctly instead of a malformed contract. | A partner-wide (not org-scoped) configuration policy. | **PASS** — partner-wide dropdown created 201 with `orgId:null` + `{label,value}` choices (DB-verified); "All organizations" badge; edit round-trip preserves choices |

---

### Not browser-verifiable (~190 commits)

api-only, agent-only, mobile-only, and docs/chore commits cannot be exercised through a Playwright browser sweep of `apps/web`/`apps/portal`. Clustered by theme below with sub-PRs and how each IS actually verified.

#### API — tenancy / org-move / org-erasure FK integrity sweep (14 PRs)
#4908, #4898, #4857, #4863, #4879, #4833, #4831, #4821, #4823, #4741, #4736, #4786, #4726, #4642

A long-running sweep making every FK that references a ticket/device/org row behave correctly across org-move, org-merge and org-erasure (sever, tombstone, re-stamp, or cascade, per column). **Verified via:** `apps/api` FK-ON-DELETE contract tests (#4519 "contract-test FK ON DELETE actions against org erasure", #3880 "end-to-end coverage for cascadeDeleteOrg breadth and failure semantics") and the migration-replay guard added in this same sweep (#4896, "preserve current functions after migration replay").

#### API — site/partner-scope RLS & config-policy enforcement (8 PRs)
#4846, #4847, #4848, #4855, #4835, #4803, #4690, #4493

Closes several site-scope and partner-wide RLS escalation gaps (config-policy assignment reads, DR plan mutations, maintenance windows, remote-access launcher partner read, device-activity-feed RLS-promotable predicate). **Verified via:** `apps/api` RLS integration suite (site-scope-coverage.integration.test.ts and friends).

#### API — AI agent session lifecycle & notification correctness (9 PRs)
#4892, #4801, #4796, #4785, #4742, #4697, #4640, #4684, #4406

Session eviction/staleness races (Anthropic + OpenAI providers), MCP tool-name allowlist enforcement, dedup of intent-outcome notifications, auto-demote notify-on-no-run, alert baseline fan-out suppression. **Verified via:** `apps/api/src/services/ai` unit suite (session eviction/staleness tests referenced in each PR's own description).

#### API — patch/backup/DR dispatch & audit-chain reliability (7 PRs)
#4843, #4845, #4849, #4852, #4548, #4400, #4407

Stranded-patch flipping, silent-failure paths in patch dispatch, backup-dispatch retry duplication, bounded nightly audit-chain verification, and the new outbox/anomaly-incident retention/prune job (#4210, backing the `TICKET_OUTBOX_RETENTION_*` / `INTENT_OUTBOX_RETENTION_*` / `METRIC_ANOMALY_INCIDENT_RETENTION_*` env vars above). **Verified via:** patch-dispatch and audit-chain integration tests; the retention job has its own worker-role test suite.

#### API — QuickBooks/accounting internals (1 PR)
#4858 — accounting `captureException` tags dropped by the Sentry allowlist. **Verified via:** Sentry-tag unit test on the accounting error path.

#### API — remote-access / extensions / desktop protocol (3 PRs)
#4403, #4401, #4398 — split remote-access launcher availability from issuance, extension `activeVersion` recorded only after built-in activation succeeds, `desktopWs` input schema gap dropping paste-as-keystrokes on the WS fallback. **Verified via:** `desktopWs` schema tests, remote-access launcher unit tests.

#### API — ops/CI/migration guards (7 PRs)
#4866, #4896, #4878, #4689, #4591, #4409, #4478 — migration guard hardening, non-UTC TZ CI coverage extension, worker `/metrics` wired into Prometheus scrape config, migration-order pre-push guard, `docker-compose.monitoring.yml` composability, guided-setup.sh end-to-end CI run. **Verified via:** the CI jobs these PRs themselves add/fix (self-referential — check the named workflow is green on main).

#### API — miscellaneous correctness & hardening (39 PRs)
#4856, #4899, #4841, #4844, #4861, #4865, #4874, #4890, #4839, #4720, #4829, #4826, #4818, #4799, #4789, #4784, #4790, #4794, #4735, #4725, #4723, #4724, #4649, #4638, #4700, #4637, #4563, #4538, #4515, #4516, #4419, #4411, #4410, #4402, #4395, #4506, #4507, #4738, #2826

Grab-bag of one-off API fixes: mail-transport transaction ordering (#3905), PAM cleanup edge case, config-policy RLS branches, alert-rule template categories, script-dispatch actor-probe escaping, enrollment-key TTL defaults, the v0.109.0 sweep doc itself (#4507), AI-field-provenance stamping (#4738), and the new Partner API `POST /partner-api/v1/enrollment-keys` endpoint (#2826, gated by the `PARTNER_API_ENROLLMENT_KEY_*` env vars above). **Verified via:** each PR's own `apps/api` unit/integration test (named in the commit body); the Partner API endpoint has its own route test suite (not a browser surface — it's a service-to-service API).

#### Mobile app (iOS/Android) — not verifiable without a device/TestFlight build (19 PRs)
#4851 (api-side, mobile pagination), #4780 (Android FCM registration), #4779, #4727, #4696, #4589, #4579, #4568, #4566, #4560, #4541, #4536, #4508, #4534, #4396, #4393, #4494, #4488, #4793 (W02 challenge/verify mobile registration protocol, part of the platform-bound-passkey L4 work gated by `BREEZE_AUTHENTICATOR_ATTESTATION_ENFORCED` above)

Also the mobile-only defects (1–4, 6, 7) inside the **iOS papercuts (#4916)** row already listed in Group 3 above — approval-takeover overlay, duplicate-approval replay guard, per-request toast keying, keyboard dismiss/scroll on ticket screens, and camera-upload error-mapping — need a TestFlight build against a real device, not a browser. **Verified via:** `apps/mobile` vitest suite (each PR names its new test file) plus the next TestFlight build for anything gated on real device behavior (camera upload, Face ID, push notifications).

#### Go agent + native remote-desktop apps (viewer/helper Tauri) — needs a live enrolled agent (16 PRs)
#4914 (Homebrew cleanup), #4876 (#3109 scrub agent logs / helper session ids), #4859 (confirm delivery before advancing upload state), #4862 (macOS code-signing identity churn), #4864 (Caps Lock sync over sync instead of forwarding as a key), #4891 (script parameters on runAs=user helper path), #4840 (secrets.yaml rename retry), #4592 (PAM cleanup for never-provisioned elevation accounts), #4817 (go-m1cpu bump for Apple Silicon), #4781 (extract custom-field markers from script stdout), plus 6 Tauri dependency bumps in `apps/viewer`/`apps/helper` (#4814, #4813, #4812, #4811, #4810, #4809).

**Verified via:** the Go agent's own test suite (`go test ./...`) plus manual exercise against a live enrolled agent for anything touching the update/install/PAM lifecycle; the Tauri dependency bumps are covered by the viewer/helper build + existing e2e smoke tests, not new behavior.

#### Docs / CI / dependency / self-host chores — no application code path (14 PRs)
#4915, #4894, #4889, #4532, #4443, #4635, #4631, #4625, #4632, #4580, #4564, #4539, #4479, plus the portal dev-mode-only infra pair **#4540 + #4563** ("serve the dev module graph under /portal so islands hydrate" / "non-blocking portal dev-mode e2e job") — this last pair only affects `astro dev` hot-reload behavior and does not apply to a docker-compose/production-built stack, so it is not checkable in a seeded release-candidate environment regardless of surface.

**Verified via:** the CI checks each of these touches directly (Dependabot version bumps go green on their own PR checks; the self-host guided-setup and CADDY_LOCAL_CERTS backfill are covered by `ci(selfhost): run guided-setup.sh end to end on a runner` (#4478)); doc-only commits have no test to run — spot-check the rendered doc/CLAUDE.md page instead.

---

### How to use this document

Work top-to-bottom through Groups 1–5 with a seeded local stack (see `worktree-stack` skill) logged in as an org admin (web) and a portal user (portal) plus one platform-admin account for the rows marked as requiring it. Flip each Status to PASS/FAIL/BLOCKED as you go, and file a GitHub issue per FAIL with the row number and PR(s) it maps to. Rows marked BLOCKED in the Prereqs column need either a live agent connection, a mobile TestFlight build, or a real third-party OAuth app (QuickBooks) and should be explicitly called out as un-testable in this pass rather than silently skipped.


## Sweep log (append as you go)

### [Group 1 — Billing, Quotes, Contracts & Accounting] — opus, 2026-09-05T05:04Z

Stack: web http://localhost:33028 built from origin/main `78eae279f`, logged in as `admin@breeze.local` (platform admin, org "Breeze"/Default Organization). Billing tables started **empty** (0 contracts / 0 quotes / 0 invoices / 0 catalog items, 2 seeded devices, both `device_role = unknown`), so most rows were exercised by creating the data through the UI — which doubles as the everyday-workflow checks.

#### [#4636 — Billing entry in Settings sidebar] — PASS
- ✅ `aside` nav dump shows the Settings section as: `/settings/partner` (Partner) → **`/settings/billing` (Billing)** → `/settings/organizations` → `/settings/users` → … The entry is present, correctly placed next to Partner, and renders for a partner-scope platform admin as designed (`Sidebar.tsx`, gated `partnerScopeOnly` + `invoices:write`).
- ✅ `/settings` index page also lists a **Billing** card ("Invoice currency, tax, numbering, terms, and footer d…") → `/settings/billing`.

#### [Everyday workflow — create a quote from scratch] — PASS
- ✅ Billing → Quotes → **New quote** → org "Default Organization", title "QA Sweep Quote — Office refresh" → **Create draft**: toast **"Draft quote created"**, routed to `/billing/quotes/0cfd7751-3b0d-4446-9aca-599b2d9a5b67`.
- ✅ Empty state renders ("No quotes yet" + its own New quote button); workspace shows Editor/Preview/Details tabs, ADD SECTION palette (Heading, Rich text, Image, Pricing table, Contract, Table, Callout), LIVE TOTALS panel, and the send gate "Add at least one item before sending."
- ✅ Added a **Pricing table** block → autosaved ("Saved 11:05 PM"), ghost row appears. Typed "Managed workstation onboarding" @ $125.50, Enter → line persisted (`quote-line-name-237136d2…` = the typed name) and LIVE TOTALS updated to One-time $125.50 / Subtotal $125.50 / Due on acceptance $125.50, with an SR-live line "Totals updated. One-time $125.50, monthly recurring $0.00 …". Autosave, totals, and the aria-live announcement all correct.

#### [#4408 — WebP rejected for quote line/cover images] — PASS
- ✅ Live DOM: `input[type=file][data-testid="quote-line-image-input-<lineId>"]` has `accept="image/png,image/jpeg"` (no `image/webp`).
- ✅ Server boundary: `POST /api/v1/quotes/<id>/images` with a real RIFF/WEBP multipart → **415** `{"error":"WebP images can't be used in quote PDFs — please upload a PNG or JPEG image instead."}`.
- ✅ UI boundary (forced a WebP `File` onto the line image input via DataTransfer, bypassing `accept`): toast **"WebP images can't be used in quote PDFs — please upload a PNG or JPEG image instead."** — the 415 is surfaced, not swallowed.

#### [#4639 — WebP rejected for catalog item images] — PASS
- ✅ Settings → Catalog → created item "QA Managed Workstation" (SKU QA-MW-001, $89.00) — toast **"Item "QA Managed Workstation" created"**.
- ✅ Editor drawer image field: `catalog-form-image-input` has `accept="image/png,image/jpeg"`; help copy reads **"PNG or JPEG, up to 5 MB."** (no WebP).
- ✅ Forced a WebP file onto that input → toast **"WebP images can't be used in quote PDFs — please upload a PNG or JPEG image instead."**; no image attached to the item.

#### [Everyday workflow — create + edit an invoice] — PASS
- ✅ Billing → Invoices → **New invoice** → *Blank invoice* → org → **Create draft**: toast **"Draft invoice created"**, routed to `/billing/invoices/a701416a-…`.
- ✅ Catalog picker found "QA Managed Workstation" by name, showed type/SKU/price; **Add** → toast **"Line added"**, autosave hint "Saved 11:09 PM", summary $89.00, plus the honest nudge "Lines are marked taxable, but no tax rate is set. Set one in Billing settings." and "No billing contact set. Add one in Organization settings."
- ✅ Edited the line (qty 1 → 3, unit price 89 → 95): autosaved on blur, summary → **$285.00**, Detail tab and DB agree (`invoice_lines` qty 3.00 / unit_price 95.00). Editor ↔ Detail totals stayed consistent after a tab round-trip.

#### [#4802 — change-currency dialog for draft quotes/invoices] — PASS
- ✅ Quote: Details tab → **"Change currency"** (`quote-currency-open`) → dialog titled **"Change quote currency"**, description "This draft quote is priced in USD.", currency `<select>` (34 supported currencies), two modes ("Remove all lines" / "Reprice catalog lines" with their hints), a confirm checkbox, and a submit disabled until confirmed.
- ✅ Negative path: choosing *Reprice catalog lines* with one manual line → the server refuses and the reason lands **both** as a toast and inline in the dialog: "1 non-catalog line(s) have no price in the new currency — remove all lines first, or keep the current currency". Dialog stays open, nothing changed.
- ✅ Happy path: *Remove all lines* + EUR → toast **"Quote currency changed to EUR."**, dialog closes, lines cleared, totals re-render as **€0.00**, DB `quotes.currency_code = EUR`.
- ✅ Invoice: draft invoice Detail tab renders the same **"Change currency"** control (`invoice-currency-open`); gating matches the row (draft only).

#### [#4819 — draft values reseeded during render, not from a passive effect] — PASS
- ✅ Regression probe: typed an unsaved edit into the quote **Terms & conditions** field ("… QA-DIRTY-EDIT"), then — while it was still dirty — fired a sibling save that triggers a full quote refetch (cover-page toggle). The in-progress text survived the refetch byte-for-byte (before === after). Under the old passive-effect reseed this is the commit where the edit was silently overwritten.
- ✅ Quote title round-trip: typed a new title, blur → PATCH persisted (`quotes.title`), field kept exactly what was typed after the refetch; no revert, no duplication.
- ✅ Invoice line drafts (qty/price) survived an Editor→Detail→Editor remount and matched the server.

#### [#4397 — failed quiet refetch no longer strands a new quote line] — PASS
- ✅ Forced the exact failure: patched `window.fetch` to 500 only the quote-detail **GET** (`/api/v1/quotes/<id>?orgId=…`), leaving the line **POST** to succeed. Added a manual line ("Firewall appliance") → network log shows `POST …/lines` (2xx) then the forced-failed `GET …/quotes/<id>`, and the UI raised the warning toast **"Line added, but the list could not refresh. Reload the page to see it."**
- ✅ The warning is honest, not defensive: after removing the patch and reloading, the "Firewall appliance" line was there — write landed, list hadn't.
- ✅ Catalog-save half: manual add-line panel with **"Save to catalog"** ticked → toast **"Saved to catalog"**, item created, line appended and LIVE TOTALS moved to €145.00.

#### [#4698 — quote line grid at 1280px with the sidebar open] — PASS
- ✅ Viewport set to exactly **1280×900** with the left nav (256px) and the right LIVE TOTALS rail both open, 2 lines in the pricing table: the table wrapper measures `clientWidth 574 === scrollWidth 574` — **no horizontal scroll**; `document.body.scrollWidth === clientWidth === 1280`.
- ✅ Shipped classes match the fix: table `w-full min-w-[26rem]` (was `36rem`), item column `min-w-[8rem]` (was `12rem`).
- ✅ Screenshot `group1-quote-1280.png` — full ITEM/QTY/UNIT PRICE/TOTAL grid visible alongside the totals rail.
- ⚠️ Paper cut found at the same width (different element — not this PR's grid): see paper cuts below, the **quote title input collapses to ~102px** in the header.

#### [#4634 — approximate-total line after a partner currency reset] — PARTIAL
- ✅ The approximate line renders and converts correctly: with the partner reporting currency **USD** and a €145.00 sent quote, `quotes-signature-approx` shows **"≈ $168.52 approximate · rates as of 2026-09-04"**.
- ✅ After changing the partner reporting currency to **GBP** in Settings → Billing (toast "Billing settings saved") the same line reads **"≈ £124.55 approximate · rates as of 2026-09-04"** — no stale USD total, no "could not load exchange rates" stuck state. Reset back to USD afterwards (state left clean).
- ⚠️ Not fully provable from the browser: the *in-place, never-unmounted* refetch this PR actually fixes needs `resetPartnerCurrencyCache()` to fire in the same JS context as a mounted `ApproximateMoneyLine`. Its only two callers are `stores/auth.ts` (logout) and `PartnerBillingSettings.tsx` (save) — and web nav between `/settings/billing` and `/billing/quotes` is a full document load (Astro islands), so the two never coexist in one mounted tree today. The user-visible outcome is correct; the no-remount path stays covered by `useApproximateTotal.test.tsx` only.

#### [#4816 — superseded-quote read-only state + ticket resolve-note prefill] — PASS
- ✅ Superseded quote (seeded `quotes.status='superseded'` on the QA quote, since reaching it organically needs an acceptance + revision): the workspace renders **read-only and correctly** — status chip "Superseded", **no Editor tab** (only Preview/Details), no "Send proposal", **0 editable inputs/textareas/selects** in `main`, lines and totals rendered as text (€145.00), terms shown read-only. No crash, no empty-state flash.
- ✅ Ticket half: created ticket **T-2026-0001** ("QA sweep — printer offline"; toast "Ticket T-2026-0001 created"), set status → **Resolved**: the resolve form opens with the **"Resolution note (visible to requester)"** textarea plus Cancel / Resolve ticket. Note is empty here because prefill sources from an **AI resolution-note draft** (`ticket-ai-draft-resolution_note`) and this stack has no LLM provider configured — the race #4816 deflakes only exists when such a draft is present, so the deflake itself is a unit-level guarantee. Everything reachable without an LLM behaves correctly.

#### [#3205 W01–W07 — device-set billing (contracts, invoices, quotes)] — PARTIAL (one FAIL inside)
Set-up: `devices.device_role` seeded `workstation` on `e2e-windows.local`, left `unknown` on `e2e-macos.local` (both offline, no live agent needed).

- ✅ **Contract line editor** (`/contracts/<id>`): line types are Flat fee / Per device / **Per device role** / **Per device group** / Per seat / Manual quantity. Choosing *Per device role* reveals an 11-role checkbox picker (`contract-line-role-workstation` … `-nas`) with the honest hint "Device roles (a device is billed when its role is one of these)" and "Pick at least one role.", a **Site** scoper ("optional — scopes the device count"), and an allowance toggle "Include a fixed quantity, then handle extras" which reveals **Included quantity** + **Extras beyond the included quantity** (*Bill extras* / *Flag extras for review*) + **Price per extra unit**.
- ✅ Adding the line → toast **"Line added"**; row renders "Per device role · Workstation · $45.00 · **1 of 1 included**". Editing the price (45 → 49) → toast **"Line updated"**, row and ESTIMATED THIS PERIOD both move to $49.00. Line type is correctly locked on edit ("Line type can't be changed. Remove the line and add a new one.").
- ✅ **Coverage warning** on the contract page: `contract-coverage-warning` reads "1 device(s) on this organization are not billed by any line on this contract: **1 Unknown**", with `contract-coverage-role-link` → `/devices#orgId=…&filtersV2=<base64>` decoding to `{"field":"deviceRole","operator":"equals","value":"unknown"}`. Followed the link: Devices renders the chip "Device Role is unknown", "1 of 2 devices", listing exactly the uncovered macOS device. Deep link works end-to-end.
- ✅ **Activate + generate**: "Contract activated" → **Generate invoice now** → **both** expected toasts fire: "Invoice generated" and **"Invoice generated, but 1 device(s) are not billed by any line on this contract: 1 Unknown"**. Landed on the new draft invoice ($49.00, one line).
- ✅ **Quote device-set lines**: in a quote pricing-table block, set Billing = Monthly (the toggle is correctly gated to recurring lines), tick **"Bill by device set"** → Device set = *Per device role* → Workstation. Created line shows **"Auto quantity · 1 workstations · estimated"** and the type-lock note.
- ✅ **Drift chip + Refresh counts**: flipped the second device to `workstation` in the DB, reloaded the quote → the line shows the drift chip **"count changed: 1 → 2"** and a **"Refresh counts"** button (`quote-line-device-set-drift-…` / `-refresh-…`).
- ✅ **Drift toast**: fires on *send* (not on load — `QuoteActions.tsx:538`). Sent the quote (30 s undo window → **Send now**) → warning toast **"1 line(s) no longer match the live device count: Managed workstation (monthly): 1.00 → 2"**. (The stack has no SMTP, so the companion honest failure toast "Proposal marked Sent, but the email could not be delivered… Use "Copy share link"…" also fired — correct behaviour, not a defect.)
- ✅ **Invoice device appendix** renders on an **issued** invoice: "Devices (1)" → *Billed devices* table `e2e-windows.local · Workstation · Included`.
- ❌ **BUG — the device appendix is unreachable on a DRAFT invoice (it bounces you to the Editor tab).** Repro: open a contract-generated **draft** invoice → **Detail** tab → click **"Devices (N)"** (`invoice-line-devices-toggle-<lineId>`). API actual: no request is made. UI actual: the hash becomes `#detail&devices=<lineId>` and the workspace **switches back to the Editor tab**; the device list never renders (verified twice; `invoice-tab-editor` ends `aria-selected=true`). Cause: `apps/web/src/components/billing/InvoiceWorkspace.tsx:29-34` — `readTab()` exact-matches the *whole* hash against the tab values (`TAB_LABELS.some((t) => t.value === raw)`), so the composed hash `detail&devices=…` matches nothing and falls back to `isDraft ? 'editor' : 'detail'`. `InvoiceLineDevices.writeOpenIds` (`apps/web/src/components/billing/InvoiceLineDevices.tsx:61-67`) deliberately *composes* segments; the workspace reader never learned to parse them. `selectTab` (`InvoiceWorkspace.tsx:117`) has the mirror-image flaw — it assigns `#${next}`, clobbering any `devices=` segment. Contrast proof: issuing the same invoice makes the identical hash work (fallback becomes `detail`) and the table renders — so the defect is exactly the draft branch. Fix is a one-liner in `readTab` (match the first `&`-segment) plus preserving other segments in `selectTab`.

#### [QuickBooks Phase C + D — #4492/#4531/#4800/#4795/#4827] — BLOCKED (no live QBO OAuth app)
- **Prereq missing:** `QBO_CLIENT_ID` / `QBO_CLIENT_SECRET` / `QBO_REDIRECT_URI` are unset on this stack, so no OAuth handshake is possible and no `quickbooks_connections` row can exist. Everything gated on a live connection is untestable here: the **Payment sync** toggle (`QuickbooksIntegration.tsx:571`, rendered only inside the connected `status.*` branch), the **"{n} queued for QuickBooks"** toast, **"Invoice pushed to QuickBooks."**, the reconcile skip-reasons, and the voided-invoice block copy ("This invoice is voided — there is nothing to push to QuickBooks.", `en/billing.json:1842`).
- ✅ What *is* reachable, and behaves correctly: Integrations → **Accounting** → QuickBooks panel renders (`quickbooks-panel`) with `quickbooks-status-disconnected`, the "Inactive" chip, the explainer "Breeze stays your system of record", and a **Connect to QuickBooks** button. Clicking it fails **honestly**: toast **"QuickBooks OAuth is not configured on this instance"** — no broken redirect, no silent no-op.
- ✅ Invoices bulk bar carries `invoices-bulk-action-quickbooks` (**"Push to QuickBooks"**) alongside Issue / Void / Delete drafts; selecting the issued invoice and pushing raises the same honest **"QuickBooks OAuth is not configured on this instance"** toast rather than pretending to queue.
- ✅ Fail-closed on the invoice detail: with no connection, the whole QuickBooks card is absent (no dead "Push" button, no stale sync status).
- Not checked (needs the OAuth app): bulk-limit warning "Select up to {{limit}} at a time." — copy exists at `en/billing.json:1899`, but this stack only has 2 invoices so the limit can't be crossed anyway.

#### [Everyday workflow — open a contract; allowance/overage math] — PASS
- ✅ After the second device also became a `workstation`, the contract detail recomputed live and honestly: coverage warning **replaced** by "Every device on this organization is billed by a line on this contract."; the line QTY column reads **"1 included · 1 over (billed)"**; a plain-English note explains "Managed workstation (per device) — 1 over 1 included, billed at the overage rate"; EST./PERIOD = **$99.00** (1 × $49 included + 1 × $50 overage). Billing history lists the generated period with a "View invoice" link.
- ✅ Billing → Contracts list agrees: "Est. monthly recurring $99.00 · 1 active contract", row shows Cadence Monthly / Next bill 10/5/2026 / Est. per period $99.00. Tabs present: Contracts · Templates · Documents · **Currency mismatches**.

#### Paper cuts (Group 1)

1. **Quote title is clipped to ~7 characters at 1280px.** On `/billing/quotes/<id>` the header title input measures `clientWidth 102px` against `scrollWidth 208px` at a 1280-wide viewport ("QA Swee…"), while at 1600px it gets 420px and fits. The header flex row (title + status chip + customer + Show cost & margin + Send proposal + Download PDF + overflow) starves the title first. Screenshot: `<repo root>/group1-quote-1280.png` (gitignored, kept locally). 1280 is a very common laptop width.
2. **The change-currency dialog lets you pick a mode the data forbids.** With a manual line present, *"Reprice catalog lines"* is selectable and submittable; only the server refuses ("1 non-catalog line(s) have no price in the new currency — remove all lines first…"). The dialog already knows the rule (its own hint says "Required when any line is manual or a bundle") — the radio should be disabled with that reason attached, rather than costing a round-trip.
3. **"Change currency" hides on the Details tab.** The currency is shown on every total in the Editor, but the only way to change it is to switch tabs to Details and scroll to the totals card. Nothing in the Editor points there.
4. **`Included quantity` is used as three different labels in one fieldset** (`QuoteBlockCard.tsx:930-933`, mirrored at `QuoteLineRows.tsx:385-391`): the sr-only legend, the *enable-the-allowance* checkbox, and the number input all render the same string, so the visible form reads "Included quantity ☐ / Included quantity [ ]" and a screen reader hears it three times. The contract editor gets this right — its checkbox says "Include a fixed quantity, then handle extras".
5. **Role `unknown` renders as an empty ROLE cell on Devices**, while the contract coverage warning names the same devices "1 Unknown". Following the deep link therefore lands on a filter chip "Device Role is unknown" over a row whose Role column is blank.
6. **Generate invoice fires two toasts for one click** ("Invoice generated" immediately followed by "Invoice generated, but 1 device(s) are not billed…"). The second supersedes the first; stacking both makes the warning read like a second, separate event.
7. **The Invoices list drops its filter toolbar in the empty state** (only the empty card renders), while Quotes keeps search + status filter visible. Minor inconsistency between two sibling list pages.
8. **"Connect to QuickBooks" is offered on an instance that cannot connect.** With `QBO_CLIENT_ID` unset the button is fully enabled and the failure only arrives as a toast after the click. A disabled button with "Not configured on this instance — set QBO_CLIENT_ID…" inline would be the honest surface for a self-hoster.

**Group 1 totals: PASS 8 · PARTIAL 2 · FAIL 0 rows (one ❌ defect inside row 1) · BLOCKED 1 · N/A 0.** State left on the stack: catalog item "QA Managed Workstation"; quotes Q-2026-0001 (superseded — seeded) and Q-2026-0002 (sent); invoices INV-2026-0001 (issued, generated from the contract) plus one $285 draft; contract "QA Managed Services Agreement" (active); ticket T-2026-0001; both seeded devices now `device_role = workstation`; partner reporting currency restored to USD.

### [Group 2 — Devices, Patching & Reboot] — opus, 2026-09-05T05:40Z

Stack: web `http://localhost:33028` built from origin/main `78eae279f`, logged in as `admin@breeze.local` (platform admin, org "Default Organization"). Seed had 2 offline devices (`e2e-windows.local`, `e2e-macos.local`), 3 patches, **0 configuration policies**. Rows needing data were seeded either through the UI (config policy + assignment — doubles as the everyday-workflow check) or with `psql` (10,050 audit rows, one `severity IS NULL` patch, one scheduled-restart device row).

#### [Row 1 — Reboot deferral #3207 W1/W5 (#4686 #4699 #4728 #4733 #4737)] — PARTIAL
- ✅ **W1 policy UI.** `/configuration-policies/<id>` → **Patches** tab → toggling **"Let users postpone the restart"** (`patch-reboot-allow-deferral-toggle`) reveals exactly the two promised fields: **Maximum postponements** (`patch-reboot-max-deferrals`, default 3, min 1 / max 10) and **Postpone by (minutes)** (`patch-reboot-deferral-minutes`, default 60, min 5 / max 1440). Save → toast `Patch settings saved`; DB confirms `config_policy_feature_links.inline_settings` = `{… "rebootAllowDeferral": true, "rebootMaxDeferrals": 3, "rebootDeferralMinutes": 60 …}`. Copy is accurate about the helper requirement and the Linux terminal fallback.
- ❌ **BUG: W5 `RebootScheduledBadge` never renders on the device page — the five reboot fields are dropped by the detail page's whitelist transform.** Seeded `devices` row: `reboot_scheduled_at = now()+2h`, `reboot_deadline = now()+8h`, `reboot_deferrals_used = 1`, `reboot_max_deferrals = 3`, `reboot_source = 'patch_job'`, `pending_reboot = true`.
  - **API actual (`GET /api/v1/devices/e65460f3…` → 200):** `{"pendingReboot":true,"rebootScheduledAt":"2026-09-05T07:35:12.979Z","rebootDeadline":"2026-09-05T13:35:12.979Z","rebootSource":"patch_job","rebootDeferralsUsed":1,"rebootMaxDeferrals":3}` — the API side is correct.
  - **UI actual:** header renders only `Reboot pending` (`data-testid="device-pending-reboot-badge"`). No restart-scheduled badge, no deferral counter; `document.body.innerText` contains no "Restart"/"postpone" anywhere on the page.
  - **Root cause (read, not inferred):** `apps/web/src/components/devices/DeviceDetailPage.tsx:84-130` builds `transformedDevice` as an *explicit field whitelist* and copies `pendingReboot` but none of `rebootScheduledAt` / `rebootDeadline` / `rebootSource` / `rebootDeferralsUsed` / `rebootMaxDeferrals` (`grep -n 'rebootScheduledAt' DeviceDetailPage.tsx` → zero hits). `DeviceDetails.tsx:622-628` therefore always passes `undefined`, and `RebootScheduledBadge.tsx:37` (`if (!rebootScheduledAt) return null`) bails. This is the exact "#800/#1273/#2138 dropped-field mode" that file's own comment at line 108-112 warns about. `RebootScheduledBadge` has no other consumer (`DeviceList.tsx` only declares the types), so the badge is unreachable in the product.
  - Fix is one-line-per-field in that transform + a DeviceDetailPage test asserting the five fields survive it.
- 🚫 **BLOCKED (as the row predicted):** W2–W4, the actual Windows/macOS/Linux end-user restart prompt with the Postpone button, needs a live enrolled agent to receive the reboot command and a real user session for the helper. Not reachable from a seeded stack.

#### [Row 2 — Unrated patch severity (#4721 web, #4787 api)] — PASS
- Seeded `patches` row with `severity = NULL` (`QA Unrated Update (no CVSS rating)`) + a `device_patches` link.
- ✅ `/patches#patches` severity filter offers `All Severities · Critical · Important · Moderate · Low · **Unrated**`; selecting it (value `unrated`) narrows the table to `1 of 4 patches` and returns exactly the null-severity row.
- ✅ The row renders an **`Unrated`** badge with the sub-label **"Will not auto-approve"** — not coerced to "Low".
- ✅ Sorts last after every rated level. Severity-sorted ascending: `Critical → Important → Moderate → Unrated`; reversed: `Unrated → Moderate → Important → Critical`. (No `low` patch existed to seed between Moderate and Unrated, but the ordering is unambiguous — Unrated is below the lowest rated value in both directions.)

#### [Row 3 — Windows-installer hardening (#4730 TLS 1.2, #4688 OS floor)] — PASS
- Devices → **Add Device** → **CLI Commands** → **Windows**. Generated one-liner (verbatim from the `<pre>`), both hardening clauses present and ordered before the download:
  - `$osv=[System.Environment]::OSVersion.Version; if($osv.Major -lt 10){throw "Breeze: Windows 10 or Windows Server 2016 or later is required (detected $($osv.Major).$($osv.Minor))"};`
  - `[Net.ServicePointManager]::SecurityProtocol = [Net.ServicePointManager]::SecurityProtocol -bor [Net.SecurityProtocolType]::Tls12;`
- ✅ Also still carries the MZ-header check ("a captive portal or web filter may be intercepting this network") and per-step `$LASTEXITCODE` throws. `$ErrorActionPreference='Stop'` is first.

#### [Row 4 — Activities-tab count cap (#4842)] — PASS
- Seeded 10,050 `audit_logs` rows against `e2e-macos.local` (resource arm, `actor_type='user'`).
- ✅ **API actual:** `GET /devices/42fc7de0…/events?page=1&limit=50&withTotal=true` → 200, `"pagination":{"page":1,"limit":50,"total":10000,"totalIsLowerBound":true}` — clamped at `FEED_TOTAL_CAP`, flag set.
- ✅ **UI actual:** the Activities tab header renders **`(10,000+ total)`** rather than a falsely precise number, and the tab paints immediately (no timeout).
- ✅ The device with a small history still shows an exact count (`(1 total)`), so the below-cap path is unchanged.

#### [Row 7 — device Patches tab policy deep link (#4701)] — PASS
- ✅ **No-policy branch:** with no configuration policy assigned, the Patch Controls card shows only `Manage Patches` → `/patches`. No stray label, no crash.
- ✅ **With-policy branch:** created policy **"QA Standard Patch Ring"** (org-scoped) and assigned it at Organization level; the same card now reads **"Managed by policy: QA Standard Patch Ring"** with the name linking to `/configuration-policies/17ba2ce3-f1bc-4603-8d83-eee73cfa3489`, alongside the unchanged `Manage Patches` → `/patches`.
- The effective-config fetch is best-effort (`DevicePatchStatusTab.tsx:661`, comment documents the fail-absent behaviour); with the fetch succeeding here both branches render as specified.

#### [Row 8 — org name in device breadcrumb is a link (#4695)] — PASS
- ✅ `/devices/<id>` breadcrumb renders `Default Organization` as `<a href="/settings/organizations/e26711c9-ce4c-4ab8-9916-8a011350d36d">`; clicking it lands on **Organization Settings → Default Organization** (200, correct org). Not static text.

#### [Row 5 — Connect Desktop no longer revokes every live session (#4832)] — PASS
- Prereq satisfied by seeding: flipped `e2e-windows.local` to `status='online'` for the duration of this check (restored to `offline` afterwards), then created **two concurrent live sessions** — one `desktop` via the button and one `terminal` seeded directly into `remote_sessions` (`status='active'`).
- ✅ **Client never sweeps.** Full `window.fetch` capture across two Connect Desktop clicks: `POST /remote/sessions` → `POST /remote/sessions/<id>/desktop-connect-code` → `GET /remote/sessions/<id>` (poll). **`DELETE /remote/sessions/stale` is never called** (`staleCalled: false`), which is exactly what #4832 removed.
- ✅ **Only the superseded session dies.** After the second Connect Desktop, `remote_sessions` reads: prior `desktop` `1c6af642…` → `disconnected` (server-side supersede, scoped to device+type as designed); concurrent `terminal` `5db13bec…` → still **`active`**, never touched; new `desktop` `a53cc3c4…` → `pending`. Under the old unscoped sweep the terminal row would have been marked disconnected and the WS closed `4003 Session revoked`.
- Note: the WebRTC handshake itself cannot complete without a live agent, but the revocation regression lives entirely in the session-creation request sequence, which is fully exercised here.

#### [Row 6 — advanced filter fails closed with a toast (#4783)] — PASS
- Baseline first: `/devices` → **Advanced** → condition `Hostname contains e2e` → **Apply** → `2 of 2 devices`, both rows render (proves the happy path and that the control is live).
- Then forced the failure by intercepting `POST /api/v1/filters/preview` → **500** `{"error":"QA forced failure"}` (4 intercepted calls) and re-applying with `Hostname contains e2e-win`.
- ✅ **Toast actual (verbatim):** `Advanced filter failed to load — showing no devices. Edit or clear the filter to try again.` — byte-identical to `devices.json:1809`.
- ✅ **Fails closed:** counter drops to `0 of 2 devices` and the table renders `No devices found. Try adjusting your search or filters.` — no stale/unfiltered list.
- ✅ Bonus, beyond the row: the persistent inline pill `device-filter-error` also renders **"Filter failed to load — showing no devices"**, so the state survives the toast's 5 s auto-dismiss.

#### [Row 9 — permanent-delete surfaces the agent-unreachable warning (#4394)] — PASS
- Prereq satisfied by seeding a throwaway device (`qa-throwaway2.local`, `status='decommissioned'`, agent never connected) — the row's "agent can't be reached" case, which per #4368 is the *common* case since decommission force-closes the agent WS.
- ✅ **Toast actual (verbatim, `type: warning`, 10 s):** `qa-throwaway2.local was deleted, but The agent could not be reached for remote uninstall. You may need to manually remove it from the endpoint.` — the API's `warning` is surfaced instead of the old flat green success.
- ✅ **API actual:** `DELETE /devices/<id>/permanent` → 200; audit row `device.permanent_delete` `result=success` `details={"uninstallCommandSent": false}`. Device row gone from `devices`.
- ⚠️ The click-path in this row's inventory entry says "→ confirm", but there is **no confirmation dialog**: `DevicesPage.tsx:940-972` fires an `undo`-type toast and executes 5 s later via `setTimeout`. That is deliberate (see the code comment), but see paper cuts 2 and 3 — the undo toast omits the hostname, so an irreversible fleet-wide-visible action is gated by a 5-second window that never names its target.

#### [Everyday-workflow checks folded in] — PASS
1. **Open a device detail** — `/devices/<id>` paints Overview with CPU/RAM/uptime/last-seen tiles, billing card, activity rail, and 11 tabs + More. One console 404 on `GET /api/v1/reliability/<id>` is handled gracefully ("No reliability snapshot available yet").
2. **Create a patch policy end-to-end** — `/configuration-policies/new` → *Configure New* → name + **A specific organization** scope → **Create Policy** navigates straight to the new policy; **Patches** tab configured (ring, sources, schedule, reboot policy, deferral) and saved with toast `Patch settings saved`; **Assignments** tab → Organization / Default Organization → **Assign** creates the assignment row. Verified end-to-end by the device's Patches tab then naming that policy (Row 7).
3. **Set maintenance mode on a device** — not on the device page or the row action menu; it lives under **Bulk Actions → Enable Maintenance** after ticking a row checkbox. Works: toast `1 devices put into maintenance mode`, `devices.status` → `maintenance` in the DB, list status chip switches, and **Disable Maintenance** reverses it (`1 devices taken out of maintenance mode`).

**Paper cuts (Group 2)**

1. **`RebootScheduledBadge` is dead code in the shipped product** (the Row 1 ❌ above). Worth a guard beyond the fix: `DeviceDetailPage.tsx`'s whitelist transform has now silently dropped fields four times (#800, #1273, #2138, and this one) and its own comment says so. A test asserting the transform preserves every field `DeviceDetails` reads would end the pattern; a `Pick<>`-driven mapping would end it structurally.
2. **The permanent-delete undo toast never names the device.** `t('devicesPage.toasts.permanentDeleting', { hostname })` is called with a `hostname` interpolation but `en/devices.json:1842` is the constant string **`"Permanent Deleting"`** — the placeholder is dropped. On a fleet list, the only 5-second chance to cancel an irreversible delete shows no indication of *which* device is going away. (Same key in all 8 locales.)
3. **"Permanent Deleting" / "Permanently Deleted" are not sentences.** Every neighbouring toast is a phrase about a named device ("Removing", "{{hostname}} has been restored"); these two read like enum values leaked into the UI.
4. **The warning toast splices a capitalised API sentence mid-clause**: `"{{hostname}} was deleted, but {{warning}}"` + the API's `"The agent could not be reached…"` renders as *"…was deleted, but The agent could not be reached…"*. Lower-casing the API string or rewording the wrapper to end at a full stop would fix it; the PR body already notes the API text isn't localised.
5. **Bulk maintenance toasts don't pluralise**: `1 devices put into maintenance mode` / `1 devices taken out of maintenance mode`.
6. **Assigning a configuration policy gives no confirmation.** `AssignmentsTab.tsx` has no `runAction` and no `showToast` (`grep -n 'runAction\|showToast' AssignmentsTab.tsx` → no hits) and isn't in `runActionAllowlist.ts`; the only signal that the POST worked is a row quietly appearing in *Current Assignments*. Same for **Create Policy**, which relies on the navigation as its confirmation.
7. **Maintenance mode is only reachable through Bulk Actions.** Putting one box into maintenance before a reboot is a per-device act, but the device detail page (Power menu, Settings modal) and the row action menu offer no way to do it — you must tick the row's checkbox and open a bulk menu.
8. **Enrollment secret is rendered in cleartext in the CLI install command** with a one-click Copy and no reveal/mask affordance (Add Device → CLI Commands). Correct for the workflow, but it means a screen-share during onboarding leaks a working enrollment secret.

**Group 2 totals: PASS 7 · PARTIAL 1 · FAIL 0 rows (one ❌ defect inside Row 1) · BLOCKED 0 rows outright (Row 1's W2–W4 agent-side prompt is the only blocked sub-item) · N/A 0.** State left on the stack: config policy **"QA Standard Patch Ring"** (org-scoped, patch feature link with deferral enabled) assigned at Organization level; patch **"QA Unrated Update (no CVSS rating)"** (`severity NULL`) + its `device_patches` link on `e2e-windows.local`; **10,050 seeded `audit_logs` rows** on `e2e-macos.local` (kept — they are what makes Row 4 reproducible; they are `actor_type='user'`, `action='device.patch.install'`, `actor_email='qa-seed@breeze.local'`); three `remote_sessions` rows all left `disconnected`; both seeded devices restored to `status='offline'` with all `reboot_*` columns cleared; both throwaway devices gone (one permanently deleted through the UI, which is what Row 9 tested).

### [Group 3 — Tickets, AI Agents & Workspace] — opus, 2026-09-05T06:05Z

Stack: web `http://localhost:33028` built from `origin/main` `78eae279f`, logged in as `admin@breeze.local` (platform admin, partner scope, "Default Organization"). AI agent tables started **empty** (0 agents / 0 runs / 0 impact rows / 0 tickets), so the AI-agent rows were exercised by creating the data through the UI — which doubles as the everyday-workflow checks. Seeded by hand where a DB row was the only way to reach a state (graduation evidence, impact rollup); every seed is listed at the end of this section.

> **Harness note that changed two readings.** A concurrent fix agent was hot-reloading `apps/web`, so Vite HMR full-reloads landed *between* MCP tool calls and reset React state. Two apparent defects (the partner-wide owner-scope radio "not selecting"; the "kind" dropdown "not offering triage") were measurement artifacts of that, and both are **correct** when click-and-read happen inside a single `browser_evaluate`. Recorded here so the next sweep does not re-file them.

#### [#4187 AI agents phase 2 — graduation & impact (#4490, #4533, #4565, #4486, #4504, #4581, #4595)] — PASS

**Graduation panel** (`/settings/ai-agents` → Edit an agent):
- Section **"Graduation"** renders with the stated subtitle ("What this agent has earned over the last 30 days, operation by operation…"), testid `ai-agent-graduation-panel`.
- **Fail-closed empty states are distinct and correct.** With no partner-wide baseline of that kind, `GET /ai/agents/graduation?orgId=…&kind=triage` answers **404** and the panel renders `ai-agent-graduation-no-baseline` — *"No baseline agent yet · Graduation tracking starts once a partner-wide baseline agent of this kind exists…"* — not a generic error with a dead retry button (`AiAgentGraduationPanel.tsx:172-186` documents exactly this). After creating a partner-wide baseline the same call answers 200 and the panel switches to `ai-agent-graduation-empty` ("No evidence yet"). Three states, three different messages: verified.
- With evidence seeded (24 `verified` rows, first 20 days old, partner ceiling `manage_services:restart`), the table renders **OPERATION / STATE / VERIFIED / FAILED / RECURRED / FIRST VERIFIED / WHY NOT YET**, row state **Eligible**, count **"24 of 20"**, and an **enabled Promote** button (`ai-agent-graduation-promote-manage_services:restart`).
- **Promote** → confirm dialog *"Request pre-authorization — This asks a second approver to pre-authorize `manage_services:restart` for this organization's agent. It applies to future runs only and never authorizes a request that is already waiting for a decision."* → toast **"Pre-authorization requested. A second approver has to approve it before this operation runs unattended, and it applies to future runs only."**, and a **"Track this request in the approvals inbox"** link appears in the panel.
- ⚠️ **Revoke button not reachable with one account** — a promoted row only exists after a *second* approver approves the four-eyes request. Correct fail-closed behaviour, not a defect; the revoke-with-reason path is the only sub-item of this row left unexercised.

**Impact page** (`/ai-agents/impact`): all eight tiles present and correct once the rollup has rows — `ai-impact-tile-alerts-judged` (72), `-noise-flagged` (19), `-tickets-triaged` (29), `-drafts-sent` (15), `-fixes-executed` (12), `-est-seconds-saved` (10.2 hours), `-llm-cents` ($21.24), `-promote-eligible` (0). **Export PDF** downloads a real file (`ai_agent_impact-report-2026-09-05.pdf`). Empty-window behaviour is also correct: with no outcomes the tiles and both Export PDF copies are hidden behind the "No AI outcomes in this window yet" empty state, matching `ImpactPage.test.tsx:329`.

#### [#4388 AI budget alerts W01–W04 (#4487, #4496, #4517, #4529, #4805)] — PARTIAL

- **#4805 render-seed fix verified against a non-default ladder.** Saved `45, 75, 92` to the org (`ai_budgets.alert_threshold_pcts = {45,75,92}` confirmed in Postgres), then reloaded and sampled `ai-budget-thresholds-input.value` every 20 ms for 3.2 s: **exactly one distinct value ever observed, `"45, 75, 92"`** — no flash of the `50, 80, 95` default. This is the real proof; sampling with the default ladder stored would have been vacuous, since the seeded value and the placeholder are the same string.
- Fired-rung line renders (`ai-budget-fired-rungs`: *"80% monthly alert sent 9/4/2026"*).
- **Partner-level threshold fields** (Settings → Partner → **AI Budgets**, `#ai-budgets`): after ticking "Enable AI budget enforcement", all five numeric fields plus `partner-ai-budget-thresholds-input` render with placeholder **"Not set, orgs configure individually"** when unset. Verified.
- ⚠️ **"Breeze AI credits remaining" tile NOT rendered** — `AiUsagePage.tsx:261` gates it on `usage?.credits`, which this self-hosted stack has no credit ledger to populate. Expected conditional, not a defect, but the label itself is unexercised here. **This is why the row is PARTIAL rather than PASS.**
- Saving the budget shows an **inline green "Saved successfully"** (3 s), not a toast: `handleSaveBudget` (`AiUsagePage.tsx:164-200`) is deliberately not `runAction`-wrapped and carries its own confirmation. Measured, then confirmed against source — not a silent mutation. (It is also not listed in `runActionAllowlist.ts`; noted as a paper cut, not a sweep finding.)

#### [#4667, #4729, #4822 — AI agents UI critique passes] — PASS

- **Impact refresh:** clicked `ai-impact-refresh` and sampled its `aria-label` every 200 ms — observed the full sequence **`Refresh` → `Rebuilding…` → `Refresh`**, with toasts **"Impact rebuild queued."** then **"The impact report has been rebuilt."** Freshness line moved from *"Not built yet — first rebuild runs nightly"* to *"Data through 2026-09-04 (UTC) · rebuilt 9/4/2026, 11:57:36 PM"*.
- **"More actions" menu:** `ai-impact-overflow-menu` is a `<summary aria-label="More actions">` inside a `<details>`; opens to Edit weights (+ Export PDF once the window has outcomes).
- **Six effective weights in an accessible disclosure, not a hover tooltip:** `ai-impact-est-seconds-saved-disclosure` is a real `<details>` with a keyboard-reachable `<summary>` (`tabIndex 0`, "How is this estimated?"), listing all six weights — *Alert judged 1.5 min · Flagged as noise 4 min · Ticket triaged 6 min · Draft sent 5 min · Fix executed 15 min · Weekly report delivered 30 min*. **Zero `title=` attributes anywhere in the tile** (asserted directly), so the hover-only tooltip is genuinely gone.
- **Disable confirm dialog:** titled **"Disable QA Partner Baseline Triage?"** with the full consequence copy ("Future runs stop, attached schedules stop firing, and organization overrides of this agent stop applying…"). Toast **"Agent disabled"**.
- **All-agents-disabled banner:** after disabling all three agents, `ai-agents-all-disabled` renders **"All agents are disabled · Nothing is running right now."** with a **"Re-enable an agent"** CTA, and the Disabled section auto-expands — correctly *not* the "No agents yet" first-run state.
- **Re-enable:** toast **"Agent re-enabled"**, and the restored row carries the inline note *"Restored, switched off. Review its policy, then turn it on."*
- Runs list (`/ai-agents/runs`) renders its own empty state (`runs-list-empty`, "No runs yet."). No runs exist (no LLM key, no live agents), so the mobile runs-list and policy-key label corrections could only be checked structurally.

#### [#4704 — AI kill switch] — PASS

- `/admin/ai-kill-switch` titled **"AI Kill Switch"**, testids `ai-kill-switch-{refresh,status-badge,epoch,updated-by,updated-at,last-reason,toggle}`.
- **Kill:** confirm dialog *"Kill AI actions platform-wide?"* with a **required** reason field, placeholder exactly **"Describe the incident or reason for this change…"** (`ai-kill-switch-confirm-reason`) → toast **"AI actions killed platform-wide."**; badge → "AI actions killed", **epoch 0 → 1**, reason persisted to the panel.
- **Restore:** dialog *"Restore AI actions?"* (also reason-required) → toast **"AI actions restored platform-wide."**; badge → "AI actions active", **epoch → 2**. Left **active**.
- ⚠️ Non-platform-admin "Platform-admin access required" copy not exercised — only one account on this stack.


#### [#4691 — ticket-triage skip reasons + drifted-card deselect] — PARTIAL

- ⚠️ **Inventory click-path correction.** The row says "Tickets → AI triage batch view". **No such view exists.** Tickets → **Review queue** (`tickets-tab-review`) is the *inbound-email quarantine* queue ("Quarantined (unknown sender) and failed inbound emails"), unrelated to AI triage. #4691's actual web surfaces are `components/aiAgents/RunDetailPage.tsx` and `components/approvals/ApprovalsInbox.tsx` (confirmed against `gh pr view 4691 --json files`).
- ✅ **Skip reasons verified on the run detail page.** Seeded a `profile='triage'` run whose `outcome.ticketTriageSkipped` names three slots, and the page renders a **"NOT APPLIED"** block (`ai-agent-run-triage-skipped`) with one addressable row per slot:
  - *Field updates: Confidence was below the threshold for an automatic write.* (`…-skipped-fields`)
  - *Device link: The ticket already has a linked device.* (`…-skipped-link`)
  - *Resolution-note draft: No resolution note was proposed.* (`…-skipped-draft-resolution`)
  The proposal itself still renders in full alongside it (summary, proposed fields with confidence, device match, draft reply, notes) — the card is **not** silently dropped, which is the fix.
- ⚠️ **Drifted-card auto-deselect not exercised — and effectively unreachable by design.** It fires only when the server answers `batch_not_homogeneous` 422 and names `offending` ids. Since #4868 made client and server share ONE grouping key (`approvalBatchGroupKey` in `@breeze/shared`), the client can no longer offer a batch the server would refuse, so the 422 needs a genuine race (a row's intent changing between render and decide). Not producible on a stack with no live agents. `buildSections`'s `driftedIds` branch is covered by `approvalGrouping.test.ts`.

#### [#4683 — AI draft action lock scoped to its own draft id] — PASS

Seeded two active `ticket_drafts` on ticket **T-2026-0002** (one `reply`, one `resolution_note` — the per-kind unique index means that is the only way to get two cards on one ticket). Wrapped `window.fetch` to hold the draft mutation open for 2.5 s so the in-flight window was observable, then clicked **Discard** on the *reply* card and sampled both cards:

| | before | during the reply discard | after |
|---|---|---|---|
| reply Discard | enabled | **disabled, label "Saving…"** | (card gone — discarded) |
| reply Send as me | enabled | **disabled** | — |
| **resolution_note Discard** | enabled | **still ENABLED** | still enabled |

Exactly the #4683 contract: `sendingDraftId === draft.id` / `discardingDraftId === draft.id` (`TicketWorkbench.tsx:1350-1367`) locks only the acting draft. Discard persisted (`ticket_drafts.state = 'discarded'` for that row only).

#### [#4702 — rename AI Workspace chat tabs] — PASS

`/workspace` is the **AI Assistant workspace** (multi-tab AI conversations), *not* the `ee/workspace` extension — so it is reachable with `BREEZE_WORKSPACE_ENABLED=false` and this row is **not** blocked. New Conversation → the tab label carries `title="Double-click to rename"`; double-clicking swaps it for `workspace-tab-rename-input` with `aria-label="Rename chat"` pre-filled with the current name; typing + Enter commits (tab renamed to "QA sweep — VPN investigation") and leaves edit mode.

#### [#4687 — shadow-mode ceiling hint, inbox link collapsing, BATCH_MAX guard] — PARTIAL

- ✅ **Ceiling hint on a shadow-mode partner agent:** the "UNATTENDED POLICY AUTHORIZATION" fieldset (`ai-agent-policy-decide`) collapses to a `<details>` summary **"Ceiling for organizations — 0 keys"** with `ai-agent-supervised-keys-ceiling-hint`: *"Partner-level keys are a ceiling, not a grant. Each organization's agent must be granted the key separately — through graduation or by editing that organization's agent."* Rendered exactly when `ownerScope === 'partner' && mode !== 'act'`.
- ✅ **Related and worth recording:** checking a key while the agent is in shadow mode raises a live `role="status"` note (`ai-agent-act-keys-cleared`) — *"Act-mode permissions are kept but won't be saved unless this agent returns to Act mode."* Verified against Postgres: the save really does write `supervisedActionKeys: []` (`AiAgentForm.tsx:699`), and the warning is what stops that being silent. Not a defect.
- ✅ **Inbox collapsing verified** — see #4868 below; two supervised siblings collapse under one group header instead of repeating the org/label per card.
- ⚠️ **`APPROVAL_BATCH_MAX` guard not exercised in the browser.** The cap is 50 (`packages/shared/src/constants/index.ts:154`) and the client check is `ApprovalsInbox.tsx:752`; the code's own comment says it is "unreachable today at the current group sizes". Seeding 51 homogeneous approvals to prove a guarded-off state was not a proportionate use of the sweep. Verified by source + shared constant only.

#### [#4705 — alert verdict badge shows who gave feedback] — PASS

Seeded an `ai_alert_verdicts` row (`transient_self_healed`, 0.82) on the *E2E fixture: high CPU* alert, then clicked the thumbs-up in the Alerts list. Toast **"Thanks for the feedback"**; `ai_alert_verdicts.feedback='up'`, `feedback_by=<admin uuid>` in Postgres. After reload the badge reads **"Self-healed · 82% confidence · Feedback by Breeze Admin"** with the name in its own visible element (`alert-verdict-feedback-by`) and `aria-pressed="true"` on the thumbs-up — **no hover required**. The deleted-user path (`feedbackByName` null ⇒ label omitted) is source-verified at `AlertVerdictBadge.tsx:95` but has no deleted user on this stack to exercise.

#### [#4868 — approvals batch-grouping rule shared via @breeze/shared] — PASS

Seeded two **supervised, AI-agent-originated** intents sharing `(orgId, manage_ai_agents, authorize_supervised_key)` alongside the one real **four-eyes** promote request, and the inbox groups them exactly as specified:

- Group header `approval-group-<orgId>--manage_ai_agents--authorize_supervised_key` reading **"2 similar requests · manage_ai_agents:authorize_supervised_key"** with **Decline all (2)** / **Approve all (2)**.
- The two members render **"Proposed by Breeze (AI agent)"** with `approval-agent-badge-*`; the four-eyes row renders **"Requested by Breeze"** and stays a standalone card — correct, `isGroupable` requires `origin === 'ai_agent' && approvalScope === 'supervised'` (`approvalGrouping.ts:80-94`), so a four-eyes promote is deliberately never batchable.
- **Decline all (2)** → group deny form *"This declines 2 requests for Default Organization."* with an optional reason (live 0/500 counter) → one ceremony denied both, toast **"Action denied"**, inbox dropped to 1. No regression from the refactor.

#### [#4875 — SweepProposalReason promoted to @breeze/shared] — PASS

Seeded a `profile='sweep'` run with two findings and two `sweepProposals`. Both the table (`ai-agent-run-sweep-findings`) and the mobile card list (`ai-agent-run-sweep-finding-cards`) render, and the reason copy resolves correctly:

- `disposition: not_created`, `reason: not_allowlisted` → **"Tool not allowed for this agent"** + a **"Review agent permissions"** link (`ai-agent-run-sweep-permissions-link-0`).
- `disposition: intent_created` → **"Approval requested"** + a link to the intent (`ai-agent-run-sweep-proposal-link-1`).

No visible change from the type promotion, as expected for a type-only refactor.

#### [iOS papercuts #4916 defect #5 — web chat clears pendingApproval on the awaited tool_result] — BLOCKED

The code is present and correct-looking (`apps/web/src/stores/processStreamEvent.ts:125-145`, `case 'tool_result'` with the documented "a result for the awaited tool IS the decision landing" comment), but exercising it needs, in order: an LLM completion → a `tool_use` for an approval-required tool → an open SSE stream → an out-of-band decision from a second session. **Exact missing prereq: no LLM API key is configured on this stack**, so the Workspace chat can never emit a `tool_use`, let alone the `tool_result` that clears the card. Defects 1-4/6/7 of #4916 are mobile-only and out of scope for a browser sweep.

**State left on the stack (Group 3).** Three AI agents: **QA Partner Baseline (triage)** (partner-wide, shadow, enabled, `supervisedActionKeys = ['manage_services:restart']` set by hand), **QA Sweep Triage** (org, triage, re-enabled but switched off), **QA Partner Baseline Triage** (org, patch, *disabled*). Graduation: 24 `ai_agent_op_evidence` `verified` rows + one `ai_agent_graduation` row (`manage_services:restart`, `tracking`, evaluates `eligible`) — these are what make the Promote path reproducible. One **still-pending four-eyes promote approval** in the inbox (the two seeded supervised clones were declined). Two seeded `ai_agent_runs` (`QA sweep seeded run`, triage profile with a ticket proposal + three skip reasons; `QA sweep seeded sweep run`, sweep profile with two findings). Two `ai_agent_impact_daily` rows (2026-09-03/04) — **note: clicking Refresh on the Impact page rebuilds from source facts and will zero these**. Org AI budget ladder left at **45, 75, 92** (was unset); partner AI-budget enforcement was toggled on in the form but **never saved**. Tickets **T-2026-0002** (created here, status Open, one comment) and T-2026-0001, each with one active `reply` draft; T-2026-0002 also has an active `resolution_note` draft. One `ai_alert_verdicts` row with thumbs-up feedback. AI kill switch left **active** (epoch 2). One Workspace chat tab renamed.

**Everyday-workflow checks (Group 3)** — all PASS, run as a technician would:

1. **Create an AI agent, then edit it.** Settings → AI Agents → "Create your first agent" → mode Shadow, name, Save → toast **"Agent saved"**, row appears with a Shadow / "Not running" badge. Re-opened it, ticked **Enabled**, Save → **"Agent saved"**, badge flipped to "Running". New agents are correctly created switched off, and the editor says so ("New agents are created switched off. Review the tool allowlist and the limits below…"). Created a second, **partner-wide** agent via the "APPLIES TO → All organizations" radio → row carries an **"All orgs"** badge and `ai_agents.partner_id` is set with `org_id NULL` (the dual-ownership shape CLAUDE.md requires). The kind dropdown correctly re-expands to all three kinds when the owner scope flips, since kinds are taken per-axis (`freeKinds`, `AiAgentForm.tsx:209`).
2. **Create a ticket, comment on it, change its status.** Tickets → Create ticket → org + subject + description + priority High → toast **"Ticket T-2026-0002 created"** and the workbench opens on it. Posted a reply through the composer → it appears in the feed with author, timestamp and Edit/Delete (no toast: `TicketComposer` delegates to a `runAction`-wrapped sender and the feed entry *is* the confirmation). Changed status New → Open → toast **"Status updated"**, an audit line **"Breeze Admin changed status: New to Open"** in the feed, and the SLA panel appeared with "First response Met / Resolution 23h 59m left". The status/priority/assignee selects are UUID-valued, so drive them by option text, not by value.
3. **Flip the platform AI kill switch and restore it** — see #4704 above. Both directions, reason enforced, epoch 0 → 1 → 2, left active.

**Paper cuts (Group 3)**

| # | Where | Observation | Severity |
|---|---|---|---|
| 1 | `apps/web/src/components/aiAgents/RunDetailPage.tsx:526-533` | `sweepKindLabel` / `sweepSeverityLabel` call the dynamic `t()` with **no unrecognized-token guard**, so an out-of-registry `kind` renders the raw key path to the operator — I saw literal `aiAgentsPage.runs.sweep.kinds.disk` in the CHECK column of the findings table. The neighbouring `SWEEP_PROPOSAL_REASONS` list five lines above exists *precisely* to prevent this for reasons ("Checked before the dynamic `t()` so an unrecognized token renders as itself rather than as a raw … key path"), so the two paths are inconsistent. The outcome column is jsonb with no compile-time shape, and an API-side kind shipping ahead of the locale would hit this in production. | Low |
| 2 | `apps/web/src/components/approvals/ApprovalsInbox.tsx` (pagination footer) | After a batch decline the footer still reads **"Showing 1 of 3"** — the total is stale against the list it labels. Refreshing fixes it. | Low |
| 3 | Approvals inbox, batch decide | The toast for a two-card batch decline is the singular **"Action denied"**. One ceremony deciding N cards should say how many it decided; the count is the whole point of the group affordance. | Low |
| 4 | `/admin/ai-kill-switch` | **"Last changed by"** renders the raw actor UUID (`0d15c84b-b578-414d-8d32-39f2b4176520`) instead of a display name — on the one platform-wide control whose audit trail is the reason the reason-field is mandatory. Every other surface in this sweep resolves actor names server-side (cf. `routes/alerts/actorNames.ts`, which #4705 added for exactly this). | Medium |
| 5 | `apps/web/src/components/alerts/AlertVerdictBadge.tsx` | Voting updates `decided` locally but not `verdict.feedbackByName`, so the new **"Feedback by …"** label only appears after a reload. The voter sees the pressed thumb but not the attribution their own click just created. | Low |
| 6 | Approvals inbox load | Three browser-console **404s** per page load (`GET /ai/agents/graduation?orgId=…&kind={triage,patch,helpdesk}`) — the "always allow" prefetch asks for every kind and a missing partner baseline answers 404 by design. Handled correctly in code, but it makes the console permanently red and buries real errors during triage. | Low |
| 7 | `apps/web/src/components/settings/AiAgentsPage.tsx` (new-agent editor) | The owner-scope default is **"This organization only"** (`defaultOwnerScope`), which walks straight into the Graduation panel's own dead end: create an org-scoped agent and the panel immediately tells you *"Create one with the 'All organizations' scope to begin tracking evidence"*. Given CLAUDE.md's "Partner-Wide First" rule for config-ish surfaces, the partner default would avoid the round trip. | Low |
| 8 | `apps/web/src/components/settings/AiUsagePage.tsx:164-200` | The budget save is the only mutation Group 3 exercised that is not `runAction`-wrapped; it hand-rolls a 3-second inline "Saved successfully". It surfaces its outcome, so it is not a silent mutation, but it is also **not recorded in `runActionAllowlist.ts`** — so neither the guard nor the allowlist accounts for it. | Low |

**Group 3 totals: PASS 8 · PARTIAL 3 · FAIL 0 · BLOCKED 1 · N/A 0.**


### [Group 4 — Org, Contacts, Settings, Audit & Trust] — opus, 2026-09-05T06:20Z

Stack: web `http://localhost:33028`, portal `http://localhost:33028/portal`, API `/api/v1`, built from origin/main `78eae279f`. Logged in as `admin@breeze.local` (platform admin, partner "Default Partner", org "Default Organization" `e26711c9-…`). No SMTP configured, no live agents. Seeded during this pass: contact `Group4 QA Contact <group4.qa@example.com>`, two duplicate-email contacts `Dupe One`/`Dupe Two <dupe.qa@example.com>`, org `Group4 QA Org` (`966a63db-…`, deliberately left mid-archive), portal invites for both emails.

#### [Row 1 · Org contacts #3258 (#4530, #4557, #4546, #4590, #4593, #4594)] — PASS
- ✅ `#contacts` tab renders **"Contacts"** with **"Import CSV"** (`org-contacts-import-button`) and **"Add contact"** (`org-contacts-add-button`), site + role filters, and the empty state *"No contacts yet. Add one, or import a CSV from your previous tool."*
- ✅ Add contact form: name / email / phone / mobile / job title / "Belongs to" site select / 7 role checkboxes (billing, technical, escalation, admin, site, after_hours, portal) / notes / primary checkbox / **"Save contact"** (`contact-form-submit`). Submitting → `POST …/contacts` **201**, toast **"Contact added"**, row rendered with **Edit** / **Delete** actions.
- ✅ **#4590 ambiguous-contact invite warning, both branches proven:**
  - clean 1:1 match → `POST …/portal-users/invite` **200** `{"contactLink":"linked"}` → toast **"Invite sent"**.
  - two contacts sharing `dupe.qa@example.com` → **200** `{"contactLink":"ambiguous"}` → warning toast **"Invite sent, but no contact could be linked — several contacts share this email. This user will not see tickets that were emailed in until a contact is linked."**
- ✅ **#4593 composite-FK deferrability verified in the live DB**: `\d contacts` shows `portal_users_contact_org_fk`, `contact_external_links_contact_org_fk`, `report_schedule_recipients_contact_org_fk`, `tickets_requester_contact_org_fk` and `contacts_site_org_fk` all `DEFERRABLE` — the org-merge contract in CLAUDE.md holds.

#### [Row 2 · Audit log retention #4633/#4824 (#4825, #4900)] — PASS
- ✅ New nav entry **"Audit Retention" / "How long audit logs are kept"** under SECURITY & ACCESS on `/settings/organizations/[id]#audit-retention`. Panel: *"Audit Log Retention — Controls how long this organization's audit trail is kept before older entries are pruned."*, field `org-audit-retention-days` (prefilled `365`), `org-audit-retention-save`.
- ✅ Saving `365 → 180`: `PUT …/audit-retention` **200** `{"configured":true,"retentionDays":180}` + toast **"Audit retention policy saved."**; DB row updated.
- ✅ **#4900 seeded-on-create confirmed end-to-end**: created a brand-new org through the UI (`966a63db-…`, `POST /orgs/organizations` 201 at 06:24:15) and `audit_retention_policies` immediately held a matching row `retention_days=365, created_at=06:24:15` — no manual configuration needed before retention takes effect.

#### [Row 3 · Partner trust probation #4549 W02–W07 (#4567, #4588, #4599, #4603, #4602, #4604, #4558)] — **FAIL**
- ❌ **`/admin/trust-queue` is a hard 500 for every request.** UI renders only *"Unable to load the partner trust queue" / Retry*; `GET /api/v1/admin/trust/queue?limit=50&card=1` → **500** `{"error":"Internal Server Error","message":"Failed query: select \"id\", \"name\", … (SELECT count(*)::int FROM \"devices\" INNER JOIN \"organizations\" ON \"org_id\" = \"id\" WHERE \"partner_id\" = \"id\") from \"partners\" …"}`. API log cause: `PostgresError: column reference "id" is ambiguous`.
- Root cause: `apps/api/src/routes/admin/trust.ts:169-174` — the `deviceCount` correlated subquery interpolates `${devices.orgId}`, `${organizations.id}`, `${organizations.partnerId}`, `${partners.id}` into a raw ``sql`` `` template, and Drizzle emits them **unqualified** (`ON "org_id" = "id" … WHERE "partner_id" = "id"`), so `"id"` matches `devices.id`, `organizations.id` and `partners.id`. Introduced by #4603 (W05).
- Data-independent: reproduced directly in psql against the live DB — `ERROR: column reference "id" is ambiguous`, raised at parse-analysis, so it fires with zero partners in probation too. The evidence tables ("User name", "Cardholder name", "Names match", "Billing region") were therefore never reachable.
- ✅ Partial credit on the standalone action page: `/admin/trust/act?token=…` renders with page title **"Partner trust action"** and degrades correctly on a bad token (*"This link is not valid"*).
- ⚠️ Row prereq is wrong in the inventory: `/admin/trust/act` is **not** reachable without login — `POST /admin/trust/act` is bearer-only by design (`apps/api/src/routes/admin/trustAct.ts:24-30`) and additionally requires a fresh TOTP step-up (`requireFreshMfaStepUp`). The full approve/suspend walk is therefore BLOCKED on this stack (no minted trust-action token + no TOTP-enrolled platform admin), independently of the 500.

#### [Row 8 · #4869 — archiving org stays visible for the drain window] — PASS
- ✅ Archived `Group4 QA Org`: `POST …/archive` **202** `{"status":"offboarding","purgeAt":"2026-12-04T…"}`, in-modal confirmation **"Archiving started — Group4 QA Org is being archived — its agents are being uninstalled now. This finishes automatically within a few minutes. Scheduled to be permanently deleted on 12/3/2026."**
- ✅ DB: `status=offboarding, offboarding_target=archive, purge_at=2026-12-04`.
- ✅ List: the org appears under the **ARCHIVED** section with badge **"Archiving…"** (`org-archived-badge`) + **"Purges in 90 days"** (`org-archived-purge`), not "Archived".
- ✅ Detail pane: `org-archived-readonly-notice` — *"This organization is being archived — its agents are being removed, and it stays read-only until the drain finishes. Restore it to cancel and bring it back."* with a **Restore** button (`org-restore`).
- ✅ `/settings/organizations/966a63db-…#general`: `org-archived-banner` reads *"This organization is **being archived** — its agents are being removed, and a purge is scheduled for 12/3/2026."*, `org-name-input` and `org-name-save` both `disabled`, `org-archived-restore-link` present. Matches #4869's stated intent exactly.

#### [Row 4 · Portal visibility #4562 W01–W10 (#4626, #4627, #4647, #4740, #4744, #4749, #4750, #4734, #4804, #4815, #4598, #4836, #4850)] — PARTIAL
Walked the whole portal as `portal@breeze.local` (`PortalTest123!`) after turning every visibility flag on from Settings → Organizations → Default Organization → **Customer Portal** (`Enable all visibility` + Save → `PATCH …/portal-settings` **200**, toast **"Portal settings saved"**).

What passed:
- ✅ **Dashboard** (`/portal/dashboard`): all seven tiles render — `portal-dashboard-tile-devices` (Devices protected), `-security` (Security score 57 / Fair), `-action-items`, `-patches` (Patches applied this month), `-backup` (Last backup verified), `-support` (2 open requests), `-awaiting-you` — plus the lede *"As of Sep 5, 2026, 06:28 AM (UTC)."*
- ✅ **Security** (`h1: "Security"`): score card, threats-cleared chart, vulnerability severity breakdown incl. "Known to be actively exploited", and the per-device table `Device | Protection | Encryption | Last checked`.
- ✅ **Backups**: overview tiles + per-device table `Device | Last backup | Last restore test | Needs attention | Recovery readiness`. (Copy is plainer than the inventory's "Last restore point / Last test restore / Open breaches / Readiness" — this is the #4850 critique wording, same fields.)
- ✅ **Devices**: table `Device | Type | Status | Last online | Protection`, with **Last patch / Encryption / Last backup / Warranty ends** in the per-row *"More about this device"* disclosure (`DeviceList.tsx:120-123`) rather than as columns. **Export CSV** → `GET /api/v1/portal/devices/export.csv` **200**, `Content-Disposition: attachment; filename="default-organization-devices-2026-09-05.csv"`, header row exactly `Device,Type,Status,Last online,Last patch,Protection,Encryption,Last backup,Warranty ends`.
- ✅ **Tickets**: filed a ticket end-to-end through the portal (`POST /portal/tickets` **201**, `#0-FGFABKX2`); detail page carries `data-testid="portal-ticket-sla-badge"` reading **"SLA on track"**. `portal-support-usage` panel renders **Billed 60 minutes (1 hour) / To be billed 30 minutes / Covered by contract 45 minutes / Pending review 15 minutes** with a per-ticket breakdown row (seeded four `time_entries` to exercise all four buckets).
- ✅ **Reports**: empty state **"No reports yet"**; `portal-reports-generate-posture` → `POST /portal/reports/generate` **201** `status:"completed"` → ledger row with PDF + CSV links; `GET …/pdf` **200** `application/pdf` 53,532 bytes starting `%PDF-`, `GET …/csv` **200** `text/csv`.
- ✅ **Fail-closed at the data layer, per toggle.** Turning **Security** off from the MSP UI (`org-portal-toggle-enableSecurity` + Save, `PATCH` 200): the portal nav item `/portal/security` disappears server-side, and `GET /portal/security/overview` and `/security/devices` both answer **403** `{"code":"PORTAL_SECURITY_DISABLED"}`. Same for **Backups** (`403 PORTAL_BACKUPS_DISABLED`, nav item gone) and **Reports** (nav item gone). Nothing leaks client-side.

What did not:
- ⚠️ **The disabled *page* does not disappear — only `/reports` does.** `apps/portal/src/pages/reports/index.astro:21-23` explicitly bounces `PORTAL_REPORTS_DISABLED` to `/devices` (verified: `/portal/reports` → 302 → `/portal/devices`). `security/index.astro`, `backups/index.astro`, `dashboard/index.astro` and `devices/index.astro` have **no equivalent branch** — they only handle 401 — so a bookmarked `/portal/security` still renders the page shell with the generic transient copy *"We couldn't load your security summary just now. Your IT team can help."* (verified for both Security and Backups). The customer is told a load failed when the feature is in fact switched off, and is invited to chase their IT team about a non-problem. Data is safe; the presentation is wrong and inconsistent with the one page that got it right.
- ⚠️ **`/portal/devices` is gated on `enableSelfService`, not on any visibility toggle** (`apps/api/src/routes/portal/index.ts:33-34`). With the five visibility flags all on but Self-service off (the seeded default), the Devices page 403s and shows *"We couldn't load your devices just now."*, the nav item is absent, and **"Enable all visibility" does not turn it on** — the MSP has to find a checkbox described as *"Customers can use self-service tools in the portal"* to grant a read-only device list. Every #4562 Devices-page column was unreachable until `enable_self_service` was flipped by hand.

#### [Everyday workflow — create an org contact, invite it to the portal, save an audit-retention policy, log into the portal] — PASS
Ran as one continuous flow: added a contact → invited it to the portal (linked) → created a second org (retention policy auto-seeded) → saved an audit retention policy → signed into the portal as a customer → filed a ticket → generated and downloaded a report. No dead ends; every mutation produced a visible outcome.

#### [Row 5 · Auth rejection error-code hardening (#4739, #4651, #4820, #4782)] — PASS
- ✅ **#4739 change-password**: `/settings/profile` → Change password with a wrong current password → `POST /auth/change-password` **400** `{"error":"Current password is incorrect","code":"invalid_credentials"}`, inline error rendered under the field, **still on `/settings/profile`** (the 401 → refresh-and-replay → forced logout path is gone).
- ✅ **#4739 account deletion**: `POST /auth/account-deletion-request` with a wrong password → **400** `{"error":"Invalid password","code":"invalid_credentials"}`.
- ✅ **#4651 MFA proof**: started TOTP enrolment, submitted `000000` → `POST /auth/mfa/enable` **400** `{"error":"Invalid MFA code","code":"mfa_code_invalid"}` with an inline error; not signed out.
- ✅ **#4782 change-password throttle**: five wrong current-password guesses answered **400**, the sixth answered **429** `{"error":"Too many attempts. Please try again later.","retryAfter":272}` — matches the `pwd:change:<userId>` 5 / 5 min limiter, and never a 401.
- ✅ **#4820 portal password change**: same shape on the customer portal — `POST /portal/profile/password` **400** `{"code":"invalid_credentials"}` for the first attempts, then **429** *"Too many attempts. Please try again later."* surfaced inline on `/portal/profile`.

#### [Row 6 · #4860 — collapse the password form under enforceSSO] — PASS
- Seeded a partner-owned active OIDC provider with `enforce_sso = true`, then loaded `/login`: **zero password inputs rendered**. The page shows `partner-sso-button` → **"Sign in with Group4 QA IdP"** plus an explicit escape hatch `show-password-form` → **"Sign in with password instead"**; clicking it reveals the email + password form and `login-submit`. Provider removed afterwards.
- ⚠️ Side observation (pre-existing, deliberate, worth knowing): while `enforce_sso` was on, `POST /auth/login` with the *correct* password answered **401 "Invalid email or password"** (`login.ts:461-482` collapses `SsoPasswordAuthRequiredError` into the generic 401 to avoid user enumeration). The collapsed login form is therefore the *only* thing that tells a locked-out user what happened — worth remembering if the login-context read ever degrades.

#### [Row 7 · #4853 — SSO failure notice outruns the refresh eviction] — PASS
Reproduced the real race rather than simulating it: signed in, revoked all nine of the admin's `refresh_token_families` rows in Postgres (dead refresh cookie, `localStorage` still `isAuthenticated: true` — the exact `STALE_SESSION` arrival state), then hard-loaded `/#ssoCode=bogusgrant-group4qa`.
- ✅ API log shows the issue's own trace: `POST /api/v1/sso/exchange status=400` immediately followed by `POST /api/v1/auth/refresh status=401`.
- ✅ Landed on **`/login?error=sso_exchange_failed`** displaying **"Single sign-on almost completed, but the final sign-in step failed. Please start the sign-in again."** — *not* "Your session expired. Please sign in again to continue.", and not `?reason=session-expired`. The hard `window.location.replace` no longer clobbers the soft redirect.

#### [Row 9 · #4877 — stale PAM risk-tier badge] — PASS
Seeded three org PAM rules directly in Postgres (the write path now 400s an unreachable tier, so the UI branch is only reachable on pre-existing data — which is the whole point of the badge): two pinned to `tier=3` on tools that can no longer resolve to it, one healthy `tier=2` rule.
- ✅ Security → **PAM** → **Rules** shows a **"Stale"** badge on `tool=manage_alerts · tier=3` and `tool=manage_tags · tier=3`, and **no** badge on `tool=manage_alerts · tier=2`.
- ✅ Badge tooltip is the real derived text, not a placeholder: *"Risk tier 3 no longer matches anything this rule can select, so it can never match. Tiers currently in use: 1, 2."* Rules removed afterwards.

#### [Row 10 · #4870 — signed extension asset URLs] — BLOCKED
- Prereq unmet: `installed_extensions` is **empty** on this stack and `BREEZE_WORKSPACE_ENABLED` is off, so no extension ships a web asset and `/extensions/[name]` (the only route under `apps/web/src/pages/extensions/`) has nothing to render — `/extensions` itself is a 404 by design. Verifying "dynamic `import()` loads without a console/network error" needs an installed extension with a web bundle, which cannot be created without an image/env change.
- ✅ Partial, mechanism-level check that does discriminate the fix: unauthenticated `GET /api/v1/extensions/registry` → **401** (bearer gate intact), while unauthenticated `GET /api/v1/extensions/assets/t/<badtoken>/foo/abc123/entry.js` → **404 `{"error":"not found"}`** — *not* 401. The `/assets/*` bearer gate is gone and the signed-token path answers with the no-oracle 404 described in `extensionsWeb.ts:159-163`, which is exactly what makes a header-less `import()` reachable.

#### [Row 11 · #4646 — rotating recovery codes must not evict the caller] — PASS
Enrolled TOTP on `admin@breeze.local` for real (computed a live code from the setup secret), then rotated recovery codes from `/settings/profile`.
- ✅ `POST /auth/mfa/enable` **200** returned ten recovery codes **and a fresh `tokens` object** — the session is re-issued at factor change, not just revoked.
- ✅ `mfa-recovery-regenerate-start` → password step-up → `confirm-regenerate-recovery-codes` → `POST /auth/mfa/recovery-codes` **200** with ten new codes rendered on screen.
- ✅ **The caller was not signed out**: a full page navigation to `/settings/users` immediately afterwards rendered authenticated data (`1 of 1 users`, Breeze Admin row) with no `/login` redirect — so the actor's refresh family survived the `mfa_epoch` bump that revokes every *other* session. MFA disabled again afterwards to leave the stack as found.

#### [Row 12 · #4405 — `agent.command.*` must not read "success" at dispatch] — **FAIL** (API fixed, UI still says Success)
Dispatched a real command to an offline device (`POST /api/v1/security/scan/<deviceId>` → **202**, `queueCommand` → `agent.command.security_scan`).
- ✅ **API half is fixed.** `select action, result from audit_logs where action like 'agent.command%'` → `agent.command.security_scan | dispatched`. No `success` row is written at dispatch any more.
- ❌ **UI half is not.** Devices → the device → **Activities** renders that same row with a green **"Success"** badge. Root cause: `apps/web/src/components/devices/DeviceEventLogViewer.tsx:493` — `const rc = resultConfig[activity.result] ?? resultConfig.success;` and `resultConfig` (same file, lines 140-144) only knows `success` / `failure` / `denied`. The new `'dispatched'` value misses the map and **falls back to `success`**, so #4225's exact user-visible symptom ("a command that later fails still reads as a successful completion") is unchanged on the surface an operator actually looks at.
- The same row is **self-contradicting in one view**: the collapsed row reads `Breeze Admin · Agent · **Success**`, and expanding it shows `Result: **Dispatched**` (the detail pane at line 622 prints the raw value). Fix is one line — add a `dispatched` entry to `resultConfig` (neutral colour) and drop the `?? resultConfig.success` fallback to something unknown-safe.
- Not reproducible in the Audit Trail page (`/audit`): it has no Result column and the "Audit Log Detail" modal omits `result` entirely, so the wrong label is confined to the device Activities tab.

#### Paper cuts (Group 4)

1. **A disabled portal page tells the customer the load failed.** Turn off Security (or Backups, or Dashboard) and a portal user who follows a bookmark gets *"We couldn't load your security summary just now. Your IT team can help."* — an invitation to open a support ticket about a setting the MSP deliberately switched off. `/reports` already does the right thing (302 to `/devices`, `reports/index.astro:21-23`); `security/`, `backups/`, `dashboard/` and `devices/` need the same `PORTAL_*_DISABLED` branch.
2. **"Enable all visibility" does not enable the portal Devices page.** `/portal/devices` is gated on `enableSelfService` (`routes/portal/index.ts:33-34`), a checkbox in the *features* group described as "Customers can use self-service tools in the portal". An MSP who turns on every visibility toggle still gets a Devices page that 403s and a missing nav item, with no hint which switch is responsible. Either move it under Visibility or say so in the toggle's help text.
3. **An org that was just archived still visibly disappears.** #4869's row is correct once you find it — but the org leaves the ORGANIZATIONS list immediately and reappears only inside the **ARCHIVED** disclosure, which is **collapsed by default**. The operator who just clicked Archive watches the row vanish and has to know to expand a second section. Auto-expanding ARCHIVED after an archive (or keeping the row in place, greyed, for the drain) would deliver what the issue actually asked for.
4. **"Invite sent" is shown when no mail was sent.** `POST /portal-users/invite` answers `{"data":{…},"emailSent":false}` when SMTP is unconfigured, and `OrgPortalUsersEditor.tsx:79-86` branches only on `contactLink` — so the MSP sees a green **"Invite sent"** for an email that never left the building. The field is already in the response; a warning variant ("Invite created, but email delivery is not configured") costs one branch.
5. **Creating an organization produces no confirmation toast.** `POST /orgs/organizations` **201** closes the modal and selects the new org, but `OrganizationsPage.tsx:790-800` passes no `successMessage`. Every neighbouring mutation on the same page toasts. The outcome is inferable from the list, so it is a paper cut and not a FAIL — but it is the odd one out.
6. **Disabling MFA logs the actor out, which is the bug #4646 just fixed for rotation.** Disabling TOTP from `/settings/profile` succeeded (`POST /auth/mfa/disable` 200) and immediately bounced the same tab to `/login?next=…&reason=session-expired`. Rotating recovery codes now re-issues the caller's session (#4646); the disable path evidently still routes through the un-patched eviction. Same helper, same blast radius, same fix shape — worth a follow-up issue.
7. **Mixed timezone presentation inside the portal.** The dashboard, backups and support-usage panels all stamp their times "(UTC)"; the ticket detail page renders bare local-browser times ("Created Sep 5, 2026, 12:30 AM" for a 06:30 UTC event) with no zone label. A customer comparing the two reads a six-hour discrepancy.
8. **The portal Security table collapses AV and firewall into one "Protection" column.** Not wrong, and probably deliberate plain-English simplification — but the inventory row and the #4562 spec both describe separate AV/firewall columns, so this is worth confirming as intended rather than lost.

**Group 4 totals: PASS 8 · PARTIAL 1 · FAIL 2 · BLOCKED 1 · N/A 0.**




### [Group 5 — Scripts, Custom Fields & Remote Tools] — opus, 2026-09-05T07:05Z

Stack seeding used for this group (left in place, per sweep convention): custom fields `qa_asset_tag` (org-owned, script-write ON) and `qa_site_tier` (partner-wide dropdown); scripts `QA G5 Custom Field Script` (+ `(copy)`); 9 `script_executions` rows on `e2e-windows.local` spanning every `execution_status`; `devices.helper_lifecycle_mode='on-demand'` on `e2e-windows.local` (was NULL — needed to unhide the session-target control). `devices.status` was temporarily flipped to `online` to open the run/dispatch flow and has been **restored to `offline`**.

#### [#4917 / #4692 / #4722 / #4703 — Script custom-field write-back] — PASS

Both help affordances are real `HelpTooltip`s (`role="tooltip"`, click-toggle as well as hover, `Escape` dismiss — hover-independent as specified).

- Scripts → script → Parameters → Source **"From a device custom field"** reveals a **Custom field key** input with trigger `aria-label="About the device custom field binding"`. Bubble text verbatim: *"Enter the Field Key shown in Settings → Custom Fields (case-sensitive). Read: once bound, the resolved value is delivered to the script just like a normal parameter — no prompt is shown. Write: to save a new value back, have the script call the device's custom-fields API with a PATCH request and an API key."*
- Settings → Custom Fields → create/edit modal → **Field Key** carries the sibling tooltip (`aria-label="About using this field in scripts"`): *"This key is what scripts use to read and write the field. Read: bind a script parameter's source to "From a device custom field" using this key. Write: have the script call the device's custom-fields API with a PATCH request and an API key to save a new value."*
- **#4722 write-back toggle**: `custom-field-script-write` checkbox ("Allow scripts to write this field") present on the create modal with the marker-grammar helper text; the list gains a **SCRIPT WRITE** column rendering a `Script write` badge. DB confirms `custom_field_definitions.script_write = t`.
- **#4722 run-detail panel**: `ExecutionDetails` renders **Custom field writes** with `Applied` / `Rejected`. Seeded `{"applied":["qa_asset_tag"],"rejected":[{"key":"unknown_key","reason":"unknown_field"},{"key":"qa_site_tier","reason":"write_not_allowed"}]}` → panel showed `Applied: qa_asset_tag`, `Rejected: unknown_key — No matching field definition`, `qa_site_tier — write_not_allowed`. (The raw second reason is correct: `write_not_allowed` is not a real `CustomFieldWriteRejection`; the shipped enum value is `not_script_writable`, which *is* translated. Verified against `apps/api/src/services/customFields/scriptWriteBack.ts` before flagging — no defect.)
- **Read path exercised end-to-end**: the device Run-Script parameter step rendered `assetTag — Supplied automatically from device custom field qa_asset_tag` and the submitted payload carried `parameters: {}` (resolved server-side), i.e. no prompt shown, exactly as the tooltip promises.
- #4917 (API-side value validation) was not exercised directly — no browser surface calls it on a path reachable without a live agent.

#### [#4788 / #4799 — Script cancellation #3525 W01-W02] — PARTIAL

**W01 status maps: PASS.** Seeded one execution per `execution_status` value and read every rendered badge:

| DB status | Label | Badge classes | Icon |
|---|---|---|---|
| pending | Pending | `bg-muted text-muted-foreground` | Clock |
| queued | (Queued — in filter) | `bg-muted` | Clock |
| running | Running... | `bg-blue-500/20 text-blue-700` | Loader2 |
| **cancelling** | **Stopping…** | **`bg-warning/15 text-warning border-warning/30`** | **Loader2 (spinner)** |
| completed | Completed | `bg-success/15` | CheckCircle |
| failed | Failed | `bg-destructive/15` | XCircle |
| timeout | Timeout | `bg-warning/15` | AlertTriangle |
| **cancelled** | **Cancelled** | **`bg-muted text-muted-foreground`** | **Ban** |

No row fell through to a default and no render crashed. The status filter dropdown offers `Pending / Queued / Running... / Stopping… / Completed / Failed / Timeout / Cancelled`.

**Cancel action: BLOCKED / not shipped.** There is no cancel affordance anywhere in the Executions list, the execution detail panel, or the device Scripts tab — and that is correct for this release: #4799's own body states the wave "is inert at runtime… nothing in it queues a `script_cancel` yet — the request-side route rewrite is the follow-on". `grep -rn "cancelExecution\|/cancel" apps/web/src/components/scripts/` returns nothing. Stop-in-progress behaviour additionally needs a live agent.

**Extra finding (not a regression, worth knowing before the next wave):** `resolveExecutionStatusLabel` in `apps/web/src/components/scripts/executionStatus.ts:57` is dead code. Neither `ExecutionHistory.tsx` nor `ExecutionDetails.tsx` calls it, and the API list/detail selects (`apps/api/src/routes/scripts.ts:1104-1125`, `:1150-1175`) never return `cancelState`. Proven in the browser: rows seeded `(completed, cancel_state=unconfirmed)` and `(timeout, cancel_state=failed)` still render plain **"Completed"** and **"Timeout"** instead of the `status.completedCancelTooLate` / `status.cancelFailed` strings the resolver exists to produce.

#### [#4906 — run context selectable and visible everywhere] — PARTIAL

Converted surfaces are correct:
- **Script Builder → Test run**: `data-testid="test-run-context"` with accessible name **"Run as"**, options `Script default (System) / System / Logged-in user`. Selecting `Logged-in user` on a device whose helper is `on-demand` reveals `data-testid="test-run-session-target"` with accessible name **"Target session"** and option `Any active session`. Gating is honoured — with `helper_lifecycle_mode = NULL` the session control correctly stays hidden.
- **Execute modal** confirm step echoes the context in prose (*"Run "QA G5 Custom Field Script" on 1 device(s) as System"*), and `ExecutionDetails` shows a **Run context: System** chip.

**Gap — the device "Run Script" picker was not converted.** In `apps/web/src/components/devices/ScriptPickerModal.tsx`:
- `:291-299` `data-testid="script-run-as"` has **no `<label>` and no `aria-label`** — the words "Run as" survive only inside the option text (`Run as: System`), so the combobox has no accessible name.
- `:301-315` `data-testid="script-session-target"` has **no label at all, visible or accessible**. Confirmed live: selecting `Run as: Logged-in user` renders a bare dropdown whose only option reads `Any session (console)`, with nothing on screen saying it is the session target.

Everywhere else #4906 touched uses the shared `RunContextSelect` (`apps/web/src/components/common/RunContext.tsx:95,112,141`), which does emit both labels — so this is one unconverted surface, not a broken pattern. It is also the highest-traffic one (device page → Run Script).

#### [#4897 — Duplicate script row action] — PASS

`/scripts` row action carries `title="Duplicate script"`. Click → `POST /api/v1/scripts/04f3a592…/clone` → **201** with `"name":"QA G5 Custom Field Script (copy)"`, then navigation to `/scripts/d6ef78f4…` (the clone's edit page), which is the visible confirmation. No success toast by design — `ScriptsPage.tsx:141-145` wraps the call in `runAction` with `errorFallback: t('scriptsPage.errors.duplicate')` and no `successMessage`; the failure toast string **"Failed to duplicate script"** is present at `apps/web/src/locales/en/scripts.json:811`.

#### [#4895 — "Run again" on a past execution] — PARTIAL

**Pre-fill: PASS.** Executions → a past execution → **Run again** re-opens the Execute Script modal with the original device already selected (`Clear (1)`, `1 device(s) selected`, its checkbox checked), `Run As` restored to the run's own context (System), and the bound parameter shown as `assetTag — Supplied automatically from device custom field qa_asset_tag`.

**Submit: PASS on outcome.** Execute → Confirm Execute → `POST /scripts/:id/execute` body `{"deviceIds":["e65460f3…"],"parameters":{},"runAs":"system"}` → **201** `{"status":"queued","targets":[{"admission":"admitted","executionId":"2a137cf4…","commandId":"f7cf4a14…"}]}`. In-modal confirmation reads *"All selected devices were admitted and queued — 1 of 1 devices admitted to the queue · E2E Windows Test Device · Queued"* for ~1.5 s, then the list refreshes and the new `Pending` row appears (7 → 8 rows).

**Gap vs the row's stated expectation:** it does **not** navigate to the new execution and shows **no success toast**. That half of the PR (#4886) is wired only in `apps/web/src/components/scripts/ScriptsPage.tsx:193-195` (`navigateTo(deviceScriptsHref(...))` for a single-device run, `scriptExecutionsHref(...)` otherwise). `ScriptExecutionsPage.tsx:145-170` — the page that actually hosts "Run again" — just calls `fetchExecutions()` and returns. A single-device Run again therefore leaves the operator on the list instead of on `/devices/:id#scripts/:executionId`. The device-page run path *does* navigate correctly (verified: URL became `…#scripts/b5c1d6b8…`), so this is one missing wiring, not a broken feature.

#### [#4893 — survive a failed xterm stylesheet preload] — PASS

The literal prereq (throttle the network so the `<link rel=stylesheet>` preload fails) is **no longer reachable**, and that is the fix: `RemoteTerminal.tsx:183` now imports `@xterm/xterm/css/xterm.css?inline` and injects the CSS as a `<style>`, so there is no second asset to go stale. Verified in the browser instead of by throttling:

- `document.getElementById('xterm-css')` exists with **7112 chars** of xterm CSS; `document.querySelectorAll('link[rel=stylesheet][href*=xterm]')` is **empty**.
- Styles are actually applied: `.xterm-viewport` computes `position: absolute`, `.xterm-helper-textarea` computes `position: absolute` (the offscreen input that keyboard/IME depends on).
- **Cold first mount** — a fresh full page load straight to `#terminal` — produced a live pane with the welcome banner, `Connecting to E2E Windows Test Device…`, `Error: Device is not online`, `Failed` and a **Retry** button. That is precisely the symptom #4152 reported as broken (permanently "Disconnected", no banner, no reconnect).
- Idempotency holds: after a Terminal → Processes → Terminal round trip there is still exactly **one** `#xterm-css` element.
- The `console.warn("Terminal stylesheet failed to load; continuing with an unstyled terminal")` catch never fired, as expected.

#### [#4641 — Remote Tools tab in the URL hash + terminal fills the viewport] — PASS

- Clicking **Terminal** rewrites the URL to `…/remote/tools?deviceId=…#terminal`.
- A full page load at that URL comes up **on the Terminal tab** with xterm mounted (no bounce back to Processes). `#processes` works as an entry point too.
- Viewport fill: the `.xterm` element measures **637 px** tall in a 900 px viewport — i.e. it grows into the `flex-1 min-h-0` tab area rather than stopping at the old 400 px floor.
- Prereq note: no connected agent, so this was exercised against the failed-connection pane. That still proves both #4512 (hash) and #4510 (layout), which are pure client-side concerns.

#### [#4791 — partner-wide custom-field gate + dropdown options contract] — PASS

Create modal exposes **APPLIES TO → All organizations (partner-wide) / This organization only** (`custom-field-owner-partner` / `custom-field-owner-org`), correctly **create-only** (the edit modal omits it, matching the partner-wide playbook).

- Created a **partner-wide dropdown** with choices Gold/Silver → `POST /api/v1/custom-fields` **201**, body `{"orgId":null,"partnerId":"6bcf5db3…","type":"dropdown","options":{"choices":[{"label":"Gold","value":"gold"},{"label":"Silver","value":"silver"}]}}`. Postgres agrees: `custom_field_definitions` row has `org_id` NULL, `partner_id` set, `options = {"choices":[{"label":"Gold","value":"gold"},{"label":"Silver","value":"silver"}]}`. The pre-fix `z.array(z.string())` schema would have 400'd this.
- List renders the partner-wide row with an **"All organizations"** badge; the org-owned row has none.
- **Edit round-trip does not wipe choices** (the `normalizeChoices` half of the fix): re-opening the dropdown field for edit repopulated `Gold/gold` and `Silver/silver` rather than blank inputs.
- Device-level consumption works: the partner-wide field appears on the device Details tab and its editor offers Gold/Silver.

#### [Everyday-workflow checks] — PASS (1 paper cut)

1. **Create a script and run it against a device (dispatch half): PASS.** Built `QA G5 Custom Field Script` in the Script Builder (Monaco content, PowerShell, Windows, one custom-field-bound parameter) → toast **"Script created"**. Device → Run Script → `POST /scripts/:id/execute` **201** `{"status":"queued","targets":[{"admission":"admitted","executionId":"b5c1d6b8…","commandId":"9b5f765f…"}]}`, toast **"Script "QA G5 Custom Field Script" queued for e2e-windows.local"**, URL moved to `…#scripts/b5c1d6b8…`. Postgres evidence: `script_executions` row `status=pending, run_as=system` and `device_commands` row `type=script, status=pending`. Execution beyond dispatch is BLOCKED (no live agent).
   Nice touch worth keeping: with the device offline the **Run Script** button is `disabled` with `title="Device is offline"` rather than failing after the fact.
2. **Create a custom field and set it on a device: PASS.** Both fields created from Settings → Custom Fields; device Details tab → Custom Fields → inline edit → selected `Gold` → toast **"Custom field saved"**, value persisted. `devices.custom_fields = {"qa_site_tier": "gold"}` in Postgres.
3. **Open Remote Tools on a device and read the offline state: PASS for Terminal, paper cut for Processes.** Terminal states the problem plainly (`Error: Device is not online` + Retry). The **Processes** tab, on the same genuinely-offline device, swallows a `503` from `/api/v1/system-tools/devices/:id/processes` and renders `Processes 0 · Total CPU 0.0% · Process Memory 0 KB · No Data · Showing 0 of 0 processes` — a tech reads "this box has no processes", not "we can't reach it". Error is console-only (`RemoteToolsPage.tsx:382`).

#### [Extra — Execution History date/duration defects] — FAIL

Two real, user-visible bugs on the Executions surface, both re-confirmed after a hard reload (not HMR artifacts):

1. **A not-yet-started execution shows an epoch date.** `script_executions.started_at` is NULL while a run is `pending`/`queued`, the API returns `startedAt: null`, but `apps/web/src/components/scripts/ExecutionHistory.tsx:31` types it as a non-nullable `string` and `:338` passes it straight to `formatDateTime`. Result: the STARTED cell renders **"Dec 31, 05:00 PM"** (`new Date(null)` → epoch 0) for every pending row. Reproduced on 3 separate pending executions.
2. **The DURATION column is dead.** `formatDuration(execution.duration)` at `ExecutionHistory.tsx:347` reads a `duration` field the API never sends — the list select at `apps/api/src/routes/scripts.ts:1104-1125` has no `duration` and no computed alias, so every non-`running` row renders **"-"**. Confirmed on a completed run with `started_at` and `completed_at` exactly 60 s apart: list shows `-`, and the execution detail panel likewise shows `Duration —` next to a populated `Started At Sep 5, 2026, 12:29:53 AM` and `Completed At Sep 5, 2026, 12:30:53 AM`. Only the `running` branch renders anything, and it renders the literal word "Running...", not a duration.

Neither is attributable to a Group 5 PR (both predate this range), but both sit on the surface #4788/#4895 ship into, and #1 is the kind of thing a customer screenshots.

**Paper cuts (Group 5)**

1. **Two adjacent help texts describe mutually exclusive write mechanisms.** On the same Custom Fields modal, the `scriptWrite` checkbox helper (from #4692) says a script writes by *printing* `::breeze:custom-fields:: {"key":"value"}`, while the Field Key `HelpTooltip` (#4703) says to *call the device's custom-fields API with a PATCH request and an API key*. Both are true, but a tech reading them together has no idea which one to use, and the tooltip omits the marker path that the very checkbox beside it enables. The Parameter Binding tooltip in the Script Builder repeats the API-key-only version.
2. **`script-session-target` in the device Run Script picker has no label whatsoever** (`ScriptPickerModal.tsx:301-315`) — a bare combobox reading "Any session (console)". `script-run-as` beside it has no accessible name either. Both are the exact controls #4906 set out to make legible; this one surface was missed.
3. **Remote Tools → Processes renders a 503 as an empty table** (`RemoteToolsPage.tsx:382`) with headline stats reading `Processes 0 · Total CPU 0.0%`. Silent-failure shape: the operator cannot distinguish "unreachable" from "idle". Terminal, one tab over, gets this right.
4. **Pending executions show "Dec 31, 05:00 PM"** — see the FAIL block above. Low effort to fix (`startedAt?: string | null` + a `—` fallback), high embarrassment if a customer sees it.
5. **Execution DURATION never populates** — a whole column of `-`. Either compute it API-side or drop the column.
6. **"Run As" vs "Run as" collide on the Script Builder page.** The Execution Settings block has a **Run As** select (System/Current User/Elevated — the script's stored default) and the Test run row has a **Run as** select (Script default/System/Logged-in user — this one run's override). Same words, different scope, ~400 px apart, and one of them is invisible until you inspect the aria-label.
7. **Neither Custom Fields create/edit nor Duplicate Script emits a success toast.** Both are defensible (inline `formError` and navigation-to-the-clone respectively, and `CustomFieldsPage.tsx` is deliberately outside the `runAction` set), but the create modal simply vanishing after "Create Field" reads as a no-op until you spot the new table row.
8. **"Run again" from the executions list doesn't take you to the run it just started**, while the same action from the device page does — see the #4895 block. Inconsistent enough to look like a bug.

**Group 5 totals: PASS 5 · PARTIAL 3 · FAIL 1 (extra, unlisted) · BLOCKED 0 · N/A 0.**



## UI/UX paper cuts

| # | Where | Observation | Severity | Disposition (fixed <sha> / issue # / noted) |
|---|---|---|---|---|

## Fixes applied

All on this branch (`qa/sweep-post-v0.109.0`), each red-test-first, eslint + `tsc --noEmit` clean.

| Commit | PR/area | What | Test |
|---|---|---|---|
| `06d23ac0b` | Billing / #3205 W07 (G1 row 1) | `InvoiceWorkspace` `readTab` parses the first `&`-segment; `selectTab` preserves other segments → draft device appendix opens | `InvoiceWorkspace.test.tsx` |
| `5d23653d5` | Devices / #3207 W5 (G2 row 1) | `DeviceDetailPage` transform carries the five `reboot*` fields → `RebootScheduledBadge` renders | `DeviceDetailPage.rebootScheduled.test.tsx` |
| `35de8c8d6` | Devices (G2 paper cut 1) | permanent-delete undo toast names the device (`{{hostname}}`, 8 locales) | `DevicesPage.test.tsx` + `localeParity` |
| `aea92c3c8` | Config policies (G2 paper cut 2) | `AssignmentsTab` add/remove wrapped in `runAction` with success toasts; added to `no-silent-mutations` TARGET_GLOBS | `no-silent-mutations.test.ts` |
| `62cef544e` | AI agents (G3 paper cut 2) | `RunDetailPage` sweep kind/severity labels guard unknown tokens | `RunDetailPage.test.tsx` |
| `061993c07` | Approvals inbox (G3 paper cut 3) | batch decide toast pluralised (8 locales); footer total decremented after batch | `intentApprovals.test.ts`, `ApprovalsInbox.test.tsx` |
| `5ecc2d870` | Devices activity log / #4405 (G4 row 12) | `resultConfig` gains `dispatched`; unknown results render neutrally instead of "Success" | `DeviceEventLogViewer.test.tsx` |
| `6f7bc0f7c` | Scripts (G5 FAIL) | `ExecutionHistory`/`ExecutionDetails`: nullable `startedAt` (no epoch-0 date); duration computed from timestamps | both co-located tests |
| `1eb7be710` | Scripts / #4886 (G5 row #4895) | `ScriptExecutionsPage` "Run again" navigates to the new execution | `ScriptExecutionsPage.test.tsx` |
| `705282d05` | Scripts (G5 row #4906) | `ScriptPickerModal` run-context selects get accessible names | `ScriptPickerModal.test.tsx` |
| `8498c8714` | process | gh-queue skill: staleness sweep is a full pass; zero-comment community issues are waiting on me | — |

Merged separately to `main` during the sweep: **#4926** `8ff2834d5` (RLS-scoped replay migration + `REPLAYED_BY` assertion) and **#4930** (admin trust-queue 500, G4 row 3, with a real-Postgres integration test).

## Issues filed

| Issue | Title | From row |
|---|---|---|
| #4922 | docs(deploy): upgrade guide never tells operators to refresh docker-compose.yml (compose drift re-exposes TRANSPORT-001) | pre-sweep (#2987) |
| #4931 | [Admin] AI kill switch "Last changed by" shows a raw user UUID | G3 paper cut 1 |
| #4932 | [Portal] Disabled visibility pages show an error message instead of redirecting | G4 paper cut 1 |
| #4933 | [Portal] "Enable all visibility" does not enable the portal Devices page | G4 paper cut 2 |
| #4934 | [Auth] Disabling MFA logs the acting user out (reason=session-expired) | G4 paper cut 3 |
| #4935 | [Remote Tools] Processes tab swallows a 503 into "No Data" | G5 paper cut 2 |
| #4936 | [Devices] Maintenance mode only reachable via Bulk Actions; toasts do not pluralise | G2 paper cut 3 |
| #4937 | [Quotes] title input collapse / illegal reprice offer / triple "Included quantity" label | G1 paper cuts 1-3 |

## Summary

| Group | PASS | PARTIAL | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|
| 1 Billing, Quotes, Contracts & Accounting (11) | 8 | 2 | 0 | 1 | 0 |
| 2 Devices, Patching & Reboot (9) | 8 | 1 | 0 | 0 | 0 |
| 3 Tickets, AI Agents & Workspace (12) | 8 | 3 | 0 | 1 | 0 |
| 4 Org, Contacts, Settings, Audit & Trust (12) | 8 | 1 | 2 | 1 | 0 |
| 5 Scripts, Custom Fields & Remote Tools (8) | 5 | 3 | 0 (+1 unlisted) | 0 | 0 |
| **Total (52 rows)** | **37** | **10** | **2 (+1)** | **3** | **0** |

Defects found: 7 — all fixed on this branch or in #4930 (merged). Everyday-workflow checks (14 across groups): all PASS.

**Top findings:**

1. **Explicit-whitelist transforms drop new fields** (systemic, 4th instance per its own comment): `DeviceDetailPage.tsx` dropped the five `reboot*` fields so `RebootScheduledBadge` was unreachable — the reboot-deferral feature's only end-user web surface never rendered. Fixed here; a type-level exhaustiveness guard is the durable fix (not a 10-line change, left as a note).
2. **Raw `sql` templates with interpolated Drizzle Column objects emit UNQUALIFIED identifiers** — `admin/trust.ts` 500'd on every request (`column reference "id" is ambiguous`), even with zero partners in probation. Drizzle-mock unit tests cannot catch SQL-text bugs; #4930 adds a real-Postgres integration test. Same lesson already documented next to `routes/software.ts`'s versionCount subquery.
3. **Defaulting an unknown outcome to "Success"** — `DeviceEventLogViewer` badged `dispatched` commands as Success (the exact #4223 symptom #4405 set out to fix on the API side). Fixed to render unknown results neutrally.
4. **Composed URL hashes**: `InvoiceWorkspace` read the whole hash as the tab, so any `&devices=` segment snapped a draft back to Editor. Fixed reader and writer.
5. **Non-nullable timestamps rendering epoch 0** (`ExecutionHistory` pending rows → "Dec 31, 05:00 PM") and a DURATION column reading a field the API never returns.
6. **HMR artifacts are a real false-positive source** when fix agents run concurrently with the sweep: two Group 3 "defects" vanished when click+read happened in one `browser_evaluate`. Documented in-line so they are not re-filed.
7. **Inventory error**: Group 3 row 5's click-path ("AI triage batch view") does not exist; the real surfaces are RunDetailPage + ApprovalsInbox.
8. **Process**: a fixer's `git reset --hard` wiped the uncommitted tracking doc mid-sweep; it was rebuilt by replaying the sweep agents' Edit/Bash calls from their transcripts. Commit the doc after every group, not at the end.

**Oldest PR reached:** #4406 (Group 3 / #4384 LRU) — every web/portal PR in the range has a row.

**Before the release cut:**
- Merge this PR (fixes + doc). #4926 (`8ff2834d5`) and #4930 (`efe501335`) are already on main.
- Flags: none new are required. `BREEZE_WORKSPACE_ENABLED` needs a pgvector Postgres image (unchanged from v0.109.0).
- BLOCKED rows needing a live agent / external service: G1 row 2 (QuickBooks OAuth, `QBO_*` env), G3 row 12 (#4916 defect 5, needs an LLM key + `tool_use`), G2 row 1 W2-W4 (reboot prompt on a real endpoint — the pre-release real-box dialog check from the #3207 handoff still stands).
- Manual: confirm prod Postgres >= 15 (org-contacts column-list `ON DELETE SET NULL` syntax); run `~/Documents/breeze-release-checks/prod-rls-grant-check.sql` once per region (expects `rolbypassrls = t` and `roles_missing_grant = 0`); after deploy read the W05 site-stamp backfill WARNING count and remove `AUDIT_CHAIN_VERIFY_ENABLED=false` from the US droplet; QuickBooks hosted-rollout checklist in `docs/release-notes/next-release-draft.md`.
- Open product decisions: #4933 (portal Devices gate), #4936 (per-device maintenance mode).
