# RMM-QA-164 Partner Admin `force_mfa` Seed Design

**Goal:** Make `force_mfa = true` an invariant of every system-defined Partner Admin role on every creation path — the global template seeded by `seedRoles()`, the per-tenant copy inserted by `createPartner()`, and rows that already exist on installed databases — so that a password-only Partner Admin is told to enrol (`mfaEnrollmentRequired: true`), is minted an access token with `mfa: false`, and is refused by every `requireMfa()` gate until a real factor exists. Finding RMM-QA-164 (S1, `docs/qa/evidence/2026-08-22-partner-role-mfa-matrix.md` in the QA repo) proved the opposite on a fresh stack: the applied migration `2026-05-25-f-role-force-mfa.sql` promised the flag, the freshly seeded row stored `false`, login minted `mfa: true`, and a command-free invalid write behind `requireMfa()` reached child validation.

**Branch:** `fix/rmm-qa-164-partner-admin-force-mfa-seed`, cut from `origin/main` at `0fb5af40d`. The verified brief was written against `6f496f4d0`; main moved by two commits on this surface (`c77fceaa2` #3608 partner inbound-email default, `b04192a76` #4215 device org-move migration). Neither touches the defect. The only consequence is the migration ordering ceiling: the max shipped migration is now `2026-09-29-detach-ticket-runs-on-device-org-move.sql`, not `2026-09-28-quickbooks-entity-mappings.sql` as the brief states (§D4).

**September 5 continuation:** the unshipped reconcile migration now sorts after main `a52504b9ee` and its latest migration `2026-10-09-000600-rls-scoped-replay-v0110.sql`. Active paths below use the final filename; baseline facts and original reasoning remain historical.

**Method note:** every file:line below was re-opened in the worktree at `0fb5af40d`. Claims not executed are labelled *inferred*. Rigor is HIGH (auth + migration): every behaviour change is RED-first with the failing output retained in the commit message; every test control is proven to discriminate by mutating the implementation, watching it fail, and reverting.

## Non-goals and boundaries

- No change to `2026-05-25-f-role-force-mfa.sql` or any other shipped migration; the fix is forward-only.
- Org Admin, Partner Technician and every other system role stay `force_mfa = false` (D9). The migration header records this as the MSP opt-in posture; nothing here changes it.
- No change to the MFA policy resolver (`services/mfaPolicy.ts`), the login mint (`routes/auth/login.ts:650-652`), the verify-email mint (`routes/auth/verifyEmail.ts:476-480`) or the 428 gate (`middleware/auth.ts:618-656`). All four already read the stored flag correctly; the stored flag is the defect.
- No web change (D8). The web store's 428 handler already redirects to `/auth/mfa/setup?forced=1`.
- Custom roles (`is_system = false`) that happen to be named "Partner Admin" never inherit the invariant (D2, D4, D11).
- No production deployment, no hosted-fleet rollout claim. The rollout note (§6) is a description of the deploy-time effect, not a schedule.
- The QA probe/evidence files under `/Users/toddhebebrand/breeze-rmm-qa/docs/qa/` are not edited. (No file under `docs/qa/probes/` carries the ID; the characterization lives in `docs/qa/evidence/2026-08-22-partner-role-mfa-matrix.md` and the helper `docs/qa/run-local-partner-role-matrix.mjs`, which reads `SELECT name,id,force_mfa FROM roles` and asserts the login `mfaRequired`/claim shape.)

## 1. Verified facts (worktree at `0fb5af40d`)

| # | Fact | Evidence |
| --- | --- | --- |
| F1 | `SYSTEM_ROLES` (10 entries) carries `name`, `scope`, `description`, `permissions` only — no `forceMfa` on any entry. | `apps/api/src/db/seed.ts:231-379` (Partner Admin at 233-238) |
| F2 | `seedRoles()` looks up `existing` by `eq(roles.name, roleDef.name)` alone — no `is_system`, `partner_id` or `org_id` filter — and on the existing branch only logs. The insert passes `{ name, scope, description, isSystem: true }`. | `seed.ts:846-883` |
| F3 | `roles.force_mfa` is `boolean NOT NULL DEFAULT false`. | `apps/api/src/db/schema/users.ts:97`; `migrations/2026-05-25-f-role-force-mfa.sql:18-19` |
| F4 | `autoMigrate()` applies every migration (step 7), re-runs `ensureAppRole()`, then seeds only when `users` is empty (step 8). On a blank database the `2026-05-25-f` `UPDATE` therefore runs against an empty `roles` table and is recorded as applied; the seeded row is never revisited. | `apps/api/src/db/autoMigrate.ts:700-720` |
| F5 | `2026-05-25-f` is a one-shot `UPDATE roles SET force_mfa = true WHERE name = 'Partner Admin' AND is_system = true AND force_mfa = false`. Its header names Partner Admin as the only forced role and Org Admin et al. as intentionally opt-in. | `migrations/2026-05-25-f-role-force-mfa.sql:7-25` |
| F6 | `createPartner()` inserts the tenant Partner Admin copy as `{ partnerId, scope: 'partner', name: 'Partner Admin', description, isSystem: true }` — no `forceMfa`. It later reads the global template with `name = 'Partner Admin' AND is_system = true AND partner_id IS NULL` to copy permissions. | `apps/api/src/services/partnerCreate.ts:103-111, 142-156` |
| F7 | Login: `mfaEnrollmentRequired = ENABLE_2FA && !user.mfaEnabled && policy.required`; `mfaSatisfied = !ENABLE_2FA \|\| (!user.mfaEnabled && !policy.required)`; the token carries `mfa: mfaSatisfied`; the response carries `mfaEnrollmentRequired` and `enrollUrl`. Verify-email computes the same pair from `combineMfaPolicyFacts({ roleForceMfa: roleRow.forceMfa === true, … })`. | `routes/auth/login.ts:650-652, 668, 783-784`; `routes/auth/verifyEmail.ts:474-480, 792-793` |
| F8 | `getEffectiveMfaPolicy()` derives `roleForceMfa` solely from the stored `roles.force_mfa` joined through `partner_users` / `organization_users`; `combineMfaPolicyFacts()` applies the kill switch only to the role-force component. There is no name-based Partner Admin force anywhere. | `services/mfaPolicy.ts:85-105, 132-150` |
| F9 | `requireMfa()` / `hasSatisfiedMfa()` accept `token.mfa === true` (or `ENABLE_2FA` off). The 428 enrollment gate in `authMiddleware` calls the same resolver for non-exempt paths. | `middleware/auth.ts:855-888, 618-656`; exempt list `:212-240` |
| F10 | `MFA_FORCE_FOR_PARTNER_ADMIN` defaults ON and is read at call time (`envFlag`). `ENABLE_2FA` defaults ON. | `config/env.ts:436-438`; `routes/auth/schemas.ts:10` |
| F11 | The epoch trigger `breeze_roles_permissions_epoch` fires `AFTER UPDATE OF … force_mfa ON roles` and bumps `users.permissions_epoch` for every member of the role via `organization_users` ∪ `partner_users`. | `migrations/2026-08-06-b-live-authorization.sql:170-206` |
| F12 | `registerPartnerMfaPolicy.integration.test.ts` sets `ENABLE_2FA=true`, `MFA_FORCE_FOR_PARTNER_ADMIN=true` before dynamic import, seeds the global template with `force_mfa = true` in `beforeEach`, and still installs a `BEFORE INSERT` trigger `breeze_test_role_force_mfa` to model "the row state a corrected seed would produce". Its control test asserts that with no trigger the mint is `mfa: true`. | `apps/api/src/__tests__/integration/registerPartnerMfaPolicy.integration.test.ts:21-25, 52-58, 132-160, 200-244` |
| F13 | `seed.test.ts` asserts permission topology on `SYSTEM_ROLES` only (zero `forceMfa` assertions); `partnerCreate.test.ts` captures every `tx.insert(...).values(...)` into `insertCalls` and asserts table membership and partner values (zero `forceMfa` assertions). | `apps/api/src/db/seed.test.ts:84-160`; `apps/api/src/services/partnerCreate.test.ts:5-50, 134-170` |
| F14 | `seed-idempotency.integration.test.ts` already runs `seedPermissions()` + `seedRoles()` twice against real Postgres and asserts role/grant counts. `setup.ts` truncates `roles` per test (`cleanupDatabase`). | `apps/api/src/__tests__/integration/seed-idempotency.integration.test.ts:75-118`; `setup.ts:209-245` |
| F15 | `permission-epoch.integration.test.ts` proves an `UPDATE roles SET force_mfa` bumps member epochs by exactly 1 and rolls back with the transaction. | `permission-epoch.integration.test.ts:255-297` |
| F16 | Migration ordering is `localeCompare`; the max shipped file is `2026-09-29-detach-ticket-runs-on-device-org-move.sql`. The preferred new-file form is `YYYY-MM-DD-HHMMSS-<slug>.sql`; rule 3 (sort strictly after every committed file) is enforced only by `scripts/check-migration-naming.sh --staged` (pre-commit). `autoMigrate.test.ts` asserts every `migrations/<file>.sql` path referenced from `apps/api/src` resolves. | `autoMigrate.ts:369-373`; `scripts/check-migration-naming.sh:30-55`; `migrations/README.md:141-146`; `autoMigrate.test.ts:580` |
| F17 | The root `docker-compose.yml` maps `ENABLE_2FA` but NOT `MFA_FORCE_FOR_PARTNER_ADMIN`; only `deploy/docker-compose.prod.yml:263` maps it, and only `deploy/.env.example:184` documents it. `envComposeParity.test.ts` fails when a var documented in an `.env.example` is not referenced by its paired compose file. | `docker-compose.yml:278`; grep of `MFA_FORCE_FOR_PARTNER_ADMIN` across compose/env files; `apps/api/src/config/envComposeParity.test.ts:9-38` |
| F18 | The CI `smoke-test` job (non-blocking on PRs, required on `main`) boots the root compose stack from a generated `.env`, logs in as the bootstrap admin (`BREEZE_BOOTSTRAP_ADMIN_EMAIL`), and expects HTTP 200 from ~20 `authMiddleware`-gated `GET` routes with that token. The bootstrap admin is created by `seed()` as a `partner_users` member of the global Partner Admin role with `mfaEnabled` at its default `false`. | `.github/workflows/ci.yml:2040-2200`; `seed.ts:1165-1210` |
| F19 | The CI `auth-browser-transition` job seeds via `pnpm db:seed`, logs in as `admin@breeze.local`, and exercises only `/auth/*` routes (`browser-binding/bootstrap`, `login`, `refresh`, `logout`); `/auth/refresh` is not behind `authMiddleware`. Local Playwright suites (`e2e-tests/tests/{auth,login,tickets,pam,intent-self-approve}.spec.ts`) log in as the seeded admin and drive the dashboard. | `ci.yml:2300-2390`; `e2e-tests/browser-contracts/auth-browser-transition.spec.ts:1-40`; `routes/auth/login.ts:790, 854` |
| F20 | The only two code paths that create a role named "Partner Admin" are `seed.ts` and `partnerCreate.ts`. The name-based readers (`accountDeletion.ts:88`, `tenantOffboarding.ts:1771`, `seed.ts:1171`, `partnerCreate.ts:147`) read; none write `force_mfa`. The test helper `db-utils.createRole` inserts without `forceMfa` (stays `false`; unaffected). | grep `'Partner Admin'` across `apps/api/src`; `__tests__/integration/db-utils.ts:254-269` |
| F21 | Web: `apiLogin()` drops `mfaEnrollmentRequired`/`enrollUrl` from its return shape and `LoginPage` navigates to `/` (or `/setup`); the first `authMiddleware`-gated fetch then hits the store's 428 handler, which hard-navigates to `/auth/mfa/setup?forced=1`. `apiVerifyEmail()` likewise drops the field. | `apps/web/src/stores/auth.ts:1193-1200, 1239-1295, 1745-1800`; `components/auth/LoginPage.tsx:241-275`; `VerifyEmailPage.tsx:52-66` |

## 2. Decisions

**D1 — `SYSTEM_ROLES` carries an explicit `forceMfa: boolean` on every entry, typed.** Add `type SystemRoleDefinition = { name; scope; description; permissions: string[]; forceMfa: boolean }` and declare `SYSTEM_ROLES: readonly SystemRoleDefinition[]` (or `satisfies`), with `forceMfa: true` on Partner Admin and `forceMfa: false` on the other nine. Rationale: an optional field on one entry makes `roleDef.forceMfa` a union-typed access and hides the posture of every other role; an explicit boolean per role makes the definition the readable source of truth and lets the unit test assert "exactly one role is forced". Cost-if-wrong: nine extra lines; zero runtime cost.

**D2 — `seedRoles()` narrows its `existing` lookup to the global system row and reconciles one-directionally.** The lookup becomes `and(eq(roles.name, roleDef.name), eq(roles.isSystem, true), isNull(roles.partnerId), isNull(roles.orgId))` (`isNull` is a new import from `drizzle-orm`; `and` already imported). The insert passes `forceMfa: roleDef.forceMfa`. On the existing branch: `if (roleDef.forceMfa && !existing.forceMfa)` → `db.update(roles).set({ forceMfa: true }).where(eq(roles.id, existing.id))`, logged as `Role reconciled (force_mfa): <name>`. This satisfies verifier concern 1 verbatim: a custom `is_system = false` "Partner Admin" or a tenant copy (`partner_id` set) can no longer be matched by the seed, so it cannot be flipped or mistaken for the template. One-directional because the migration header (F5) frames Org Admin et al. as per-deployment opt-ins; a bidirectional "make it equal the definition" would silently revert an operator's raised flag on every `pnpm db:seed`. Tenant copies are reconciled by the migration alone (D4), exactly as the verifier asked. Side effect accepted: on a database whose global template is missing but a custom same-name row exists, the narrowed lookup now creates the missing template — the correct outcome, since `createPartner()` (F6) requires that exact row. Cost-if-wrong: if a future definition ever needs to *relax* a role, it must ship an explicit migration — the right friction for an auth flag.

**D3 — `createPartner()` writes `forceMfa: true` as a literal on the tenant Partner Admin insert.** Not copied from the global template: the template lookup happens after the insert (F6), and the invariant is "system Partner Admin forces MFA", not "whatever the template currently says". The env kill switch (F10) remains the single relief valve, and it applies uniformly to every tenant. Cost-if-wrong: an operator who wants no forcing must set `MFA_FORCE_FOR_PARTNER_ADMIN=false` rather than editing a row — which is the documented mechanism.

**D4 — One idempotent forward migration reconciles installed databases, filtered to system rows.** File `apps/api/migrations/2026-10-11-170000-partner-admin-force-mfa-reconcile.sql` (preferred `HHMMSS` form; sorts strictly after `2026-09-29-detach-ticket-runs-on-device-org-move.sql` — re-verify the max at execution time, main moves; a `2026-09-29-100000-…` name would sort *before* the `-d…` file because digits precede letters). Body:

```sql
DO $$
DECLARE n integer;
BEGIN
  UPDATE roles
  SET force_mfa = true
  WHERE scope = 'partner'
    AND name = 'Partner Admin'
    AND is_system = true
    AND force_mfa = false;
  GET DIAGNOSTICS n = ROW_COUNT;
  IF n > 0 THEN
    RAISE WARNING 'partner-admin force_mfa reconcile: flipped % system Partner Admin role(s) to force_mfa=true', n;
  ELSE
    RAISE NOTICE 'partner-admin force_mfa reconcile: 0 rows needed flipping';
  END IF;
END $$;
```

It flips the global template (`partner_id IS NULL`) and every tenant copy (`partner_id` set); it excludes `is_system = false` rows by construction (openDecision 4). No inner `BEGIN;`/`COMMIT;`; re-application is a true no-op (`force_mfa = false` predicate). The count is always logged (the `2026-06-10-c` lesson: a zero is evidence too). Verifier concern 3 satisfied: `2026-05-25-f` is untouched and the new file sorts last.

**D5 — The epoch side effect is accepted and documented, not suppressed.** Each flipped tenant row fires `breeze_roles_permissions_epoch` (F11), which bumps `permissions_epoch` for every member of that role: a one-time permission-cache invalidation and, for every unenrolled Partner Admin, a 428 enrollment wall on their next non-exempt request. That is the intended security outcome of the finding. The migration test (§4 T5) pins it: exactly one bump per member on first apply, zero on re-apply. The rollout note (§6) names `MFA_FORCE_FOR_PARTNER_ADMIN=false` as the relief valve. Verifier concern 2 satisfied.

**D6 — Make the relief valve reachable in the self-host stack.** Add `MFA_FORCE_FOR_PARTNER_ADMIN: ${MFA_FORCE_FOR_PARTNER_ADMIN:-}` to the `api` service environment block of the root `docker-compose.yml` (beside `ENABLE_2FA`, F17) and a commented `# MFA_FORCE_FOR_PARTNER_ADMIN=true` entry with the kill-switch explanation in the root `.env.example` (mirroring `deploy/.env.example:181-184`). Without this, a self-hoster locked out by D4/D5 has no documented escape short of `psql`. The parity guard (F17) requires the two edits to land together. Cost-if-wrong: none at runtime (`${VAR:-}` passes an empty string, which `envFlag` treats as unset).

**D7 — CI smoke keeps its 200 expectations via the relief valve and gains a stored-flag assertion.** Add `MFA_FORCE_FOR_PARTNER_ADMIN=false` to the smoke job's generated `.env` (F18) so the bootstrap admin's token is minted `mfa: true` and the endpoint sweep stays meaningful; add one step after "Login and export token" that asserts the seeded template stored the flag: `docker compose exec -T postgres psql -U … -d breeze -tAc "SELECT force_mfa FROM roles WHERE name = 'Partner Admin' AND is_system AND partner_id IS NULL"` must print `t` (exact service name/credentials to be taken from the job's own compose invocation at implementation time — *inferred*, not yet executed). This is the compose-stack replica of the finding's headline symptom ("fresh stack stored false") and complements the real-Postgres integration proof. Alternative rejected: enrolling TOTP inside the smoke — heavier, and the integration suites already prove the mint behaviour on real Postgres. Cost-if-wrong: the smoke no longer exercises the 428 wall; that is covered by §4 T3/T4 and the existing `auth.test.ts` gate tests.

**D8 — No web change in this PR.** `mfaEnrollmentRequired` is not threaded through `apiLogin`/`apiVerifyEmail` (F21). The exit-evidence contract is API-level; the existing 428 handler lands the user on `/auth/mfa/setup?forced=1` on the first protected fetch after login. Recorded as a follow-up UX item (one extra round trip and a possible flash of the dashboard shell), not smuggled into an auth/migration PR. Cost-if-wrong: UX only; no assurance impact.

**D9 — Org Admin and all other system roles stay `forceMfa: false`.** Matches the migration header (F5) and openDecision 1. The reconcile in D2 never lowers a raised flag, so an MSP that opted Org Admin in keeps it.

**D10 — `MFA_FORCE_FOR_PARTNER_ADMIN` stays the runtime relief valve; no new flag.** The resolver already scopes it to the role-force component only (F8); settings-driven `requireMfa` is unaffected.

**D11 — Custom same-name roles are excluded on every path**: the D2 lookup filter, the D4 `is_system = true` predicate, and the D3 literal apply only to rows the system creates. Pinned by §4 T2 and T5.

**D12 — `registerPartnerMfaPolicy` loses its role trigger and gains a kill-switch control.** Remove `breeze_test_role_force_mfa` (install + attach + drop) and rewrite the header (F12 lines 21-25) to state that `createPartner` now stores the corrected row itself; keep the settings-axis trigger. The role-axis test asserts, with no trigger, that the real `createPartner` row is `force_mfa = true`, `body.mfaEnrollmentRequired === true`, `accessClaims.mfa === false`, `enrollUrl === '/auth/mfa/setup'`. The existing control ("still mints `mfa: true` when nothing requires MFA") can no longer be produced by `createPartner` — after D3 every fresh tenant admin is forced — so it becomes "kill switch off → still mints `mfa: true`": set `process.env.MFA_FORCE_FOR_PARTNER_ADMIN = 'false'` inside that test (restored in `finally`), which is legal because `mfaForcePartnerAdmin()` reads the env at call time (F10). This keeps the pair self-checking: the role-axis assertions (`mfa === false`) can only pass when `ENABLE_2FA` resolved true (F7's short-circuit would mint `true`), and the control can only pass when the kill switch is honoured. Verifier concern 4 satisfied (the file already sets `ENABLE_2FA='true'` before its dynamic imports; the spec keeps that and makes the role-axis assertion the guard).

**D13 — Deviation from the brief's test list, stated.** The brief puts "insert values include `forceMfa`" in `seed.test.ts`. `seed.test.ts` imports `seed.ts` without mocking the DB and asserts definitions only (F13); mocking the Drizzle chain for `seedRoles()` there would duplicate what the real-Postgres test proves. The insert-values proof therefore lives in `seedPartnerAdminForceMfa.integration.test.ts` (§4 T2), and `seed.test.ts` asserts the definition posture (T1). `partnerCreate.test.ts` already captures insert values (F13), so its assertion is unit-level as the brief lists.

## 3. Contracts per file

| File | Change | Guard |
| --- | --- | --- |
| `apps/api/src/db/seed.ts` | D1 type + `forceMfa` on all ten `SYSTEM_ROLES` entries (Partner Admin `true`). D2: `isNull` import; narrowed `existing` lookup; `forceMfa: roleDef.forceMfa` on insert; one-directional reconcile `UPDATE` on the existing branch with a log line. No other change (bootstrap-admin section untouched). | T1, T2 |
| `apps/api/src/services/partnerCreate.ts` | D3: `forceMfa: true` on the roles insert at `:103-111`. | T3, T4 |
| `apps/api/migrations/2026-10-11-170000-partner-admin-force-mfa-reconcile.sql` | D4 body verbatim, header comment naming RMM-QA-164, the autoMigrate ordering cause (F4), the epoch side effect (D5) and the relief valve. | T5; `check-migration-naming.sh --staged`; `autoMigrate.test.ts` |
| `apps/api/src/__tests__/integration/registerPartnerMfaPolicy.integration.test.ts` | D12. | T4 (self) |
| `apps/api/src/__tests__/integration/seedPartnerAdminForceMfa.integration.test.ts` (new) | T2. | — |
| `apps/api/src/__tests__/integration/partnerAdminForceMfaMigration.integration.test.ts` (new) | T5; references the migration by path (`readFileSync(join(__dirname, '../../../migrations/2026-10-11-170000-partner-admin-force-mfa-reconcile.sql'))`, the `recoveryAuthorizationSubjectMigration` pattern). | `autoMigrate.test.ts:580` path-resolution assertion |
| `apps/api/src/db/seed.test.ts` | T1 added. | — |
| `apps/api/src/services/partnerCreate.test.ts` | T3 added. | — |
| `docker-compose.yml`, `.env.example` | D6. | `envComposeParity.test.ts` |
| `.github/workflows/ci.yml` (smoke job only) | D7: `MFA_FORCE_FOR_PARTNER_ADMIN=false` in the generated `.env`; new stored-flag assertion step. | smoke run on the PR (non-blocking) — read its log, do not trust the green |
| `e2e-tests/README.md` | One paragraph: a fresh stack's seeded admin is now MFA-forced; local suites need either TOTP enrolment for `admin@breeze.local` or `MFA_FORCE_FOR_PARTNER_ADMIN=false` in the stack's `.env` (F19). | — |

Nothing else is touched. In particular: no schema change (`force_mfa` already exists, so `pnpm db:check-drift` is unaffected and `CORE_TENANT_EXPORT_POLICY` needs no new column entry), no RLS/cascade registry change (no new table/column), no change to `mfaPolicy.ts`, `login.ts`, `verifyEmail.ts`, `middleware/auth.ts`, or the web app.

## 4. RED test list (write first, watch fail, retain the failing output in the commit message)

Each test is followed by its discrimination proof: the mutation that must turn it red again after GREEN.

- **T1 `seed.test.ts` — "every system role declares its MFA posture; only Partner Admin is forced"**: `for (const r of SYSTEM_ROLES) expect(typeof r.forceMfa).toBe('boolean')`; `expect(SYSTEM_ROLES.filter(r => r.forceMfa).map(r => r.name)).toEqual(['Partner Admin'])`. RED today: `forceMfa` is `undefined` (F1) — a type error at compile plus the runtime assertion. Discriminates: flip Partner Admin to `false` → fails; add `forceMfa: true` to Org Admin → fails.
- **T2 `seedPartnerAdminForceMfa.integration.test.ts`** (real Postgres, `it.runIf(DATABASE_URL)`, `roles` truncated per test by `setup.ts`, re-seed permissions first as `seed-idempotency` does):
  - (a) blank → `seedRoles()` → the global template (`is_system AND partner_id IS NULL AND org_id IS NULL`) has `force_mfa = true`; every other system row `false`; row count `= SYSTEM_ROLES.length`. RED today: stored `false` (F2/F3).
  - (b) `UPDATE roles SET force_mfa = false` on the template → `seedRoles()` again → `true`; row count unchanged. RED today: the existing branch only logs.
  - (c) insert a custom row `{ name: 'Partner Admin', scope: 'partner', is_system: false, force_mfa: false }` and a tenant copy `{ partner_id: <partner from db-utils.createPartner>, is_system: true, force_mfa: false }` → `seedRoles()` → both remain `false`; the template stays `true`; no duplicate template row. Pins D11 and the "tenant copies belong to the migration" split.
  - (d) raise the global Org Admin template to `true` by SQL → `seedRoles()` → still `true` (one-directional, D2/D9).
  - (e) delete the template, keep the custom same-name row → `seedRoles()` → a new template row exists with `force_mfa = true`; the custom row is untouched. RED today: the name-only lookup reports "Role exists" and creates nothing (F2).
  - Discriminates: revert the D2 filter → (c)/(e) fail; drop the reconcile → (b) fails; make the reconcile bidirectional (`set forceMfa: roleDef.forceMfa`) → (d) fails.
- **T3 `partnerCreate.test.ts` — "the tenant Partner Admin role is inserted with forceMfa: true"**: `const roleCall = insertCalls.find(c => c.table.__t === 'roles'); expect(roleCall.values).toMatchObject({ name: 'Partner Admin', isSystem: true, forceMfa: true })`. RED today: `forceMfa` absent (F6). Discriminates: change the literal to `false` or delete it → fails.
- **T4 `registerPartnerMfaPolicy.integration.test.ts`** (D12): role-axis test with no trigger asserts stored `true`, `mfaEnrollmentRequired: true`, claim `mfa: false`, `enrollUrl`. RED today: stored `false`, claim `true`, `mfaEnrollmentRequired: false` (F6/F7 — exactly the QA symptom). Kill-switch control asserts claim `mfa: true` and `mfaEnrollmentRequired: false` with `MFA_FORCE_FOR_PARTNER_ADMIN='false'`. Discriminates: revert D3 → role-axis fails; set `ENABLE_2FA` false in the file → role-axis fails (proves it is not vacuous under the short-circuit); remove the env override in the control → control fails (proves the control depends on the valve, not on an absent policy).
- **T5 `partnerAdminForceMfaMigration.integration.test.ts`** (real Postgres; replays the migration file as the schema owner, same as `recoveryAuthorizationSubjectMigration`): fixtures — global template `false`, tenant copy `false` under a `createPartner` fixture with one `partner_users` member, a custom `is_system = false` same-name row `false`, the Org Admin template `false`; record the member's `permissions_epoch`. Apply once → template and tenant copy `true`; custom row and Org Admin `false`; member epoch `+1`. Apply again → identical rows; member epoch unchanged. RED today: `ENOENT` on the migration path (the file does not exist), which also fails `autoMigrate.test.ts:580`. Discriminates: remove `is_system = true` from the predicate → the custom-row assertion fails; remove `scope = 'partner'` → add an `organization`-scope system row named "Partner Admin" to the fixture set and its assertion fails; drop the epoch assertion's fixture member → the test can no longer distinguish "flipped" from "already true". Note on the `force_mfa = false` predicate: the epoch trigger compares `OLD.force_mfa IS NOT DISTINCT FROM NEW.force_mfa` (F11), so an unconditional same-value `UPDATE` on re-apply would *not* bump the epoch either — the predicate's only observable is the logged row count. Decision: the test asserts idempotency on row state + epoch; the `0 rows` re-apply count is captured from the Postgres notice stream in the RED/GREEN evidence (postgres.js `onnotice` on the test client) and recorded in the PR, not asserted by the test.
- **Existing regression evidence kept green and cited in the PR** (no edits): `middleware/auth.test.ts:1020-1035` (`requireMfa` rejects `token.mfa=false`), `routes/auth/login.test.ts:770-800` (mints `mfa:false` + `mfaEnrollmentRequired:true` under a required policy), `routes/auth.test.ts` TOTP/recovery stale-pending suites, `__tests__/integration/pendingMfaEpoch.integration.test.ts`, `permission-epoch.integration.test.ts:255-297`, `seed-idempotency.integration.test.ts`.

## 5. Verification battery

Local, before push:

- Unit (Test API job scope): `pnpm --filter @breeze/api test -- src/db/seed.test.ts src/services/partnerCreate.test.ts src/db/autoMigrate.test.ts src/config/envComposeParity.test.ts src/middleware/auth.test.ts src/routes/auth/login.test.ts src/services/mfaPolicy.test.ts`; typecheck.
- Real Postgres (fresh private disposable stack, torn down after; `vitest.integration.config.ts` with `DATABASE_URL`): `seedPartnerAdminForceMfa`, `partnerAdminForceMfaMigration`, `registerPartnerMfaPolicy`, `seed-idempotency`, `permission-epoch`, `emailRecoveryRegistration`, `auth-browser-transition`, `pendingMfaEpoch`, `rls-coverage` (no new table, expected unchanged), plus a fresh-DB `autoMigrate` run to confirm the new file applies in order and logs the count.
- Blank-stack replay of the QA symptom (the exit-evidence row): boot the root compose stack from a blank volume with defaults (kill switch ON), log in as the bootstrap admin, assert the response carries `mfaEnrollmentRequired: true`, the decoded access token carries `mfa: false`, and a command-free invalid write behind `requireMfa()` returns `403 MFA_REQUIRED` (or 428 from the gate) instead of reaching child validation. Then `SELECT force_mfa FROM roles WHERE name='Partner Admin' AND is_system AND partner_id IS NULL` → `t`. Record the transcript in the PR.
- Migration gates: `bash scripts/check-migration-naming.sh --staged` on the commit that adds the file; `autoMigrate.test.ts` (path resolution + ordering); `pnpm db:check-drift` (expected no-op).
- Discrimination pass: for T1–T5 apply the listed mutation, run, confirm red, revert; keep the red output in the commit message or PR body.
- Post-push: confirm workflow runs attached to the head; read the smoke job log for the new stored-flag step (the job is non-blocking on PRs, so a green check is not evidence); one independent review round (this touches auth + a migration, so the fix round itself gets a review only if it re-touches those surfaces).

## 6. Rollout note (for the PR body and release notes)

On the first boot after upgrade, `2026-10-11-170000-partner-admin-force-mfa-reconcile.sql` flips every system Partner Admin role that is still `force_mfa = false` — the global template and one row per tenant. Each flipped tenant row bumps `permissions_epoch` for that tenant's Partner Admin members (one-time cache invalidation), and every Partner Admin without an enrolled factor receives `428 mfa_enrollment_required` on their next non-exempt request until they enrol at `/auth/mfa/setup` (the web app redirects there automatically). Fresh installs seed the flag directly, so the bootstrap admin must enrol MFA before the setup wizard's protected calls succeed. Relief valve, if an enrolment outage locks operators out: `MFA_FORCE_FOR_PARTNER_ADMIN=false` in `.env` (now mapped in the root compose as well as the prod compose), which suppresses only the role-force component; settings-driven `requireMfa` stays enforced. The migration logs the flipped-row count as a `WARNING` (or `NOTICE … 0 rows`) in the Postgres log.

## 7. Ship sequence

1. Spec commit (this file).
2. RED commits: T1, T3 (unit), T2/T4/T5 (integration; T5 red on `ENOENT`) — each with the failing output in the message.
3. GREEN commits, one per surface: `seed.ts` (D1, D2); `partnerCreate.ts` (D3); the migration (D4, committed with the hook enabled so rule 3 runs); `registerPartnerMfaPolicy` rewrite (D12); compose/env (D6); CI smoke + e2e README (D7).
4. Discrimination pass, battery (§5), push, exact-head CI, one review round, then hand back for the exit-evidence re-run of `run-local-partner-role-matrix.mjs` on a blank stack.

## 8. Non-claims

- Not claimed: that any hosted or self-hosted database has been reconciled. The migration exists; it runs at each deployment's next boot.
- Not claimed: that Org Admin or any other role should force MFA. Deliberately unchanged (D9).
- Not claimed: that the web app navigates to enrolment immediately from the login response. It navigates on the first 428 (D8).
- Not claimed: that the smoke job proves the 428 wall. It proves the stored flag and endpoint reachability with the valve off (D7); the wall is proven by T4 and the blank-stack replay.
- Not executed: the exact `docker compose exec … psql` incantation for D7 and the migration's logged count in T5 (both marked *inferred* above; both are verified during implementation, not asserted by this spec).
- The Codex advisor round (read-only, `gpt-5.6-sol`) was convened on D1–D8; its findings, if any, are recorded in the implementation plan rather than here if they arrive after this spec is committed.
